[
  {
    "id": "example-replace-me",
    "query": "describe a real task here, naming the function/module/subsystem it touches",
    "gold_ids": [
      "code:path/to/module.py::function_name",
      "doc:path/to/guide.md::section-slug",
      "hub:some-topic"
    ],
    "note": "TEMPLATE — edit before use. List the node ids that SHOULD surface for this query; copy exact ids from `inspect_graph.py` (or `inspect_graph.py --grep <word>`). These placeholder ids resolve in no real graph, so eval_retrieval reports recall 0 here and the consolidation eval_gate SKIPS (it never falsely rejects on unresolved cases). Point retrieval/tuning and `eval_gate.cases` at this file once you have labeled a handful of real tasks, or at your own cases file."
  }
]
