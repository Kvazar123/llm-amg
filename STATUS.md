# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап:** **Этап 16 (командная работа и git-граф) — В РАБОТЕ.** Группы 1–2 (устойчивость хранилища под общий доступ; устойчивость к конфликтам) **выполнены**; впереди группы 3–4. Этап 15 закрыт и выпущен (v1.6.0, на `origin`). **20 прогонов селфтестов зелёные** (19 движка + `selftest_install`; +5 новых кейсов групп 1–2) + mypy (17 модулей) + eval-демо 1.00.
- **Этап 16 — контур (согласован) и ход:**
  - **Замысел:** два канала — общая папка (single-writer lock, низкая конкурентность) + **опциональный граф-в-git** (трекать `nodes/*.md`, реальный markdown-merge, отчёт о конфликтах, рекомендованный `.gitignore`). **Существующий git проекта, read-only по умолчанию; своей VCS нет.** Командный режим — **минимально-аддитивно** (ветка/commit в status, read-only сравнение, per-branch как документированный паттерн). Полные решения — roadmap §5 «Этап 16».
  - **Переносимость:** правки — в универсальных скриптах (`graph_store`/`lifecycle`/`reconcile`), НЕ в хуках; работают и в бесскиловых/Codex-средах без сессионных хуков (петлю ведёт модель прямыми вызовами). Деградация контеншена есть и в `lifecycle`, и в `graph_store` CLI.
  - **Группа 1 — выполнено:** `graph_store._lock_is_stale` host-aware (чужой-host lock не крадётся по локальной пробе pid — только по возрасту) + docstring `lock`; мягкая `StoreLockError`-деградация в `lifecycle._heal`/`repair`/`session_end` и в `graph_store` CLI `recover`/`verify --repair`. Тесты: `selftest_graph_store.case_lock_cross_host`, `selftest_lifecycle.case_shared_folder_contention`.
  - **Группа 2 — выполнено:** три парсера узлов (`reconcile.parse_node`/`retrieve._parse`/`consolidate._parse`) защищены от `yaml.YAMLError` + non-dict (битый узел теперь пропускается, не валит `load_nodes`); `reconcile.find_conflict_markers` (регэксп `<<<<<<<`/`>>>>>>>`/`|||||||`, `=======` исключён ради setext); вывод в `reconcile.plan` summary (`conflict_markers`), `lifecycle.status` (поле `conflicts` + строка), `repair`/`session_start` (нота). Тесты: `selftest_reconcile.case_merge_conflict_resilience`, `selftest_consolidate.test_conflict_skip`, `selftest_lifecycle.case_merge_conflict_surfaced`.
  - **Группы 3–4 (впереди):** 3) `_git_branch` best-effort + ветка/commit в `status` + свежесть по коммиту в `verify_claims`; 4) доки по слоям (consistency-model §9 / 03-storage / GUIDE / READMEs / 09-config) + `.gitignore`-рецепт + read-only сравнение + **строка Этапа 17 в `BASE-PROMPT.md`**.
  - **Не трогаем:** `migrate_schema`, `index_store`, `extract_structure`, логику `consolidate`, `embed`, `_provenance` (ветка — на уровне store, не схемы узла).
  - **Карта:** найден §1.36 (`log.md` → обычный `.log` без markdown-заголовков) — записан в roadmap §1, **вне периметра Этапа 16**, отдельным косметическим исправлением / Этап 18.
- **Этап 15 — что реализовано (детали свёрнуты в roadmap §5 «Этап 15»; CHANGELOG [1.6.0]):**
  - **`export_graph.py`** (новый, read-only, `skills/amg-retrieve/scripts`): скан `nodes/*.md` → `{meta, nodes, links}` с **полным frontmatter** (не проекция `_node_from_meta` — панели нужен весь узел); ядро `build_graph_data` → JSON (`--json`) и **инлайн-HTML** (данные+`3d-force-graph`+glue в одном файле; данные в `<script application/json>` с экранированием `</`, т.к. `file://`+`fetch`=CORS). Связи из `edges`(+`coact`)+`part_of`-к-хабу, висячие отброшены. Только пишет `cache/graph.html`.
  - **`viewer/`**: вендоренная `3d-force-graph.min.js` 1.80.0 (MIT, байт-в-байт, `VENDOR.md`), `viewer.template.html`, `viewer.js`. Цвет по бакету + подсветка вердиктов арбитража, размер по степени, толщина по `w`(+`coact`), фильтры/поиск/кластер/тема (GitHub-палитра, день-ночь), large-graph mode (hubs-first+expand+скрытие слабых рёбер), пауза рендера на скрытой вкладке, кроссбраузерные префиксы.
  - **Запуск:** CLI, скилл `amg-retrieve`, `/amg view` (прямой read-only вызов, НЕ через lifecycle), словесно. Блок конфига **`viewer`** (`quality` auto/high/medium/low; `large_graph_mode` auto/on/off; `large_graph_nodes`; `min_edge_weight`; сквозной passthrough `options` в библиотеку). `selftest_export.py` (8 кейсов).
  - **Доки по слоям:** `10-eval-tools` (основной — экспортёр/вьюер), `09-config` (блок `viewer`), `01-overview`/`02-data-model` (карта/`cache/graph.html`), GUIDE «3D-просмотр графа» (опережающее→реализовано), READMEs; чекпойнт **2.12 закрыт**. **Новой теории нет** (визуализация инструментальна).
- **Найдено на Этапе 15 → исправить на Этапе 18 (проход по промптам, рядом с §1.33):**
  - **§1.34** — захардкоженные русские примеры намерения в `entrypoint/CLAUDE.md` (столбец «Verbal intent» + пары синонимов). Своё (триггеры вьюера в `CLAUDE.md`/`SKILL.md`) исправлено на этапе; пред­существующее — на Этап 18. Прочие блоки (`AGENTS.md`/`AGENTS.codex.md`/`commands`) и `agents/*.md` уже английские.
  - **§1.35** — словесные команды памяти не привязаны к ключу «AMG»/память: общая фраза («покажи граф», «подведём итоги») может ложно запустить `/amg`. Фикс — во всех блоках+доках сделать триггеры срабатывающими только при явной ссылке на память; назначен на Этап 18.
- **Перенос графа между средами (вопрос пользователя на Этапе 15):** делается **копированием папки `<agent_dir>/amg/`** — `nodes/` это канон markdown, env-агностичный (id и `source_path` проектно-относительны); править лишь `agent_dir`/`entrypoint` в `config.yml`, кэши (`cache/`/`journal/`) пересобираемы. **JSON-импорт НЕ нужен** (был бы лоссовым обходом канона). Если документировать — GUIDE/Этап 16. JSON-экспорт (`--json`) остаётся для сторонних инструментов анализа, не для переноса.
- **Среда/процедуры (durable):** **torch/эмбеддинги РАБОТАЮТ**. Гейт типов: `python -m mypy` без аргументов → **17 модулей** (+`export_graph`; предупреждение `[mypy-selftest_*]` — давнее, безвредно, exit 0). **20 прогонов селфтестов зелёные** (19 движка + `selftest_install`; новый `selftest_export`), `eval --make-demo` pack-recall 1.00. **Коммитить через Bash-heredoc** (PowerShell 5.1 ломает многострочный commit); пуш на закрытии — `git push --follow-tags`. Дев-проверка вьюера: `node --check viewer.js` (Node есть в дев-среде; в продукт НЕ входит — вьюер это готовый UMD-бандл). Стенды `../amg-bigtest` и `../amg-testbed` **НЕ удалять** (замер Хебба на реальном usage — Этап 17 з.8); демо-HTML вьюера лежат в их `cache/graph.html`. `BASE-PROMPT.md` — gitignored. Не-Claude-Code-среды — Этап 19.
