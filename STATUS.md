# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап:** **Этап 14 (эпистемический арбитраж противоречий + улучшенное хеббово правило) — В РАБОТЕ.** Этап 13 закрыт и выпущен (v1.4.0). Порядок групп (подтверждён): **Гр.1 правило весов** → Гр.2 арбитраж-ядро → Гр.3 извлечение+промпты → закрывающая сессия. Детальная строка загрузки — в `BASE-PROMPT.md`. 18 селфтестов движка + mypy (16 модулей) зелёные.
- **Этап 14 — Группа 1 (СДЕЛАНО, не закоммичено на момент записи):** `consolidate.fold_weights` переписан на **outcome-gated reward (headroom `rate·(1−w)`) + exposure-gated decay**: награда — co-used пары принятой сессии из `work/usage.log`; затухание — рёбра, показанные в пакете (`coactivation.log`), но не использованные (демотирует «магистрали»); поддержан `reverted` (−`rate·w`, авто-детекция отложена). `coactivation.log` больше НЕ усиливает (только `coact`→значимость + экспозиция). `usage.log` потребляется (→`archive/`) ТОЛЬКО при `apply_hebbian:on`; иначе копится (substrate). `lifecycle.session_end` переставлен `dump → record_usage → fold → digest` (исход сессии доступен свёртке). `_usage_pairs` хелпер, `USAGE_ACCEPTED/REVERTED`. `config.yml`/докстринги синхронизированы. Тесты `test_weights`/`test_hebbian_demo`/`selftest_usage.case_separation` переписаны под новую семантику.
- **Этап 14 — измерение правила весов:** **bigtest строго положителен** (`../amg-bigtest/_measure_big.py`, синтетический исход gold-as-used): recall/hop растут МОНОТОННО по 1→2→4→8 фолдам и всем засевам (BM25 0.60→0.83 / 0.25→0.68; m2v 0.60→0.85 / 0.31→0.72; ST 0.58→0.85 / 0.22→0.72) — инверсия слепого правила (−0.10/−0.14). **dogfood testbed** (33-узл. плотный): не вредит (BM25 инертно, ST +0.03/+0.04, pack 1.0). **Решение: дефолт `apply_hebbian` остаётся `false`** — синтетический сигнал исхода доказывает корректность+пользу-на-графе, не среднюю при РЕАЛЬНОМ usage; свёртка вне eval-gate; правило безопасно (no-op без usage, outcome-gated). Флип ждёт реального usage. `../amg-bigtest/README.md` обновлён результатом.
- **Хебб — доки измерения:** THEORY §8.1/§8.2 (числа + толкование, **без маркеров этапов** — теория вне времени, см. ниже), `07-consolidation` (правило/формулы headroom·(1−w)/exposure-decay, источники usage/coact), `06-retrieval` (роли журналов) — **синхронизированы в этой сессии** (по запросу пользователя). **На закрытии Этапа 14:** добавить в **GUIDE и READMEs** понятное последовательное руководство «когда включать Хебб (большой разрежённый граф + накопленный реальный `usage.log`), когда оставлять `false`» (правило безопасно: no-op без usage, outcome-gated, ограничено потолком); опц. упомянуть протокол off/on в `10-eval-tools`. **Дефолт `apply_hebbian` остаётся `false`; перевод в `true` — Этап 17, задача 8** (замер на РЕАЛЬНОМ usage; синтетика gold-as-used доказала лишь корректность+пользу-на-графе).
- **Принцип (обратная связь пользователя):** в THEORY **не должно быть ссылок на этапы** — этапы живут в архитектуре/роадмапе; при удалении текста — минимальная правка без переформулировок/избыточности. Реальные ситуации Хебба (для будущего GUIDE): помогает — большой разрежённый multi-hop граф + слабый засев + реальный usage; инертно — малый/плотный граф или сильный засев, и всегда no-op без usage; известного «хуже» у нового правила нет (причина вреда слепого — «магистрали» — структурно устранена), единственный непроверенный риск — шумный реальный usage (свёртка вне eval-gate).
- **Этап 13 — решённые развилки (подтверждены пользователем):** (1) схема без дублирования — плоские `source_path`/`source_hash`/`lineno`/`line_end` и есть провенанс файлового узла; блок `provenance` несёт только `kind`+опц.`commit`, а для авторских/синтезированных — `derived_from` (task 3 «source ids»); (2) `verify_claims.py` — read-only по умолчанию + опц. `--write`, БЕЗ авто-встраивания в `consolidate` (node.status уже несёт хеш-staleness; авто-стэмп дал бы редундантный сигнал, стоимость re-extract и риск ложного `verified ✓`; живой before-answer чек строго качественнее снимка).
- **Этап 13 — Группа 1 (сделано, детали в roadmap §5 «Этап 13» → «Прогресс»):** поля `line_end`/`confidence`/`provenance`/`verification` (schema_version→3); `extract_structure` line_end; `reconcile` provenance+commit+verification(added/changed/move), сброс verification при changed, line_end на дрейфе; `apply_derivation` confidence (+`derived_from` у синтезированных); `notes` kind/confidence/verification (+`--kind`/`--confidence`); проекция индекса (`index_store`+`retrieve._node_from_meta`); `migrate_schema` backfill. Новые тест-кейсы в reconcile/notes/index/migrate/stage2.
- **Этап 13 — Группа 2 (сделано, детали в roadmap §5 «Этап 13» → «Прогресс»):** новый `verify_claims.py` (read-only + опц. `--write`; пере-нарезка источника → `verified`/`stale`/`contradicted`/`skipped`; целевые id по одному файлу, без полного скана; метод `ast`/`grep`/`doc`); `retrieve._trust_marks` (stale/unverified/contradicted/low-conf — флаги, не понижение) + диапазонный указатель `path:start-end`; блок конфига `verification` (поднят в `retrieve.load_config`; `default_confidence` читает `reconcile`); промпты `amg-builder`/`amg-synth` (эхо confidence/`derived_from`), `amg-retriever`/`SKILL`/`entrypoint` («verify before answer» через `verify_claims.py`); DRY-рефактор `extract_structure._units_for_path`+`units_for_file`; `verify_claims` в mypy-гейте (16 модулей). Промпт-`.claude` рендерятся под `agent_dir` (проверено рендером в `.agents`).
- **Этап 13 — Группа 3 (сделано, детали в roadmap §5 «Этап 13» → «Прогресс»):** `retrieve._log_pack` → `work/pack-log.jsonl` (состав пакета id+source_path, гейт `log_coactivation`, отдельно от слепого `coactivation.log`); `lifecycle.session-end` собирает правленные файлы из стенограммы (`_EDIT_TOOLS`), `_record_usage` пересекает с пакетами → `work/usage.log` (`used`/`edited_files`/грубый `outcome: completed`); pack-log потребляется на session-end; `consolidate` usage.log НЕ читает. Новый `selftest_usage`.
- **Этап 13 — закрытие выполнено:** docs синхронизированы (`02-data-model` «Слой доверия», `05-reconcile`, `06-retrieval` пометки/`verify_claims`/pack-log, `09-config` блок `verification`, `08-agents-skills` usage в session-end, `03-storage`/`consistency-model §4`, `01-overview` карта модулей); **THEORY §15 «Модель доверия»** (иерархия источников, «уверенно-ложная память опаснее отсутствующей», флаг-не-понижение, разрыв циркулярности usage) + §17 переименован «Родословная»→«Истоки» (по просьбе пользователя — калька); GUIDE раздел «Доверие к памяти»; оба README (слой доверия → реализовано). Roadmap: тело Этапа 13 свёрнуто в одну сводку, чекпойнт 2.1.1 половинно (цикл противоречий → Этап 14). VERSION 1.4.0 + CHANGELOG [1.4.0].
- **Осталось на Этап 14 (Группы 2–3, арбитраж):** Гр.2 — детекция кандидатов конфликта в `make_plan` (пары по `contradicts`/`supersedes` + `verification.status:contradicted`, входы сравнения + ранг источника §15.1), новые действия в `apply_actions` (`supersede`/`dispute`/`reject`/`keep_both_with_context`/`ask_user`), статусы `disputed`/`rejected` (+`status_prior.rejected`≈0.1), аудит-след `arbitration.md`. Гр.3 — детекция намерения запроса в `retrieve` (history/audit снимает понижение `superseded`/`rejected` §1.12; «покажи противоречия» → conflict-subgraph), пометки `disputed`/`rejected` в `_trust_marks`, промпты `amg-consolidator`/`amg-synth`/`amg-retriever`. THEORY-чекпойнт 2.1.1 (цикл противоречий) закрыть. Стенды `../amg-bigtest` и `../amg-testbed` НЕ удалять. **Индекс:** новых читаемых `retrieve` полей frontmatter Гр.2–3 НЕ добавляют (`disputed`/`rejected` — значения уже-проецируемого `status`), синхронизация `index_store` не нужна — перепроверить при Гр.3.
- **Этап 12 — что сделано (детали в roadmap §5 «Этап 12», свёрнуто; CHANGELOG [1.3.0]):**
  - **Индекс** (`index_store.py`, `cache/index.sqlite`) под `retrieve.load_nodes`: проекция полей узла; читатель сверяет сигнатуру (мета-таблица sqlite, ДО скана) → индекс/безусловный скан+пересборка; 6 писателей инкрементально апсёртят через `graph_store.Transaction.node_paths()`; форма `load_nodes` идентична (общий `_node_from_meta`); **15× на 7600 узлах**; одноразовый, без конфиг-ключа. **Adjacency-кэш не введён** (измерение: <1%).
  - **Возобновляемая деривация:** `amg-builder` эхоит `content_sha`; `apply_derivation` → `skipped_stale`.
  - **Хелперы очереди** `partition_queue.py`/`inspect_queue.py`; **bench.py**; **mypy --strict** (15 модулей, селфтесты исключены); **транзакционный `log.md`**; **тиринг моделей** (`config.yml models` структурный, discovery low/synthesis high/module_summary плоский); **барьер** «Boundaries» усилён.
  - Свёрнуты §1.15/1.27/1.31, закрыт 2.16 п.6. Коммиты: `291b082`/`a44f21a`/`e03303b`/`c6d8cd1`/`5021b96`/`58301b8`/`a20d736`/`970e40c` (release).
- **Память (для будущих сессий):** `stage12-index-doc-at-closure` — ВЫПОЛНЕНО (индекс задокументирован в arch/GUIDE/READMEs как просил пользователь; можно пометить как закрытое или оставить как референс стиля).
- **Карта на Этап 13 (ключевые переносы):**
  - planned-поля `confidence`/`provenance`/`verification` УЖЕ описаны опережающе в `02-data-model` — Этап 13 их реализует, не выдумывает; `status: disputed`/`status_prior.disputed` (0.5) опережающе заведены Этапом 2; блок `verification` уже в «Планируется» `09-config`.
  - лёгкое правило «verify code claim before answer» уже в `amg-retrieve`/`amg-retriever` (Этап 2) — расширять до полного слоя (`verify_claims.py`), не дублировать.
  - **`usage.log`** (задача 9) — провенанс ИСПОЛЬЗОВАНИЯ, писать ОТДЕЛЬНО от слепого `coactivation.log` (тот циркулярен, §8.1); субстрат улучшенного Хебба Этапа 14.
  - **ВАЖНО про индекс:** `index_store` хранит ПРОЕКЦИЮ полей; новые provenance-поля, которые читает `retrieve`, нужно добавить и в проекцию (`_node_from_meta` + `index_store` синхронны), иначе на индекс-пути их не будет.
  - новая теория Этапа 13 (модель доверия; «уверенно-ложная память опаснее отсутствующей») — завести разделом THEORY при закрытии.
- **Среда/процедуры:** **torch/эмбеддинги РАБОТАЮТ**. Гейт типов: `python -m mypy` без аргументов → **16 модулей** (+`verify_claims`; предупреждение `[mypy-selftest_*]` — давнее, безвредно, exit 0). **19 прогонов селфтестов зелёные** (18 движка + `selftest_install`; новые `selftest_verify`, `selftest_usage`), `eval --make-demo` hop-recall 1.00. **Коммитить через Bash-heredoc** (PowerShell 5.1 ломает многострочный commit); пуш на закрытии этапа — `git push --follow-tags`. В фикстурах/бенче `embeddings: off`. `BASE-PROMPT.md` — gitignored (не коммитится). Не-Claude-Code-среды — Этап 19.
