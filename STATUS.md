# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** 0 — стабилизация ядра сверки (не начат)
- **Сделано в последней сессии:** аудит раскладки против 4.9 — соответствует, кроме мелочей ниже; все 4 селфтеста зелёные (в `selftest_stage2.py` SKIP pdf/docx/xlsx — не установлены опциональные `fpdf2`/`pypdf`, `python-docx`, `openpyxl`); в roadmap (преамбула раздела 2) поправлен устаревший факт про README (заглушки существуют, не «отсутствуют»); создан `.gitignore`, git инициализирован, первый коммит запушен в `git@github.com:Kvazar123/llm-amg.git`
- **Следующий шаг:** Этап 0, задачи 1–2 (stale requeue; `lineno`/`qualname` во frontmatter и queue)
- **Открытые вопросы / блокеры:** мелкое расхождение — `STATUS.md` есть в карте репозитория CLAUDE.md §2, но отсутствует в каноническом списке 4.9 (свернуть при ближайшей правке 4.9)
- **Песочница `../amg-testbed`:** не создана (существует пустой каталог; наполнение — Этап 6)
- **Заметки для следующей сессии:** —
