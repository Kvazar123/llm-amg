# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** 2 (стабилизация retrieval) — **все 11 задач выполнены (код)**; осталась закрывающая сессия (синхронизация документации по пометкам раздела 2, DoD, сворачивание, выпуск v0.4.0)
- **Сделано в последней сессии:** промежуточная сессия Этапа 2. `retrieve.py`: статусный prior `status_prior` множителем на **финальной** активации (`_apply_status_prior`; `active`/`stale` 1.0, `superseded` 0.2, `disputed` 0.5 forward) — задача 1; `_STALE_MARK` для stale в `_render`; tier `decision`/`adr` → strategic + **расширение**: тело `decision`/`adr` рендерится в любом ярусе (`DOC_BODY_TYPES`) — задача 2; **by-key слияние** (`_deep_merge`) — корень рассогласования `relation_priors` (задача 3); `_path` в `load_nodes`; режим `--explain` (`retrieve(explain=N)` + `_explain_inflow`/`_edge_label` — декомпозиция притока на сыром PPR) — задача 9. `config.yml`: дополнен `relation_priors` (`refines`/`exemplifies` 0.6, `supersedes` **0.3** ← 0.5) + `status_prior`. `inspect_graph._in_bucket` — фильтр по реальному каталогу (задача 11, 1.26). `amg-builder.md`: `exemplifies` эмитится. `amg-retrieve/SKILL.md` + `amg-retriever.md`: правило «verify code claim before answer» (задача 10). Новый `selftest_retrieve.py` (7 проверок). Все 7 селфтестов зелёные; eval-демо AMG hop-recall 1.00 vs лексика 0.00; живой `--explain` корректно атрибутирует многошаг. Коммит `624d4d5` (задачи 1–4, 11); задачи 9–10 — отдельным коммитом. Память проекта: `deep-design-decisions`; BASE-PROMPT.md дополнен постоянной установкой (вне гита)
- **Сделано (задачи 5–8):** `embed.get_embedder` выбирает мультиязычный дефолт при `working_language != en` (`_DEFAULT_MODEL_MULTILINGUAL`: model2vec potion-multilingual-128M, st paraphrase-multilingual-MiniLM-L12-v2); EN-дефолт model2vec → **potion-retrieval-32M** (по веб-поиску июня 2026: лучшая статическая retrieval-модель); `working_language` проброшен через `load_config`. `eval_retrieval.py`: demo id `docs:`→`doc:` (задача 6); режим `--compare-embeddings` + `run(cfg=…)` (задачи 5/8). **Демо разделено** (живой тест выявил: связный кейс перетягивает PPR у изолированного): `build_demo_store` — чистый multi-hop (`--make-demo`, AMG 1.00); `build_embeddings_demo` — изолированная кросс-язычная пара, **AMG 0.00 OFF → 1.00 ON** на живой model2vec. Superseded/stale ушли из демо в `selftest_retrieve` (recall@K=1 хрупок для переранжирования). `selftest_consolidate` синхронизирован под `doc:`-id. Проверки `selftest_embed`: `test_multilingual_default`, `test_eval_compare_offon` (стабом). **Находка среды:** sentence-transformers не грузится (сломан torch C-ext на Python 3.14) — мягкий откат в BM25 сработал. Все 7 селфтестов зелёные
- **Следующий шаг:** **закрывающая сессия Этапа 2** по протоколу §4.6: (1) синхронизация документации по накопленным пометкам раздела 2 — THEORY §4 (`refines`/`exemplifies`/`supersedes` + β) и §14.2 (объяснимость → `--explain`), 02-data-model (status prior), 06-retrieval (status_prior, tier decision/adr+тело, embeddings multilingual, deep-merge), 09-config (status_prior, multilingual defaults, by-key merge, budget code≠template), 10-eval-tools (`--compare-embeddings`, demo doc-канон), GUIDE (`inspect_graph --bucket`, verify-rule); пометки: 2.1.4/2.1.7/2.7/2.10.1/2.10.4/2.10.7; (2) DoD + селфтесты; (3) сворачивание тела Этапа 2 и пунктов 1.12/1.26; (4) выпуск v0.4.0 (VERSION + CHANGELOG + тег + пуш)
- **Решения сессии (обоснование — roadmap, задачи 1–4):** статусный множитель на финальной активации (переранжирование по валидности, не гейт; §6.4 не нарушается); `superseded` β 0.3 ≈ `contradicts` (оба ведут к снятому/конфликтному узлу); `decision`/`adr` → strategic с инлайн-телом (payload в мотивировке); by-key слияние снимает уязвимость «цельной замены» (одобрено пользователем)
- **Новые дефекты (в roadmap):** `consolidate.py` DEFAULTS бюджеты веток 150/60000 ≠ шаблон 400/200000 (пометка к 2.10.4); 09-config стр. 9 и 06-retrieval стр. 83 утверждают «relation_priors заменяется целиком» — теперь by-key, правка на закрытии
- **Открытые вопросы / блокеры:** мелкое расхождение — `STATUS.md` есть в карте репозитория CLAUDE.md §2, но отсутствует в каноническом списке 4.9 (свернуть при ближайшей правке 4.9)
- **Песочница `../amg-testbed`:** не создана (существует пустой каталог; наполнение — Этап 6)
- **Заметки для следующей сессии (Этап 2):** surfacing `superseded` по запросам истории/аудита — опережающая заметка добавлена в Этап 14, задача 6 (исключение из статусного множителя, нужна детекция намерения); при правках THEORY/06/09 на закрытии влить накопленные пометки разделов 2.1.4/2.7/2.10; в репозитории не держать каталог `amg/`
