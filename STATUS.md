# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** 1 закрыт (v0.3.0, 2026-06-13); следующий — Этап 2 (стабилизация retrieval), не начат
- **Сделано в последней сессии:** закрывающая сессия Этапа 1 по протоколу §4.6 — закрыты пункты реестра 2.3 пп. 1–4 и 8, 2.9 п. 1 (исправлен баг примера пути узла: `::` → один подчерк; `coact`/`last_used` подтверждены как поля рёбер); DoD прогнан полностью (миграция старого графа — `selftest_migrate.py`; `retrieve.TIER_OF_TYPE` и `consolidate._branch_members` одинаково понимают hub/overview — сверено кодом; схема == frontmatter), все 6 селфтестов зелёные (stage2 теперь вживую гоняет pdf/docx/xlsx/treesitter); roadmap свёрнут (тело Этапа 1 → однострочная сводка; пункты 1.4, 1.5, 1.25 → строки «исправлено на Этапе 1»); выпуск v0.3.0 (VERSION, CHANGELOG, аннотированный тег, пуш). До закрытия в той же серии сессий выполнены все 8 задач этапа (канон `type`/`source_kind`, поля, `migrate_schema.py`, канонизация tree-sitter `kind` + адаптер двух поколений биндинга). После выпуска: сверена и дополнена таблица Этапа 2 в BASE-PROMPT.md (gitignored, локально); правка неточности CHANGELOG `[0.1.0]` (убран gitignored `BASE-PROMPT.md`, коммит 5fbb842)
- **Следующий шаг:** Этап 2 (стабилизация retrieval), задачи 1–11. Таблица файлов Этапа 2 в BASE-PROMPT.md уже сверена и дополнена в этой сессии: к `scripts/*` + `06-retrieval.md` + `10-eval-tools.md` добавлены `config.yml` + `09-config.md` (правятся задачами 3–5, 8) и поименованы слот-4-соседи (SKILL + `amg-retriever` — задача 10; GUIDE — задача 11 и п. 1.28; разделы THEORY §4/§6–7/§14.2). Грузить по этой строке
- **Открытые вопросы / блокеры:** мелкое расхождение — `STATUS.md` есть в карте репозитория CLAUDE.md §2, но отсутствует в каноническом списке 4.9 (свернуть при ближайшей правке 4.9)
- **Песочница `../amg-testbed`:** не создана (существует пустой каталог; наполнение — Этап 6)
- **Заметки для следующей сессии (Этап 2):** канон типов из Этапа 1 — вход для tier mapping (пункт 2.7.2, описание в 06-retrieval сверяется на Этапе 2); статусный prior (1.12) не штрафует `stale`, сильно понижает `superseded`; рассогласование задачи 4 конкретно: `retrieve.py` DEFAULTS включают `refines`/`exemplifies`/`supersedes`, а `config.yml` (`retrieval.relation_priors`) и THEORY (строки ~144–153) их НЕ содержат — согласовать; недостающие `convergence_tol`/`seed_floor` в 09-config — п. 2.10.7; `--explain` (задача 9) и починка `inspect_graph.py --bucket notes` (1.26); вопрос бакета `summarize_episodes` отложен в 2.8 п. 5 (Этап 3); в репозитории не держать каталог `amg/`
