# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** 5 закрыт (v0.7.0, 2026-06-14); следующий — **Этап 6 (интеграция `amg-classifier`)**, не начат.
- **Сделано в последней сессии (закрывающая сессия Этапа 5):** реализован безопасный захват авторских узлов — новый `notes.py add` в `amg-bootstrap/scripts`. Пишет через `graph_store.Transaction` под локом (recover → атомарный commit), переиспользуя хелперы `reconcile` (`serialize_node`/`node_relpath`/`_merge_edges`/`_merge_part_of`). Типы `note`/`decision`/`adr`/`open_question`/`plan`; поля `source_kind: authored`+`policy: authored` (правило сохранности — переживает любой bootstrap), `status: captured` (дефолт, не в `status_prior` → 1.0, не прячется), `created`/`updated`/`lang`/`tags`/`part_of`/`edges`. **Решения:** (1) id `note:<slug>-<hash8>` — нейтральный namespace + контентно-адресуемый хеш (повторный идентичный захват → один узел; `--id` для стабильного живого узла); (2) `open_question`/`plan` добавлены в `episodic_types` (транзитные состояния пересматриваются консолидацией, иначе «активная» ложь); (3) `tags` индексируются в BM25 (`retrieve.load_nodes`), origin рёбер заметок — `authored`. Закрывающая сессия по протоколу §4.6: документация синхронизирована (`02`/`09`/`08`/`SKILL amg-consolidate`/`entrypoint`/`GUIDE`/`THEORY §13`); DoD прогнан (8 селфтестов зелёные, `selftest_notes` — 7 проверок); roadmap свёрнут (тело §5 Этапа 5 → однострочная сводка; закрыты пункты 2.9 п.5, 2.12 п.4, 2.16 п.1); выпуск v0.7.0 (VERSION, CHANGELOG, тег, пуш).
- **Ключевое решение Этапа 5 (держать в уме):** статус `captured` намеренно НЕ в `status_prior` (множитель 1.0 — свежая заметка находится сразу, не штрафуется); консолидация повышает её в `active` через `promote`. id заметок нейтрально-namespace `note:` (не `<type>:`), т.к. `promote` меняет `type`, а id неизменен.
- **Следующий шаг (Этап 6 — интеграция `amg-classifier`):** (1) `classification-overrides.json` (`work/`), `extract_structure.py` читает overrides до fallback-классификации, `--stats` показывает resolved-by-override / unresolved; обновить workflow `amg-bootstrap` и `amg-classifier`; тесты. (2) **Создать песочницу `../amg-testbed`** + sync-скрипт (движок → `<testbed>/.claude/`). (3) **Отложенное измерение `apply_hebbian` off/on** на реальном графе песочницы (задача 7 Этапа 6, перенесена с Этапа 4) — пошаговый протокол в roadmap. **Грузить по строке Этапа 6 в `BASE-PROMPT.md`** — дополнена в этой сессии (соседи: `reconcile.py` — потребитель юнитов; `04-ingest.md`; известные расхождения 1.13/2.5).
- **Заметки для Этапа 6:** (1) пункт 1.13 (classifier не интегрирован) — основной аудит-пункт этапа; (2) формат overrides задан в 1.13: `{ "путь": {"category","language"} }`; (3) `amg-testbed` даёт первый реальный граф для продуктового решения по `apply_hebbian` (дефолт пока `false`); (4) `notes.py` теперь часть движка — sync-скрипт песочницы обязан его копировать.
- **Открытые вопросы / блокеры:** нет. mypy у пользователя не установлен — `--strict` не прогнан (код типизирован в стиле существующего). Мелкое расхождение из Этапа 2 (`STATUS.md` в карте CLAUDE.md §2, но не в каноне 4.9) — свернуть при ближайшей правке 4.9.
- **Песочница `../amg-testbed`:** не создана (наполнение — Этап 6). Именно она даст реальный граф для продуктового решения по `apply_hebbian`.
