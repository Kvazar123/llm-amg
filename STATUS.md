# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Этап 21 ЗАКРЫТ (2026-07-08, v1.12.0, НЕ запушен по указанию пользователя).** Вся русская документация переведена на технический английский уровня носителя без калек, ни одна деталь не потеряна. Корпус `docs/en/*` зеркален `docs/ru/*` пофайлово: `THEORY.md`, `GUIDE.md`, `architecture/{README, 01–10, 12}`, плюс английский снимок-ориентир `architecture/11-roadmap.md` (живой roadmap остаётся русским). `README.md`/`INSTALL.md` были английскими и до-синхронизированы на en-цели. Перевод строго по `docs/en/glossary.md` (~190 пар; справочник сохранён, ссылок из доков нет).
- **Следующий: Этап 22 — тестирование в не-Claude-Code-средах** (Codex `--env codex` и generic AGENTS.md-среда, напр. Qwen Coder). Требует ЖИВОГО Codex/Qwen-проекта — вне возможностей dev-сессии без реальной среды у пользователя. Грузить: весь управляющий слой (`entrypoint/AGENTS.md`/`AGENTS.codex.md`, `install.py --env`, `agents/*.md`, `skills/*/SKILL.md`) + факты о среде из памяти [[models-1.14-and-env-research]] (баг #44385, кламп `model_reasoning_effort`, Codex НЕ бесскилловый). Строка загрузки Этапа 22 в BASE-PROMPT.md уже адекватна.
- **НЕ ЗАПУШЕНО.** Все коммиты Этапа 21 (перевод + линки + компакция + релиз) и аннотированный тег `v1.12.0` — локальные. Пользователь просил не пушить. Когда разрешит: `git push --follow-tags`.
- **Раздел 1 аудита: открытых пунктов НЕТ.** §1.53/§1.54 свёрнуты. Все §1.xx — одностроки «исправлено на Этапе N».
- **Проверки перевода (durable-выводы):** полная валидация ссылок и якорей (35 файлов, ноль битых/межъязыковых/битых якорей) — скрипт-валидатор в истории сессии (python: LINK-regex + GitHub-slug для якорей, НЕ схлопывать пробелы — em-dash даёт двойной дефис). Четырёхлинзовая двуязычная сверка (workflow) подтвердила полноту/нативность; правки внесены. При будущих правках доков гонять валидатор ссылок/якорей.
- **Решения пользователя (действуют):** глоссарий сохранить, ссылок из документации на него нет; живой замер ×20–30 и связности — совместная приёмка Этапов 19–20 на dogfood-стенде, вне dev-сессий; dogfood-пункты Этапа 17 (эмиссия паттернов, флип `apply_hebbian` на реальном usage.log) — на живом проекте.
- **Замечания пользователя (действуют всегда):** поставляемые артефакты и ЛЮБЫЕ комментарии скриптов без ссылок на этапы/аудит (легаси вычищено); в ТЕХНИЧЕСКОМ слое документации (architecture/en+ru) остаточные «Этап N» тоже вычищены — корпус stage-free (только §4-ссылки на решения roadmap сохранены); THEORY stage-free всегда; хардкод весов/порогов — только осознанный; документацию писать регистром живого объяснения (§3.4 / CLAUDE.md §7 п.7); английское зеркало — native-level без калек.
- **Поведение при смене конфига (сверено по коду, для GUIDE-ответов):** конфиг читается заново каждым прогоном; новые пути/политики подхватывает следующий `/amg sync`; эмбеддинг-кэш самолечится, но для линковки после включения повторить sync; смена `working_language` задним числом НЕ пересводит (граф двуязычится) — потому KEY; тиринг `models` вступает в силу переустановкой.
- **Стенды (НЕ удалять):** `../amg-bigtest`, `../amg-testbed`. Реальный dogfood-граф — путь у пользователя (не в репо).
- **Тесты:** 21 селфтест движка + `selftest_install` + mypy(18) — зелёные на v1.12.0 (правки были только документационные).
- **Среда/процедуры (durable):** torch/эмбеддинги работают, ST-бэкенд нестабилен под тяжёлым пере-кодированием (изолировать в subprocess). Гейт типов: `python -m mypy` без аргументов → 18 модулей. Коммитить через Bash-heredoc; пуш — `git push --follow-tags`. Большие правки roadmap — python по маркерам с бэкапом в scratchpad. В батарее селфтестов НЕ глотать вывод (`out=$(python "$f" 2>&1)`, печатать при провале). `BASE-PROMPT.md` — gitignored. Не-Claude-Code-среды — Этап 22.
