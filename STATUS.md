# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап:** **Этап 16 (командная работа и git-граф) — ЗАКРЫТ и ВЫПУЩЕН (v1.7.0).** Следующий — **Этап 17 (продвинутый semantic layer)**; детальная строка загрузки — в `BASE-PROMPT.md` (расширена по аналогии с Этапом 16). **20 прогонов селфтестов зелёные** (19 движка + `selftest_install`; +7 новых кейсов Этапа 16) + mypy (17 модулей) + eval-демо pack-recall 1.00. **Запушено** на `origin` (`d4d1868..7286577 main` + аннотированный тег `v1.7.0`).
- **Этап 16 — что реализовано (детали свёрнуты в roadmap §5 «Этап 16»; CHANGELOG [1.7.0]):**
  - **Хранилище под общую папку:** `graph_store._lock_is_stale` host-aware (чужой-host lock не крадётся локальной пробой pid — только по возрасту; одномашинный путь байт-идентичен); мягкая `StoreLockError`-деградация в `lifecycle._heal`/`session_end`/`repair` и в `graph_store` CLI (`recover`/`verify --repair`); явные писатели падают с сообщением о владельце.
  - **Устойчивость к конфликтам:** три парсера (`reconcile.parse_node`/`retrieve._parse`/`consolidate._parse`) защищены от `yaml.YAMLError`+non-dict — узел с git-конфликт-маркерами пропускается, не валит `load_nodes` (`export_graph` был защищён — прецедент); `reconcile.find_conflict_markers` (`<<<<<<<`/`|||||||`/`>>>>>>>`, `=======` исключён ради setext) + поверхности `plan`/`status`/`repair`/`session_start`.
  - **Git-aware:** `reconcile._git_branch`/`_git_changed_since` (best-effort, `--relative`); ветка/commit в `lifecycle.status`; `verify_claims.verify_by_commit` + `--by-commit` (актуальность источника по коммиту: `git diff` на distinct ingest-коммит, без пере-нарезки, дополняет авторитетную хеш-сверку). Ветка — на уровне store, `_provenance` НЕ тронут.
  - **Доки:** GUIDE «Командная работа», READMEs (RU+EN, раздел в теле + перенос из планов в реализованное), `consistency-model §9`/§12, `03-storage`, `02-data-model`, `06-retrieval`. Сравнение веток — документированный git-паттерн (`git diff` / `export_graph --json`), без нового кода. Новой теории нет (git над markdown-каноном §4.1 инструментален). Строка Этапа 17 в `BASE-PROMPT.md` готова.
  - **Переносимость:** все правки — в универсальных скриптах (`graph_store`/`lifecycle`/`reconcile`), НЕ в хуках → работают и в бесскиловых/Codex-средах без сессионных хуков (петлю ведёт модель прямыми вызовами). Тесты: `case_lock_cross_host`, `case_shared_folder_contention`, `case_merge_conflict_resilience`, `test_conflict_skip`, `case_merge_conflict_surfaced`, `case_freshness_by_commit`, расширен `case_status`.
- **Отложенные дефекты промптов/косметики (Этап 18):**
  - **§1.34** — захардкоженные русские примеры намерения в `entrypoint/CLAUDE.md` (столбец «Verbal intent» + пары синонимов); прочие блоки (`AGENTS.md`/`AGENTS.codex.md`/`commands`) и `agents/*.md` уже английские.
  - **§1.35** — словесные команды памяти не привязаны к ключу «AMG»/память (общая фраза «покажи граф»/«подведём итоги» может ложно запустить `/amg`); привязать триггеры во всех блоках+доках.
  - **§1.36** — `log.md` это плоский лог, а не markdown-документ → вести обычный `.log` без `## `-заголовков (формат `[ts] txid source | msg`); затрагивает `graph_store.append_log` (имя+формат+ротация), `lifecycle._last_consolidation`/`status`, `verify_claims`/`consolidate`/`reconcile` (вызовы не меняются), `selftest_graph_store.case_action_log`, доки (`02`/`03`/`consistency-model §11`/`01`). Косметика, не дефект целостности. Отдельным `z`-исправлением или в Этап 18.
- **Перенос графа между средами (вопрос пользователя, Этап 15):** копированием папки `<agent_dir>/amg/` — `nodes/` это канон markdown, env-агностичный (id и `source_path` проектно-относительны); править лишь `agent_dir`/`entrypoint` в `config.yml`, кэши (`cache/`/`journal/`) пересобираемы. JSON-импорт НЕ нужен (был бы лоссовым обходом канона; задокументировано в GUIDE «Командная работа»). JSON-экспорт (`--json`) остаётся для сторонних инструментов анализа.
- **Среда/процедуры (durable):** **torch/эмбеддинги РАБОТАЮТ**. Гейт типов: `python -m mypy` без аргументов → **17 модулей** (предупреждение `[mypy-selftest_*]` — давнее, безвредно, exit 0). **20 прогонов селфтестов зелёные** (19 движка + `selftest_install`), `eval --make-demo` pack-recall 1.00. **Коммитить через Bash-heredoc** (PowerShell 5.1 ломает многострочный commit); пуш на закрытии — `git push --follow-tags`. **Большие правки roadmap делать частями с точным копированием из свежего чтения** (мега-абзацы легко ломаются опечаткой при ручной сборке `old_string`; для удаления блоков надёжнее `sed` по номерам строк). Стенды `../amg-bigtest` и `../amg-testbed` **НЕ удалять** (замер Хебба на реальном usage — Этап 17 з.8). `BASE-PROMPT.md` — gitignored. Не-Claude-Code-среды — Этап 19.
