# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** 3 (стабилизация консолидации). **Все 14 задач реализованы (Сессии 1–3); остаётся закрывающая сессия** (документация → DoD → сворачивание → выпуск v0.5.0)
- **Сделано в Сессии 1 (коммит 7dfe3f4):** полная загрузка контекста + согласован план с 6 развилками A–F (B,D подтверждены). Задачи 1–4 в `consolidate.py`: (1) `compaction.enabled` — гейт `over_budget` в `make_plan` + гейт `COMPACTION_ACTIONS` в `apply_actions` (override `force:true`); (2,3) `_is_protected`/`_degree_map` — защита `protect_types`+high-centrality в коде; (4) `shorten` пишет `.full` один раз. +4 теста
- **Сделано в Сессии 2 (последняя):** задачи 7, 8, 9, 13 в `consolidate.py`. (7) `summarize_episodes`/`introduce_subhub` → канон synthesized (`policy: authored`, `source_hash`/`derived_from_hash: null`, `lang` из `working_language`; `load_config` теперь читает `working_language`), `summarize_episodes` → бакет `_hubs`; (8) `_inbound_grounded` + `salience(grounded_inbound)` — заземление по входящим `documents`/`implements`/`specifies`; (9) `merge` переписан: `_fold` (max-вес+сумма `coact`), `_combine_part_of` (слияние членств+симплекс), без self-edge и рёбер внутрь drop, `_dedup_edges` соседей после `redirect_inbound` (закрывает 1.22); (13) `introduce_subhub` заменяет только `parent_topic`→`hub_id`, прочие членства сохраняет+ренормирует. +4 теста (`test_merge_quality` с `force:true` для изоляции от защиты, `test_subhub_keeps_memberships`, `test_consolidation_nodes_schema`, `test_grounded_inbound`). Все 7 селфтестов зелёные (12 проверок консолидации). Отмечены задачи 7–9,13 + часть 10; пометки к 2.8 п.5–6
- **Сделано в Сессии 3 (последняя) — ветки и веса:** задачи 12, 11, 5, 6, 14. (12) `_branch_members` дополнен обходом вниз от хаба по `HUB_DOWN_RELS` (стоп на чужом хабе), вверх-путь сохранён — ветки непусты на реальном графе (развилка A); (11) `near_duplicate_sim`/`episodic_types`/`stale_age_days` читаются из конфига top-level + добавлены в шаблон (F), `default_edge_weight` проброшен в `reconcile._merge_edges`/`retrieve.build_adjacency`/`consolidate.fold_weights` (D, 1.23 закрыт); (5,6,14) флаг `weights.apply_hebbian: false` — `fold_weights` копит `coact` (питает salience), `w` меняет только при флаге+журнале; decay привязан к журналу (C, без `last_decay_at`). +2 теста (`test_branch_downward`, `test_weights` два режима); попутно укреплён helper `write_node` (slug как `node_relpath`). Все 7 селфтестов зелёные (13 проверок консолидации). Отмечены задачи 5,6,11,12,14 + остаток 10; пометки к 2.10 п.5,8
- **Следующий шаг — ЗАКРЫВАЮЩАЯ сессия Этапа 3** (протокол §4.6, строго по порядку): (1) синхронизировать `07-consolidation.md` (рабочий `compaction.enabled`, защита enforced, idempotent shorten, узлы к схеме+бакет, grounded inbound, merge, subhub, веса вхолостую), `09-config.md` (`apply_hebbian`, `default_edge_weight` рабочий, константы настраиваемые), `02-data-model.md` (узлы консолидации в `_hubs`); занести **новую теорию в THEORY**: §8 (риск самоусиления Хебба, «Хебб под измерением», idempotency весов через журнал), §10 (защита enforced, idempotent archive); (2) DoD + селфтесты; (3) сворачивание тела Этапа 3 в §5 + пунктов 1.8–1.11/1.17/1.20–1.23 раздела 1 + закрытых 2.8/2.10; (4) выпуск v0.5.0 (VERSION+CHANGELOG+тег+пуш). **Грузить:** `07/09/02-консолидация-релевантное` + THEORY §8–10 + текущий `consolidate.py`/`config.yml`
- **DoD Этапа 3 (закрыто в коде, подтвердить на закрытии):** compaction обратима ✓ · защищённые узлы защищены кодом ✓ · повторный apply не портит архив ✓ · документация не обещает строгую идемпотентность (decay при журнале) ✓ · ветки достижимы от хабов (downward) ✓ · Хебб только после eval (`apply_hebbian` off) ✓
- **Решения по развилкам (зафиксированы, держать для Сессий 2–3):**
  - A (задача 12): `_branch_members` идёт и **вниз** от хаба по охватывающим рёбрам (`documents`/`defines`/`specifies`/`implements`/`part_of`, далее `defines` к функциям), вверх-путь сохраняется — локально в `consolidate.py`, не трогает `synth`/`reconcile`
  - B (задача 14): флаг `weights.apply_hebbian: false` по умолчанию — `fold_weights` копит `coact` (питает `salience.freq`), но `w` (Хебб+decay+prune) **не меняет**, пока не включат после eval-выигрыша. Разрывает петлю «богатые богатеют» (PPR-веса→пакет→пары→те же веса). Это позиция автора (суждение+провенанс ценнее статистики собственных выдач), не временная заглушка
  - C (задачи 5/6): decay только при наличии нового журнала; без `last_decay_at`/календаря
  - D (1.23): `default_edge_weight` **пробросить** в `reconcile._merge_edges` + `retrieve.build_adjacency` + `consolidate.fold_weights` (не убирать); внесено в задачу 11
  - E (задача 7): `summarize_episodes` → бакет `_hubs` (правило «synthesized → _hubs»; `migrate_schema.py:7` прямо отложил этот вопрос на Этап 3)
  - F (задача 11): `near_duplicate_sim`/`episodic_types`/`stale_age_days` — top-level чтение из конфига + добавить в шаблон `config.yml`
- **Новая теория для THEORY (занести при закрытии этапа):** §8 — риск самоусиления хеббова сигнала и «Хебб под измерением» (почему `apply_hebbian` по умолчанию off); idempotency весов (decay привязан к активности, не к календарю). Архитектура — в 07; «почему» — в THEORY (установка сессии)
- **Будущая задача (внесена решением):** task-outcome хеббов сигнал (от принятого результата сессии, а не от факта совместной выдачи) — кандидат в Этап 13/14, требует сессий (Этап 9) и eval (Этап 4)
- **Открытые вопросы / блокеры:** нет новых. Мелкое расхождение из Этапа 2 (`STATUS.md` в карте CLAUDE.md §2, но не в каноне 4.9) — свернуть при ближайшей правке 4.9
- **Песочница `../amg-testbed`:** не создана (наполнение — Этап 6)
