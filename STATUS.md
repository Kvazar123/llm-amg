# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Финал сессии 3 (2026-07-17, третий прогон пользователя после переустановки движка): аудит снова clean (927=927, 0 аномалий), 12/12 пачек в judged/, gate ok (0 структурных / 208 модельных висячих — инертны, «чистящий проход» отклонён намеренно), консолидация отработала (promote+keep_both; компакция честно skipped_disabled).** Поле `links_parts_without_judged` ПОДТВЕРЖДЕНО живьём: оркестратор сам увидел причину незасчитанной волны и добил ровно её (§1.96 — закрыт с полевым подтверждением). Новые §1.97 (ре-спавн с «рассказом о сбое» увёл линкер в криминалистику work/ — правила рамок в промптах линкера/билдера + правило ре-спавна в SKILL) и полевой факт стартовой дисциплины: слабая модель пропускает стартовую проверку даже при императиве ПЕРВОЙ строкой блока (добавлена ×3) и строке в activation gate — потолок промптов исчерпан, системный фикс = OpenCode-плагин (внесён в задачу 23 приоритетом рядом с форк-исследованием). Изолятов 7 (3 doc-секции с честными отклонениями суда + свежие заметки сессий) — ждут `/amg relink` пользователя. THEORY-тезисы закрытия развёрнуты с числами в блоке «Закрытие (THEORY)» тела Этапа 23; BASE-PROMPT дополнен строкой-довеском Этапа 23 (сессия-3 факты, изменённая поверхность движка, план следующей сессии: Codex/Qwen + веб-исследование форка/плагина, задачи 24–25, 33; затем закрывающая по тезисам). «2 линкера вместо 5–10» и «долгие блуждания» — дисциплина конкретной модели; счёт ходов исполнителей по фактам следующих прогонов.
- **Продолжение сессии 3 (2026-07-17): АУДИТ нового полевого стенда `D:\test\graph\.agents\amg` (второй OpenCode-прогон, слабая модель) + задача 34; §1.95–§1.96.** Аудит: **clean, 0 аномалий** (915 файлов = 915 id, статусы согласованы, очередь честная, журнал пуст); metrics: **gate ok** (largest 0.9945, 0 структурных висячих / 201 модельных — новый сплит работает по замыслу), 4 изолята (три doc-секции + заметка; ровно кейс `/amg relink` — pending link-batch с ними уже наноминирован, надо досудить и применить), doc-файлов без documents — 2. Форензика применённых частей раскрыла «переименованные id»: их НЕ БЫЛО — слабые исполнители пере-набирали id (метод без префикса класса, перефразированный слаг секции, выдуманный id хаба вне existing_hubs) → §1.95, промпт-фикс «id посимвольно» билдер/линкер/синтез; замер внутрифайлового ремонта квалификатора — 17/201 (0 неоднозначных) → отклонён числами (масштаб «последнего сегмента» Этапа 22). Ложные диагнозы сборщика опровергнуты кодом: partition_queue пишет плоские имена (`/`→`_`); markdown/RST-нарезатели дедуплицируют слаги `-N`; «12 пачек не сходятся» = линкеры забывали judged-запись → §1.96, `links_parts_without_judged` в apply-derived (regression 5b). Плюс: verbatim-правило списков опросника в INSTALL×2 (плавали среды/зависимости — дополнение §1.94), «старт-проверка — первое действие сессии» в activation gate трёх бесхуковых блоков (слабая модель забывала петлю), оговорка last-writer-wins для pack.md в промпте ретривера (вопрос о гонке: печатный вывод — первичный канал, файл — зеркало последнего пакета; движковый `--out` — YAGNI). Консолидация стенда отработала: арбитраж dispute+keep_both применён, компакционное действие честно `skipped_disabled` (пользователь выключил компакцию вопросом 13 нового опросника). Батарея затронутых (build/lifecycle/install) + mypy — зелёные.
- **Текущий: ЭТАП 23, сессия 3 (2026-07-16): группа F″ (задачи 29–32) ВЫПОЛНЕНА, задача 14 ЗАКРЫТА отказом, §1.89 починен; 5 коммитов.** Происхождение: стенограммы второго OpenCode-прогона пользователя (слабая модель) + его вопросы об опроснике установщика и идее «маячков». Новые дефекты §1.91–§1.94 (все закрыты этой же сессией). Сделано: **(29)** очередь честна — `_refresh_queue` после каждого apply (единица остаётся, только пока узел `stale`; `queue_remaining` в итоге; apply_cached переиспользует), status/sync-defer считают пересечение «очередь × живые stale» (`_semantic_backlog`; жалоба «semantic queue: 820 при stale 2» закрыта); `missing_sample` в `_apply_items` (какие id выдумал исполнитель); `graph_metrics` разводит висячие по ответственности — `dangling_structural` (гейтуется, 0 по построению) против `dangling_semantic` (число+образцы, вердикт НЕ переключает: вечный attention — мёртвый сигнал), doc-метрика пофайловая `doc_files_without_documents` (ADR-подсекции не шумят); quick-reference relink во всех блоках ×4 + amg.md + help — полный цикл «номинация → суд → apply-derived → до нуля пачек», «номинация сама ничего не применяет». Отклонено из советов слабой модели: поднятие top_k/min_sim (замер 67 %→47 % в силе), «линкер не видит обе стороны» (номинация глобальна, обе сводки в записи), «линковать после синтеза» (SKILL уже так). **(30)** INSTALL×2: шаг 0 — режим по СОСТОЯНИЮ (`config.yml` есть → reinstall при любой формулировке: `in force:` + один вопрос «что изменить?»; нет → полный опросник); вопрос 7 ЗАКРЕПЛЯЕТ бэкенд (`--set retrieval.embeddings.backend=…` + группа `embeddings`/`embeddings-st`; корень бага «вписал ST — осталось auto»: auto-порядок предпочитает model2vec) с трейд-оффом и дефолтными моделями по языку; вопрос 10 расшифрован (потолок токенов на ОДИН запрос по трём ярусам); вопрос 11 сужен до text/treesitter; новая серия V: 12 — модели ×3 ролей (формат строки per среда, flow-словарь `--set "models.synthesis={model: opus, reasoning_effort: high}"`), 13 — `compaction.enabled`+бюджеты ветки, 14 — `linker.synth_input_max_chars` (слабым — 300000); активация теперь вопрос 15; комментарий compaction в шаблоне починен («включена, но простаивает», был ложный «Off by default»); READMEs/GUIDE/12-install зеркально. **(31)** turn-бюджет билдера в промпте (~6 ходов на пачку 60). **(32)** `reconcile.py audit` — постоянный read-only свип инвариантов ВМЕСТО временных «маячков» (идея пользователя оценена, заменена): дубликаты id (класс «узлы молча переписывают друг друга» — load_nodes берёт последний), путь↔id (`node_relpath`), непарсящиеся файлы, форма рёбер/part_of, active-при-отставшем-хеше, лгущая очередь, журнал/конфликты/judged; вердикт `clean|attention` с образцами; доки 05/01/GUIDE. **(14) Замер авто-полосы — ОТКАЗ с числами** (копия стенда в `D:/tmp-amg/task14`, скрипт `measure_autoband.py` там же): верхняя полоса ПУСТА (макс. sim номинаций 0.862), [0.85–0.90) precision 0.800 на 5 парах, ≥0.80 кумулятивно 0.277, ≥0.70 — 0.087; высокий косинус = дубли-ходы стенограмм и однотипные `__init__` разных классов; объёма нет (≥0.85 — 2.1 % подтверждённой популяции из 3471 пары) → `linker.auto_confirm_sim` НЕ вводится, §1.88 закрыт. **(§1.89)** флак selftest_export починен (meta.generated исключён из равенства). Проверки: 19 селфтестов + selftest_install + mypy(18) + eval-демо 1.00 — зелёные. **Осталось по этапу: группа T** (пользователь ставит граф С НУЛЯ свежим чекаутом, собирает, работает на слабой модели; следующая dev-сессия — `reconcile.py audit` его графа + разбор всех логов (actions.log, work/*, gap-report) на скрытые баги — команда для этого и сделана), **группа H** (по боли), **закрытие этапа** (THEORY-дополнения: гейт охраняет пакет §10.3, безопасные действия не заложники §13, «извлечение суждением» §7/§13; BASE-PROMPT-таблица следующего этапа; свёртка §5-тела и §1.77–§1.94 в однострочники; релиз v1.15.0 + en-снимок roadmap).
- **История: ЭТАП 23, сессия 1 (2026-07-14/15): группы A–D, F–G ВЫПОЛНЕНЫ, всё закоммичено (7 коммитов).** Происхождение: OpenCode-прогон пользователя (слабая модель, generic-установка) + полевая консолидация стенда → этап переписан (группы A–H, T), аудит §1.77–§1.89. Ключевые факты, сверенные в сессии: движок стенда `D:\test\graph2` — **v1.13.0** (проверено чтением установленных файлов; «106/169 резолвимых» тестировщика — нестрогий строковый критерий, свип §1.61 честно дал `edges_refreshed=0`); OpenCode ищет скиллы в `.agents/skills` сам (потому generic-блок противоречил видимым скиллам); **у Qwen Code есть хуки** Claude-формы в `.qwen/settings.json` и субагенты `.qwen/agents/*.md` (вводная пользователя + доки); Codex-хуки (`.codex/hooks.json`?) — проверить живьём (задача 22). Сделано: **A** — пять env-режимов (`opencode`/`qwen` пресеты, шаблон `entrypoint/AGENTS.skills.md`, generic смягчён «предпочитай найденные скиллы», qwen-хуки merge_settings, `render_opencode_agents`, qwen-санитайзер фронтматтера, рендер models во всех не-TOML средах); **B** — reinstall применяет явные `--set`/списки точечно (`updated keys:`), `VERSION` едет в `skills/amg-bootstrap/`, `lifecycle: version/help/sync-defer`, status с версией/`semantic queue`/`last_sync`/отложенным sync, `--build` расшифрован; **C** — гейт консолидации: сплит (арбитраж+promote применяются всегда; клон меряет ТОЛЬКО компакционное подмножество), метрика — pack_hop_recall с hop_gold, замороженным от базлайна (ранговый hop — не гейт), `make_plan` несёт `queued_for_semantic`+warning (порог max(100, 10%)); **D** — `retrieval.compact_ratio` (0.25 от действующих бюджетов, периферия не масштабируется), `linker.synth_input_max_chars` + деление листа на цельногрупповые части `synth-input-pNN` (gaps в части 1; SKILL/synth несут частичный протокол); **F** — блоки ×4: «пак читается целиком», wrap-up = заметки→консолидация (бесхуковые), sync-defer каденция, quick-reference таблицы, verbatim-status, «/amg-текст исполняет петля», apply per ROUND; `/amg`: version+help; **G** — READMEs (5 сред, абзац о слабых моделях, таблицы режимов, «Что память кладёт на диск»), GUIDE (envs, reinstall, полная таблица файлов хранилища), 02/06/07/08/09/12 зеркально. Проверки: селфтесты зелёные (вкл. новые: gate_split, plan_backlog, sync_defer, version, compact-ratio, synth-parts, opencode/qwen env), mypy(18) чист, eval-демо pack-recall 1.00. **Сессия 2 (2026-07-15, ревью пользователя):** задачи 26–28 выполнены и запушены (e06f8f0..0186d07): `/amg relink` (`link_candidates --isolated` — источники только бродяги, их judged-отклонения переоткрываются; глагол в команде/help/блоках/README/GUIDE); экономика ходов исполнителей (части линковщика ~20 узлов, несколько Write за ход — ориентир ~5 ходов на пачку вместо 20–30); форк-ретривер `agents/amg-retriever-fork.md` (`context: fork` подтверждён доками Claude Code: наследует весь родительский разговор, пак читает в своём окне, возвращает осведомлённую выжимку; только claude-code, скипы в install, без яруса models) + правило трёх вариантов извлечения в SKILL/блоке; попутно НАЙДЕН И ПОЧИНЕН §1.90 — ретирование judged-пачки затирало одноимённую пачку прежней волны (`-rNN`-суффикс, regression 5c); README×2 — старт бесхуковой сессии слов не требует (блок ведёт стартовую проверку сам), формат строк моделей по средам (OpenCode `провайдер/модель`), строки relink/version/help. Батарея 19+install+mypy+eval-демо — зелёные. **Осталось по этапу:** задача 14 (замер авто-полосы линковки precision@sim на копии стенда — только числа, потом код), группа H (шардирование nodes/ — дизайн зафиксирован в §1.87), группа T (живые прогоны: Codex — пользователь позже; OpenCode повторно уже `--env opencode`; Qwen), §1.89 (флаки selftest_export — попутно), THEORY-дополнения при закрытии (гейт охраняет пакет, а не ранжирование; безопасные действия не заложники). **Замечание пользователю: диск C: был полон (0.01 GB) — временные файлы Python пришлось увести на D: (D:\tmp-amg); стоит освободить C:, иначе хуки/сессии Claude Code начнут сбоить.**
- **История: ЭТАП 22 ЗАКРЫТ (сессия 8, 2026-07-13) — выпущен v1.13.0.** Задачи 29–30 выполнены: (29) `--compact` — профиль указателей по явному выбору вызывающего: `_COMPACT_BUDGET` = код-дефолты 1200/2500/6000/40 (конфиг не читается), в operational указательные строки вместо тел, decision/adr с телом; замер на копии стенда — ×3.1 меньше (128→42 КБ; 45.4k→14.8k токенов), pack-recall с периферией 0.918 лексический (55/60) / 0.806 ST (48/60), теряется глубокое gold за периферией-40 — размен зафиксирован (06 «Компактный профиль», THEORY §7); (30) `prompt-hint` под UserPromptSubmit — четыре гейта §1.76, тишина на прочих промптах, хук в шаблоне settings.json (merge/uninstall событие-агностичны), regression в selftest_lifecycle/install, THEORY §13. **Сквозной аудит по ревью пользователя (внесено):** кириллица в поставляемом слое — единственная утечка (русское имя раздела в комментарии config.yml) исправлена, мультиязычные тест-фикстуры легитимны; ссылки на этапы/аудит вычищены из ВСЕХ shipped-артефактов (реликты «(1.xx)/(task N)», кейсы stage13/stage14 → trust-fields/verdicts); паритет сред — generic-блок дополнен `--intent`, правилом verify-before-answer, сходимостью линковки («до нуля пачек», judged-выбытие) и verify-свипом, codex-блок — правилом верификации (блок всегда загружен, SKILL — по требованию); en-доки сведены с кодом (01: Write у 4 воркеров + prompt-hint в CLI-строке lifecycle; 02: сервисная строка work/; READMEs: три хука; 06: прямой вызов — дефолтный вызывающий); свип калек ru-корпуса по glossary+§3.2 — деривация→обогащение (53 замены; артефактные имена derivation* английские, якорь THEORY §4.3 обновлён), чекпоинт→контрольные части, ингест→естественные формы, крэш→сбой, свои же «гейтованный/пер-промптный» перефразированы; «линковка/линковщик» и «бакет» (архитектурный слой) — принятые термины глоссария, не тронуты; Хебб-строка «Консервативных умолчаний» переписана для новичка в обоих README (что даёт / почему off / включение БЕЗ модельных токенов — свёртка скриптовая; подтверждено по коду fold_weights) + устаревшее «полезный Хебб ждёт переработанного правила» убрано из GUIDE.ru. Свёртка: §5-тело Этапа 22 и §1.55–§1.76 — однострочники; резервы §1.75 (атрибуционный отбор, субмодулярное покрытие) — §8.5; стоимостной ориентир задачи 10 — в задаче 1 Этапа 23. Проверки: 19 селфтестов + selftest_install + mypy(18) + eval-демо 1.00 + валидатор ссылок — зелёные. **Напоминание пользователю: движок на стенде `D:\test\graph2` — СТАРЫЙ (группы E), перед рабочими сессиями переустановить из свежего чекаута v1.13.0 (`python install.py --target D:\test\graph2`; конфиг и граф установщик не трогает), затем перезапустить сессию.**
- **История: Этап 22, сессия 7 (2026-07-13): ГРУППЫ F (задачи 19–23) И G (задачи 24–28, новые §1.70–§1.74 по 15 полевым сессиям) ВЫПОЛНЕНЫ ЦЕЛИКОМ; задача 11 закрыта ретроспективами; доки синхронизированы зеркально. Этап готов к закрывающей сессии.** Пять коммитов: roadmap (§1.70–§1.74 + группа G), движок (`_pop_judged`/`_retire_judged_batches` + `_judged_pairs` — память отклонений линковки через `work/judged/`; пооценочный `_toklen` ASCII/4 + алфавиты/2.2 + CJK/1.5 в retrieve и consolidate; `lifecycle._consolidation_state`/`_consolidation_note` — напоминание о судейской при ≥3 свёртках или остаточном плане, в session-start И в status для бесхуковых сред; embed offline-first `local_files_only=True`/`force_download=False`; `eval_gate.cases` → `<store>/evals/`), промпты/блоки/скиллы (прямой retrieve = дефолт, ретривер = изоляция + правило отсутствия + полный путь verify_claims; judged-запись в частях линкера; `::slug` для doc-целей; валидация частей одним вызовом; стартовая рутина plan+status с вопросом о sync; триггер завершения → судейская; исключение каталога агента из сплошного поиска; свип `verify_claims --all --write` шагом 7 SKILL), харнесс, доки. **Вердикты харнесса (копия стенда, 11 кейсов / 60 gold-id):** baseline pack-recall 1.00, hop 0.22 vs 0.00 лексического; Хебб на реальном usage.log (11 записей, 4748 пар, 541 ребро усилено) — все дельты 0.0000 в обе стороны (плотный режим THEORY §8.2 подтверждён полем) → дефолт false остаётся с числами; задача 23/E ОТКЛОНЕНА с числами (масс-стоп ×1.01 — масса PPR размазана по построению; порог 0.15 теряет gold; реальные рычаги — честный `_toklen` (−47 % пака) и бюджеты); задача 11 — ретроспективы: ужатие капов линкера 5/0.35→3/0.5 уронило бы покрытие подтверждённых пар 67 %→47 % (отклонено; снята и рекомендация из дайджеста стенда), кап synth-input 800k валиден (лист 448k), haiku на module_summary — резерв следующей полной сборки. **Стенд обновлён:** `cases.json` → `D:\test\graph2\.claude\amg\evals\`, конфиг: капы 60/80, `eval_gate.cases` на новый путь; движок стенда СТАРЫЙ (группы E) — фиксы F/G доедут переустановкой (`python install.py --target D:\test\graph2`). Проверки: 19 селфтестов + selftest_embed + selftest_install + mypy(18) + eval-демо 1.00 + валидатор ссылок — зелёные. **Ревью пользователя (конец сессии 7):** Хебб дозамерен с ТРАНСФОРМЕРНЫМ засевом (реальный режим стенда) — дельты снова 0.0000, вердикт полный; сняты worst-rank gold [32…222 из ~600] — вход задачи 29; стилистика консолидационного абзаца README_RU/GUIDE.ru переписана формулировками пользователя (+EN по смыслу). ПЕРЕОТКРЫТО двумя новыми задачами группы G (§1.75–§1.76, полные постановки в roadmap): 29 — компактный профиль `--compact` (точечный запрос не должен получать пакет-потолок ~130 КБ; явный выбор вызывающего вместо отклонённой статистики), 30 — gated-напоминание `prompt-hint` под UserPromptSubmit (затухание петли подтверждено 15 сессиями; НЕ пер-промптный налог: молчит, пока пакет свеж/промпт короткий; только claude-code, в Codex/generic — оговорка). **Осталось: СЛЕДУЮЩАЯ dev-сессия = задачи 29–30 (грузить: retrieve.py (assemble_pack/_render/CLI), lifecycle.py, entrypoint/settings.json + install.py (рендер хука), SKILL amg-retrieve, блоки ×3, selftest_lifecycle/install; данные — отметка задачи 22 в roadmap), затем ЗАКРЫВАЮЩАЯ = только свёртка (§5-тело Этапа 22; §1.55–§1.74 → однострочники; закрыть пункты §2, если остались) + релиз v1.13.0 (VERSION, CHANGELOG, аннотированный тег, push --follow-tags) + en-снимок roadmap.** Для неё грузить: roadmap (вводка+Этап 22+§1.55–§1.74), STATUS, CHANGELOG.md, VERSION — код/доки уже сведены, перечитывать не нужно.
- **История: Этап 22, сессия 4 (2026-07-11): ГРУППА E (задачи 12–18) ВЫПОЛНЕНА ЦЕЛИКОМ + закрывающая синхронизация документации этапа (05/06/08/09, THEORY §4.4+§6.3, GUIDE/READMEs/INSTALL — всё зеркально ru+en).** Четыре коммита по группам: (1) код — `retrieve._rescale_to_max` (порог = доля от max; §1.64), `_TargetIndex`+bare-qualname починка с гардами imports/имя-файл (§1.61), `graph_metrics(…, session_prefix)`+`session_source_prefix` (дампы сессий вне `doc_without_documents`), гард `leftover_derived` (plan-поле + stderr partition_queue/link_candidates), капы 60/80 (задача 16); регрессии: `test_pack_survives_large_graph`, расширенный `case_prefix_repair_sweep` (apply+свип+гарды), leftover-проверки; (2) промпты — Write у 4 воркеров + анти-heredoc + валидация без перечитывания, конвенция направления item.id→to, язык в задании (config.yml агентам не читать), SKILL: волны 5–10, прогресс линковки, oversized, критерий полноты, «о движке — пользователю»; блоки ×3: исключение «точные входные точки — пакет не ритуал»; (3) INSTALL×2 серии вопросов ≤4 (I–IV + завершающий вопрос активации+`--build`), READMEs (таблица «кто что запускает», путь дампа+жёсткий килл, «Стоимость первой сборки»), GUIDE (пол стоимости: повторная работа vs ресенд среды, две метрики, рычаги; относительный порог; HF-варнинг); (4) синхронизация архитектуры + THEORY + roadmap-отметки. **ОСТАЛОСЬ по этапу (вне dev-сессий):** группа D — живой прогон на dogfood-стенде (задача 10: связность+стоимость по двум метрикам, вердикт по «×20–30» уже разъяснён в GUIDE; судьба задачи 6 prompt-hint; приём внутриагентного прогресса из задачи 8; старт `usage.log`), задача 11 (измерительные резервы под eval); ПОСЛЕ группы D — свёртка этапа (§5, §1.55–§1.65 → однострочники) и релиз v1.13.0 по ритуалу CLAUDE.md §10. Проверки сессии 4: 22 селфтеста + `selftest_install` + mypy(18) + eval-демо 1.00 — зелёные; валидатор ссылок по тронутым докам чист (5 XLANG — намеренные взаимные указатели зеркал README↔README_RU, INSTALL↔INSTALL_RU).
- **Ревью пользователя после сессии 4 (внесено, зеркально ru+en):** READMEs п.3 установки — все три роли `models` в примере + «`model` — любая строка среды» + «полный контроль = заранее подготовить шаблон `<amg-dir>/config.yml`» (write_config пишет проектный конфиг из шаблона — сверено по install.py); таблица операций — судейская консолидация = конец СЕССИИ (не смена фокуса) + абзац о лимитах (хук — скрипт без модели, лимит его не останавливает; судейская при исчерпанном лимите неразрушительно откладывается до `/amg consolidate`); контраст `sync`↔`consolidate` («граф↔файлы» против «граф↔собственный опыт»; sync сам доводит семантику изменившегося, друг друга не вызывают) — READMEs «Режимы и управление» + GUIDE «Активация и режимы»; INSTALL×2 — серии средо-честно (≤4 — факт инструмента опроса Claude Code; в иных средах те же серии намеренно). Разъяснения пользователю: Хебб-флип требует накопленного реального `usage.log` (граф сам по себе сигнала не несёт); готовый граф третьей установки закрыл бы качественную половину группы D (gate, §1.64/§1.61 вживую, паттерны), стоимостная приёмка — только живой сборкой.
- **Решение пользователя (2026-07-11): стоимостная приёмка группы D перенесена на первую живую сборку Этапа 23** (задача 1b там; четвёртый прогон ради метрик не делается); качественная половина группы D — dev-проверка на графе ТРЕТЬЕЙ установки (gate после session-фильтра, §1.64 вживую — непустой пакет новым движком, §1.61 вживую — свип лечит ~106 висячих, паттерны). **Ожидается от пользователя: путь к графу/копии стенда** (в репо не хранить). **Диагноз «usage.log не найден» (сверено по lifecycle._record_usage):** файл — `<проект>/.claude/amg/work/usage.log`; на третьей установке не мог появиться по цепочке §1.64: пустые tiers → `retrieve._log_pack` не писал `work/pack-log.jsonl` → `_record_usage` выходил «no pack log this session»; плюс запись добавляется только при непустом пересечении «узлы пакета × правленные файлы сессии». Чтобы начал копиться — переустановить движок на стенде (reinstall безопасен: конфиг/граф целы) и работать сессиями с правками.
- **Сессия 6 (2026-07-12): четвёртая ДОСБОРКА пользователя разобрана; Этап 22 готов к закрытию.** Досборка 186 юнитов (`/amg sync`, движок группы E): механика зелёная (11 билдеров COMPLETE, один apply/раунд, synth от листа, **usage.log ПОШЁЛ — 3 записи**), но стоимость ~38 М cache-read / ~$66; корни разложены и заведены аудитом: **§1.66** линковка не сходится (номинатор без памяти отклонений: после 887 применённых элементов снова 28 пачек, переноминация явно отклонённого — «до нуля пачек» недостижим), **§1.67** `_toklen` 4 симв./токен врёт на кириллице (пак 166 КБ ≈ 55–66k токенов при бюджете 38k), **§1.68** сплошной Grep влетает в `.claude/amg`, **§1.69** авто-sync петлёй не запускается (N перезапусков — 0 запусков; фикс — дешёвый `plan`-дифф + вопрос пользователю). Дом всех — **Этап 22, ГРУППА F (задачи 19–23 = A–E; решение пользователя: досборка и есть полевой прогон, всё закрывается в 22, Этап 23 остаётся средовым; стоимостной вердикт задачи 10 закрыт данными досборки, формальные пороги — ориентир ближайшей полной сборки, не блокер)**: A — память рассуженных пар (дизайн-развилка в §1.66, предпочтителен вариант (в) `work/judged/`), B — честный `_toklen` (ASCII/не-ASCII), C — дифф-вопрос в блоках ×3 + исключение каталога агента из сплошного поиска + валидация частей воркерами одним вызовом в конце (не по-штучно), D — зарядить eval-харнесс на стенде (~10 кейсов, cases.json вне движка; разметку делаю сам по сводкам inspect_graph — модельных затрат почти нет) и перемерить Хебб на реальном usage.log. ВВОДНАЯ пользователя (конец сессии 6): лимиты поджимают, новых рабочих сессий на стенде может не быть — НЕ ждать накопления: замер fold_weights off/on на КОПИИ store с текущими 3 записями (скрипты бесплатны), честный вердикт почти наверняка «сигнал недостаточен → дефолт остаётся false, числа приложены» — это валидное закрытие по букве задачи; флип остаётся документированным выбором пользователя (GUIDE), usage.log продолжит копиться при любой будущей работе, E — адаптивная сборка пакета (стоп по накопленной массе + MMR-дедуп; только под харнесс D; knee отклонён). Причина «28 пачек при поднятых капах»: **в конфиге стенда остались 40/40** (переустановка конфиг бережёт — новые умолчания шаблона не доезжают; в GUIDE добавлено предупреждение «сверься с шаблоном»). Задача 6 Этапа 22 РЕШЕНА (prompt-hint отклонён в пользу §1.69-механики); задача 11 — за харнессом задачи 22/D. В задачу 21/C добавлены: предупреждение о невыполненной судейской консолидации в начале сессии (по `last consolidation` + остаточному plan; автозапуск сознательно нет — дорого) и правило «doc-цель всегда с `::slug`». Доки сейчас (зеркально): README «Что это» +автоустановка; п.3 — три класса применения конфиг-правок; «Режимы» — `/amg on|off` действуют немедленно + обход памяти словами; GUIDE — «умолчания шаблона не доезжают до старых конфигов». **План: следующая dev-сессия = ГРУППА F (задачи 19–23); затем закрывающая = свёртка (§5-тело; §1.55–§1.69 → однострочники) + релиз v1.13.0.** Стенд: 1118 узлов, 5034 ребра, связность 94.3 %, stale 0, gate attention (62 изолированных — ходы стенограмм; 90 висячих — голые имена методов + свежие цели линкеров).
- **Ревью-2 пользователя (сессия 5, внесено зеркально ru+en):** READMEs — «Обновление движка — без потери графа» (конец «Установки») + пиар-абзац самообслуживания памяти в «Что это»; GUIDE — «Переустановка, обновление движка и удаление» с точной механикой write_config (существующий конфиг нетронут безусловно, `--set` к нему не применяется, `models` ре-рендерится из конфига пользователя). Ответ пользователю: конфиг при обновлении никуда переносить не нужно (живёт в проекте, установщик его никогда не заменяет; «начисто» = удалить конфиг перед прогоном; `--scope` — про размещение движка, не про конфиг).
- **Сессия 5 (2026-07-12): качественная половина группы D ВЫПОЛНЕНА на копии графа третьей установки** (стенд `D:\test\graph2`, копия в scratchpad; живой стенд не тронут). Числа: §1.64 — пакет непуст на 942 узлах (strategic: overview + 13 хабов + 3 pattern:*, топ 1.0); §1.61 — свип одним bootstrap 169→62 висячих (107 вылечено; остаток = имена методов + мёртвые символы, расширение «последний сегмент» дало бы лишь +12 — записано идеей, не реализовано); session-фильтр: doc_without_documents 191→169 (−22 хода стенограмм); паттерны живым synth ЕСТЬ (dogfood-пункт Этапа 17 снят); в work/ стенда нет НИ одного из трёх логов (usage/pack-log/coactivation) — эмпирическое подтверждение цепочки §1.64. Стенд с последнего sync изменился (added 172, queued 186) — доведут билдеры первого sync после переустановки движка у пользователя. **Ожидание от пользователя:** переустановка движка на стенде УСТАНОВЩИКОМ (`python install.py --target D:\test\graph2` из свежего чекаута; не ручное копирование — рендер models/блока/хуков), перезапуск сессии, `/amg sync`, затем рабочие сессии С ПРАВКАМИ файлов (usage.log пишется только при пересечении «узлы пакета × правки») — для задачи 6, Хебб-субстрата и передачи мне.
- **История сессий 1–3 (свёрнута): Сессия 1 (2026-07-10): задачи 1–5, 7–9 выполнены, 8 — SKILL-половина.** Сделано: `reconcile.py apply` (мульти-пути/глобы) + `apply-derived` (все `work/derived-*.json` одной транзакцией, applied→`work/applied/`, рваные→`work/invalid/`, повтор = no-op/resume; ядро `_apply_items`); `link_candidates.py --synth-input` (один файл слоя сводок + блок `gaps` для gap-report, кап 800k двухступенчатый); план консолидации несёт сводки кандидатов (`a/b_summary`, `members_preview`≤60, эпизодические с summary); промпты synth/консолидатора — «рабочий слой = входной файл, скан nodes/ запрещён»; линковщик чекпоинтит `-pNN` (~10 узлов); SKILL — один apply на раунд, запрет ad-hoc скриптов, обязательный процент в каждом отчёте; все три блока активации — императивный протокол «граф — первичный источник» (декомпозиция → retrieve на тему; смена фокуса = новый retrieve; сплошной grep только при промахе и со словом; видимость). **Задачи 7–9 тоже ВЫПОЛНЕНЫ (та же сессия, зеркально ru+en):** перезапуск сессии после установки (INSTALL×2 «Close with the restart instruction» + «После установки» без варианта «в текущей сессии»; READMEs «Первый запуск» с шагами Перезапуск/Сборка; GUIDE «Активация ≠ построение»; 12-install×2 новый подраздел); READMEs — петля в «How it works», чекпоинты в преимуществах и «What's implemented», 3D-вьюер инструментной подачей с легендой по категориям содержимого; GUIDE — петля с проверяемостью (`last pack`/`cache/pack.md`), устойчивость+`apply-derived`+Done-объяснение, вьюер-диагностика+легенда, «Куда попадает материал диалогов» (notes vs doc, дедуп, цена consolidate). Валидатор ссылок — чисто. **Осталось:** задача 6 (prompt-hint — решить живым прогоном), живая половина задачи 8 (видимость `progress: N/M` в блоке агента), 10–11 (живой прогон + измерительные резервы — сессии пользователя); закрывающая сессия этапа — синхронизация 05/06/08/09-архитектуры и THEORY §4.4 по списку «Закрытие (доки)». Происхождение этапа: вторая установка (~2 ч, ~80 Мтокенов cache-read, память не использовалась на сложных промптах); аудит §1.55–§1.60; этап сред перенумерован в **Этап 23** (+задача 1a: средовая механика Codex — статусы агентов, видимость промежуточных строк, регистрация скиллов). Строка загрузки Этапа 22 в BASE-PROMPT.md детальная.
- **Итог аудит-сессии 2026-07-10 (сверка Этапов 18–20 с кодом):** ВСЕ задачи Этапов 18–20 реализованы и сверены построчно (текст в очереди + `line_end` + кап; авто-сводка тривиальных с кэш-гардом; резолвер `calls`/`inherits`/`defines` через `import_bindings`; канонизация суффиксом + свип; `_sanitize_item`; `graph_metrics`+гейт; derivation-cache с авто-restore; чекпоинты билдера/synth + `BATCH|SYNTH COMPLETE|PARTIAL` + сверка счётчиков + прогресс в SKILL; вопросы absorb/исключений, `--absorb-once`/`--set-global`/вложенный `--set`, `in force:`, слои конфига, вето чекаута, SystemExit-диагностика). Упущений против планов этапов НЕТ; замечания поля легли в НОВЫЕ дефекты §1.55–§1.60: synth сканирует nodes/ (нет synth-input), apply по одному файлу на ход, линковщик без чекпоинт-частей, петля активации без декомпозиции/«граф раньше поиска», INSTALL молчит о перезапуске сессии, отчётность не доезжает.
- **Диагноз стоимости уточнён ревью пользователя (сессия 2):** сессию после установки он ПЕРЕЗАПУСКАЛ — сборка шла ЧЕРЕЗ SKILL, и её стоимость дала именно буква прежнего SKILL: O(файлов) apply-ходов оркестратора (§1.56, подтверждён как главный сток) + synth-скан (§1.55) + линковщики без чекпоинтов (§1.57). «Сборка без скилла» понижена до риска худшего случая; §1.59 остаётся реальным UX-дефектом (unknown command до перезапуска — пользователь наблюдал сам). READMEs дополнены по его ревью (6 пунктов, см. пометку к задаче 9).
- **ТРЕТЬЯ установка (сессия 3, 2026-07-11): механика задач 1–9 подтверждена живьём** (apply-derived одним вызовом, synth от sheet 532 КБ без скана, 100 % деривация за 5 волн, 0 skipped_invalid, связность 97.1 %, stale 0), но всплыла **группа E** — §1.61–§1.65 и задачи 12–17 в roadmap (полные тексты ТАМ; BASE-PROMPT-строка Этапа 22 дополнена полевыми числами и списком правимого). **Новую сессию начинать с задачи 12 (§1.64, КРИТИЧЕСКИЙ): pack.md пуст на ~937 узлах — абсолютный `activation_threshold` выше топ-активаций размазанного PPR; чинить relative-порогом в `retrieve.assemble_pack` (+ eval-демо, selftest, потребители tiers).** Затем 13 (§1.61 голые qualname-цели — воспроизведён кодом), 14 (§1.62 Write воркерам), 15–17 (промпты/SKILL/INSTALL-серии/доки жизненного цикла и стоимости). Суждение установщика «дедуп линковки теряет пары при краше» проверено кодом и ОШИБОЧНО (`_linked_pairs` — по применённым рёбрам); прочие его находки подтверждены. Расход поля: ~76М cache-read, 66 % — главная сессия-оркестратор (совет «дешёвая модель главной сессии» — в задачу 17). Повторный проход стенограмм добавил в задачу 15 два среза (язык — в задании спавна, агентам config.yml не читать; `doc_without_documents` не считает секции сессионных дампов) и зафиксировал решение по консолидации (задача 14): она уже оптимизирована входом-в-плане + Write, чекпоинт-части actions.json сознательно не вводятся. **Третий проход:** новая задача 16 — безопасное укрупнение партий (builder 40→60, linker 40→80, волны 5–10; качество инвариантно, −30–40 % субагентной стоимости), доки-задача перенумерована в 18 (группа E = 12–18); в задаче 10 — честный пересчёт цели (пол механики среды: cache-read ≈10–12 М, некэшевые ≈2–2.5 М на 1 МБ-проект; приёмка: некэшевые ≤ ~3 М, cache-read ≤ ~15 М; «×20–30» = про устранённую повторную работу — разъяснение в GUIDE); отклонение «один файл вместо частей» зафиксировано с обоснованием (Write-результат короткий, содержимое частей и так ввод агента).
- **Новое правило §3.5 (действует всегда):** любая правка документации — сразу зеркально в `docs/ru` И `docs/en` (READMEs, INSTALL/INSTALL_RU — так же), native-качество обоих языков; исключение — живой русский `11-roadmap.md` (en-снимок при закрытии этапов).
- **Решения пользователя (действуют):** глоссарий сохранить, ссылок из документации на него нет; живой замер ×20–30 и связности — теперь Группа D Этапа 22 (сессии пользователя); dogfood-пункты Этапа 17 (эмиссия паттернов, флип `apply_hebbian` на реальном usage.log) — там же; тестовый хост-проект в документах НЕ называть (обобщённо «реальный проект ~1 МБ»).
- **Замечания пользователя (действуют всегда):** поставляемые артефакты и ЛЮБЫЕ комментарии скриптов без ссылок на этапы/аудит; технический слой доков stage-free; THEORY stage-free всегда; хардкод весов/порогов — только осознанный; документацию писать регистром живого объяснения (§3.4 / CLAUDE.md §7 п.7); английское зеркало — native-level без калек.
- **Ответы пользователю по механике (сверено по коду, для GUIDE-задачи 9):** бакеты вьюера = категории содержимого, не папки (тесты .py = code; absorb-md = doc); notes = авторские захваты `notes.py`, сессии = doc-секции; дедуп: контентные id заметок + near-dup merge (порог 0.82) + свёртка эпизодов; `sync` = сборка (bootstrap-конвейер), `consolidate` = обслуживание (веса/отбор/сжатие), судейская половина consolidate — только моделью (хук не может), потому токеноёмка.
- **Стенды (НЕ удалять):** `../amg-bigtest`, `../amg-testbed`. Реальный dogfood-граф — путь у пользователя (не в репо).
- **Тесты:** после правок сессии 1 прогнаны 20 селфтестов (все, кроме медленных `selftest_embed`/`selftest_bench` — их пути не тронуты) + `selftest_install` + mypy(18) — зелёные. `selftest_build` расширен проверками 7 (batch door: apply-derived, карантин рваной части, no-op повтора) и 8 (synth-input: группы+gaps).
- **Пуш:** ветка синхронизирована с origin (v1.12.0 запушен); правки сессии 1 Этапа 22 (код+промпты+roadmap+STATUS + пользовательская правка CLAUDE.md) не закоммичены — коммитить в начале следующей сессии или по указанию.
- **Среда/процедуры (durable):** torch/эмбеддинги работают, ST-бэкенд нестабилен под тяжёлым пере-кодированием (изолировать в subprocess). Гейт типов: `python -m mypy` без аргументов → 18 модулей. Коммитить через Bash-heredoc; пуш — `git push --follow-tags`. Большие правки roadmap — python по маркерам с бэкапом в scratchpad (в этой сессии хватило точечных Edit по якорям). В батарее селфтестов НЕ глотать вывод (`out=$(python "$f" 2>&1)`, печатать при провале). `BASE-PROMPT.md` — gitignored. Несвёрнутые тексты этапов доступны из git: `git show d4a204a|e25bf4e|463feb2:docs/ru/architecture/11-roadmap.md` (18/19/20).
