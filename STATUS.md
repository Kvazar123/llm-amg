# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** Этап 9 (сохранение сессий) **ЗАКРЫТ** (v0.11.0, 2026-06-16; тег `v0.11.0` — после явного «пушь» пользователя). Следующий — **Этап 10 (упаковка и установка; закрытие = v1.0.0)**, не начат.
- **Что сделано на Этапе 9 (коммиты: захват → задача 9 → доки → правки по фидбеку → релиз):**
  - `2ce16fd` — захват сессий: `lifecycle.py session-end` читает stdin хука `SessionEnd` (`transcript_path`/`reason`) или `--transcript`, парсит `.jsonl` Claude Code → markdown-дамп в `<store>/sessions/ГГГГ-ММ-ДД-ЧЧММ.md` (срез thinking, фильтр мета/слэш-команд/`!`-bash/task-notification по 49 реальным стенограммам); нарезатель `_session_units` (один `section`-узел на ход, `doc:путь::m{n}`); derived-путь `session_dir`→`<store>/sessions`; фикс игнора 1.18 отдельным `_iter_session_files`; `selftest_sessions.py`.
  - `cbe2f99` — задача 9: `_heal` делает read-only `verify` **до** лока (захват крадёт устаревшую блокировку, `recover` чистит журнал), `session-start`/`repair` дружелюбно сообщают о залеченном сбое (`format_heal_note`), `session-start` молчит на чистом старте; `selftest_lifecycle` +2 кейса.
  - `3bc5072` — синхронизация документации (04/02/05/08/09, THEORY §13 — сессия как широкий эпизодический канал, GUIDE/INSTALL/entrypoint, README*); roadmap свёрнут (Этап 9 → «Выполнено», 1.18 → исправлено, 2.9 п.2 / 2.12 / 2.13 п.6 / 2.16 п.4 закрыты).
  - `<refine>` — по фидбеку пользователя: **per-attachment маркеры** `== Attachment N: <тип> ==` вместо агрегатного счётчика (несколько вложений из одного сообщения не схлопываются — важно для чат-экспортов); абзац «что можно подавать» в README*/GUIDE (любой текст + doc/pdf/xlsx; Python нативно через `ast`, прочие — `tree-sitter`); «диалог **с моделью**»; замена режущего «базовый тракт» → «базовая функциональность»/«конвейер»; **новый принцип CLAUDE.md §7 п.7** — регистр прозы: полно и просто, как объяснение коллеге, без обрубков и без художественности.
- **Ключевые решения Этапа 9:**
  - **Путь сессий derived** (`<store>/sessions`), а не литерал — переносим при любом каталоге агента; ключ `sessions` — опциональный override. (Прямо по вопросу пользователя про переносимость в среды без глобальной папки/хуков.)
  - **Фикс 1.18 — отдельный обход** `_iter_session_files` для внутреннего источника (обходит префиксный игнор `DEFAULT_IGNORE_DIRS`/`.gitignore`), а не правка общего `iter_source_files`: нормальные источники и anchored `.gitignore`-правила (`src/secret.py`) не затронуты.
  - **Узел хода — `section`** (входит в `episodic_types` → консолидация сжимает накопившийся чат), схема не меняется.
  - **Выгрузка Claude-Code-специфична** (хук + формат `.jsonl`); переносимая страховка «диалог не потерян» — заметки `notes` по ходу (универсальны, переживают kill). Парсер изолирован как адаптер под формат среды.
  - **Per-attachment маркеры** (по фидбеку): один нумерованный маркер на вложение с типом — общий формат, нарезатель его не парсит (режет только по `=== Роль ===`).
- **Этап 10 — карта дополнена** (строка в `BASE-PROMPT.md` сверена по факту Этапа 9): фикс игнора для сессий уже сделан и переносим, поэтому задача про `DEFAULT_IGNORE_DIRS` сужается до параметризации литерала `.claude` для **обычных** источников; `sessions/` засева не требует (создаётся на лету, в отличие от `digest.md`); вопрос про `session_policy` в авто-установке INSTALL уже есть; `settings.json` ради сессий не меняется; новые соседи по данным для Этапа 11 — `session_dir`/`_iter_session_files`/`_session_units`/`_render_transcript`/`_dump_session`.
- **Открытые вопросы / среда:** v0.11.0 запушен (`main` + аннотированный тег, 2026-06-16). Окончания строк LF/CRLF — git нормализует (содержимое не страдает). `mypy --strict` — Этап 12 (1.31). Одиннадцать селфтестов зелёные.
