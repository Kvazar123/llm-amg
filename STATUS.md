# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** Этап 11 — «расширение ingest» — **ЗАКРЫТ** (закрывающая сессия выполнена: документация → DoD → сворачивание → выпуск). **v1.2.0**: `VERSION`→1.2.0, `CHANGELOG` пополнен, тело §5 Этапа 11 свёрнуто, §1.29/1.30 свёрнуты, semantic-drift привязан к Этапу 17 (задача 6). **Релизный коммит и аннотированный тег `v1.2.0` сделаны локально; пуш с тегами — по сигналу «пушь» пользователя.** Следующий — Этап 12 (производительность/масштабирование). Этап 19 — тест не-Claude-Code-сред.
- **Что сделано на Этапе 11 (5 групп кода + doc-sync + релиз, всё локально на `main`, НЕ запушено):**
  - **A** (`ebe0854`) — нарезатели ndjson/csv/log/rst/pptx (канонические типы; `.pptx` из `BINARY_EXT`); `selftest_chunkers.py` + PPTX в `selftest_stage2`.
  - **B** (`830d8e7`) — recursive JSON (`_data_units`/`_json_descend`/`_json_qual`/`_has_child_container`); ключи `json_max_depth`/`json_recurse_min_chars`/`json_max_nodes`/`log_group_lines`.
  - **C** (`9132dcc`) — chat chunker (`_chat_units`, ребро `follows`, плоский дамп→`_session_units` через `_has_role_markers`); β `follows` 0.4 в `retrieve`/`config`.
  - **D** (`f34d566`) — `detect_policy_conflicts` (1.29) + `missing_sources` в `plan` (1.30).
  - **E** (`196c37d`) — политика `absorb_once` (`absorb_once_path`, гейт `frozen`).
  - **doc-sync + сворачивание + релиз** (этот коммит) — `04-ingest`/`05-reconcile`/`09-config`/`02-data-model`/`THEORY §11–13`/`GUIDE`/`READMEs`/`architecture/README`/`agents/amg-builder.md`; `requirements.txt`+`install.py DEP_GROUPS` (`python-pptx`); roadmap свёрнут; `VERSION`/`CHANGELOG`.
- **Ключевые решения этапа:** новые нарезатели → канонические типы (нулевой риск для потребителей); чат-смежность — одно ребро `follows` (build_adjacency симметризует), слабое (β 0.4), per-thread; метаданные чата в тексте, без новых полей схемы; `absorb_once` — полная инертность после первого ингеста; приоритет пересечения источников = наиболее сохраняющая политика (`absorb_once`>`absorb`>`mirror`); semantic-drift отложен (структура надёжнее статистики; точность > токенов).
- **Открытые вопросы / среда:** 13 селфтестов + `selftest_install.py` зелёные. `python-pptx` на этой машине не импортируется (сломан `_imaging` Pillow на Python 3.14) → ветка PPTX корректно мягко пропускается (тот же класс, что torch ранее). `mypy --strict` — Этап 12 (1.31). **Коммитить через Bash-heredoc** (PowerShell 5.1 ломает многострочный `git commit -m @'…'@`). Не-Claude-Code-среды (Codex/Qwen) не протестированы — Этап 19. Строка Этапа 12 в `BASE-PROMPT.md` обновлена под фактическое состояние repo (появились `_chat_units`/`follows`/`absorb_once`/`detect_policy_conflicts`).
