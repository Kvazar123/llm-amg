# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** 1 (стабилизация модели данных): все 8 задач выполнены; осталась закрывающая сессия по протоколу §4.6
- **Сделано в последней сессии:** задачи 1–4 Этапа 1 — канон `type` и `source_kind` закреплён: в 02-data-model.md новый раздел «Типы узлов (`type`)» (канон по классам `source_kind`, tree-sitter-типы как допустимое расширение до задачи 8, потребители канона) и строка `type` таблицы полей переведена на ссылку к нему; `type: derived` убран из промпта `agents/amg-synth.md` и § `amg-synth` в 08-agents-skills.md (обзор — `type: overview`, хаб — `type: hub`, происхождение — `source_kind: synthesized` при `apply`); кодовых правок не потребовалось — `retrieve.TIER_OF_TYPE`, `consolidate._branch_members`/`protect_types`/`episodic_types`, `apply_derivation` и демо-граф уже соответствовали канону (сверено чтением). Закрыты пункты реестра 2.3 пп. 1 и 4, 2.9 п. 1; задачи отмечены в §5. Все 5 селфтестов зелёные (stage2 — прежние SKIP pdf/docx/xlsx). Тем же заходом выполнены задачи 5–6 (документация без правок кода): `branch_budget` добавлен в таблицу полей 02-data-model.md, planned-поля `confidence`/`provenance`/`verification` — подразделом «Запланированные поля» с привязкой к Этапу 13; пометка к 2.3 п. 2 (закрывается после задачи 7)
- **Сделано после (та же серия сессий, задачи 7–8):** `migrate_schema.py` + `selftest_migrate.py` (идемпотентная миграция: `source_kind`/`type: derived`, грамматические `kind`, разметка `origin` по классу узла-владельца; `lineno` восстанавливает следующий `bootstrap` веткой дрейфа — связка покрыта тестом); канонизация tree-sitter `kind` картой `_TS_DEF` при извлечении + проверка `type` в дрейфе указателя `reconcile.py`; `_treesitter_units` адаптирован к двум поколениям биндинга `tree-sitter-language-pack` (alef-rewrite ≥ 1.8 ломал извлечение до файловых единиц молча); точечно синхронизированы 02/04/05; закрыт пункт 2.3 п. 2. Опциональные библиотеки установлены — selftest_stage2 прогоняет pdf/docx/xlsx/treesitter вживую, все 6 селфтестов зелёные
- **Следующий шаг:** закрывающая сессия Этапа 1 (§4.6, строго по порядку): остатков пунктов раздела 2 у этапа нет (2.3 пп. 1, 2, 4 и 2.9 п. 1 закрыты по ходу) → прогнать DoD этапа (миграция на старом графе — `selftest_migrate.py`; hub/overview одинаково в retrieve/consolidate; схема == frontmatter) и все селфтесты → свернуть тело этапа и пункты 1.4, 1.5 (остаток), 1.25 раздела 1 → выпуск v0.3.0 (VERSION, CHANGELOG, тег, пуш с тегами)
- **Открытые вопросы / блокеры:** мелкое расхождение — `STATUS.md` есть в карте репозитория CLAUDE.md §2, но отсутствует в каноническом списке 4.9 (свернуть при ближайшей правке 4.9)
- **Песочница `../amg-testbed`:** не создана (существует пустой каталог; наполнение — Этап 6)
- **Заметки для следующей сессии:** при свёртке Этапа 1 в DoD-строку включить: канон закреплён без правок retrieve/consolidate (уже соответствовали), `migrate_schema.py` + 6-й селфтест, проверка `type` в дрейфе, адаптер двух поколений tree-sitter-биндинга; селфтестов теперь шесть (добавился `selftest_migrate.py`) — упомянуть в CHANGELOG; вопрос бакета `summarize_episodes` отложен в 2.8 п. 5 (Этап 3); в репозитории не держать каталог `amg/` — иначе шаг «каталог движка» в `resolve_amg_root` укажет на него в дев-раскладке (тест 16 терпим к обоим исходам)
