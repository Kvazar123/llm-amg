# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап:** Этап 12 — «производительность и масштабирование» — **В РАБОТЕ**. **Группа 1 закрыта целиком** (фундамент: задачи 12 mypy, 14 log; хвост: 4 embed-cache, 9 bench, 10 барьер). **Группы 2–4 не начаты.** НЕ создавать отдельный подэтап перед 13: остаток — это незакрытый Этап 12, он весь перечислен ниже.
- **Готово в этой сессии (Группа 1 хвост, коммит группы):**
  - **Задача 9 — `bench.py`** (новый `skills/amg-retrieve/scripts/bench.py` + `selftest_bench.py`): самодостаточный генератор `--make-bench --nodes N --seed S` (узлы прямо в `nodes/`, эмбеддинги off) + замер на любом `--store`; мерит `load_nodes`/`build_adjacency`/`retrieve`(writes off)/`eval`/bootstrap(`--project`), best-of-N. В гейте mypy (12-й модуль). **База до индекса:** 3800 узлов → `load_nodes` ≈ 4.3 с (retrieve/eval целиком в неё упираются). Тем же bench мерить Группу 2.
  - **Задача 10 — барьер**: усилен и выделен в раздел «Boundaries» в `entrypoint/CLAUDE.md`/`AGENTS.md`/`AGENTS.codex.md` + зеркально в `08-agents-skills.md`. Реестр **2.16 п.6 закрыт**.
  - **Задача 4** подтверждена регрессией `selftest_embed` check 3 (закрыта).
  - roadmap §5 Этап 12 (задачи 4/9/10 → «Выполнено (свёрнуто)») и §2.16 п.6 обновлены; `mypy.ini` files= +`bench.py` (12 модулей).
- **Уже было (прежние сессии):** `c34c4bd` mypy --strict (задача 12); `8524939` транзакционный `log.md` (задача 14, §1.15); `9b4cbef` §4.10 ленивая деривация.
- **Решения по развилкам (ЗАФИКСИРОВАНЫ):**
  - **Индекс (Группа 2):** строить СРАЗУ; `cache/index.sqlite` (+ `cache/index.meta` сигнатура); эжер-обновление писателями + дешёвая валидация в читателе (`retrieve.load_nodes` сверяет сигнатуру `nodes/` stat-обходом, при несовпадении/порче — скан+пересборка, скан-fallback безусловен); BM25 оставить Python-овским (не FTS5), чтобы не сдвинуть eval; форма `load_nodes` не меняется. **Уточнение по телу узла:** индекс ХРАНИТ `body` (в реальном графе тела почти всегда пустые — только синтез-хабы; так `assemble_pack` не трогается). **Кросс-скилл refresh:** писатели в `amg-bootstrap`/`amg-consolidate`, `index_store` в `amg-retrieve` → `sys.path.insert` (паттерн `mypy.ini`/`consolidate._load_eval_modules`); продумать инкрементальный upsert (особенно `notes.add_note` — одиночная запись на большом графе) vs полная пересборка.
  - **Задача 7:** скрытый переключатель `derivation` НЕ вводим; блоки дают 5/6/13; полная лень → §4.10 / Этап 17.
  - **Задача 8:** per-role дефолты + `reasoning_effort`; живой замер отложить.
  - **mypy селфтесты:** ИСКЛЮЧЕНЫ из гейта. Канонический гейт: `python -m mypy` без аргументов (читает `files=`) → 12 модулей движка.
- **ОСТАЛОСЬ в Этапе 12:**
  - **Группа 2 — ИНДЕКС (ядро DoD):** задачи 1–3. Новый `skills/amg-retrieve/scripts/index_store.py` (+`selftest_index.py`): строит `cache/index.sqlite` (id/type/status/summary/source_path/lineno/searchable-text/edges-json/part_of-json/body) + кэш adjacency (инвалидация по версии графа + хешу `relation_priors`/`default_edge_weight`); `retrieve.load_nodes` читает индекс при свежести, иначе скан+пересборка; best-effort refresh-хук у ВСЕХ писателей (`reconcile.plan`/`apply_derivation`, `consolidate.apply_actions`/`fold_weights`, `notes.add_note`, `migrate_schema`) после `tx.commit()`. Дописать индекс-замеры в `bench.py` (build/index-load/rebuild — «до/после» в одном прогоне). DoD: малый граф прост; большой не парсит все `.md`; индекс удаляем/пересобираем; `--make-demo` hop-recall 1.00 не меняется; selftest_index build→read→delete→rebuild идентичный выход.
  - **Группа 3:** задача 11 (`consolidate.make_plan`: ограничить near-dup кандидатов типами `episodic_types` — `ids`/`toks` по эпизодическим, §1.27, 2 строки); задача 13 (resume: `agents/amg-builder.md` эхоит `content_sha` в derived-элемент [сейчас выдаёт `{id,summary,lang,edges}`], `reconcile.apply_derivation` сверяет `content_sha`↔текущий `source_hash`, skip протухшего `skipped_stale`; SKILL bootstrap «применить уцелевшие `derived-*.json` ПЕРВЫМИ»); задачи 5–6 (`partition_queue.py`/`inspect_queue.py` в `amg-bootstrap/scripts`, замена ad-hoc нарезки очереди в SKILL шаг 3); задача 7 (блоки — см. выше).
  - **Группа 4:** задача 8 — в `config.yml` блок `models` добавить структурную форму с `reasoning_effort` (низкий discovery, высокий synthesis) + задокументировать протокол замера; живой замер отложить.
- **К ЗАКРЫТИЮ этапа (doc-sync + сворачивание + релиз v1.3.0):** синхр. `03`/`06`/`09`/`10` + THEORY/GUIDE/READMEs под индекс/задачи; индекс в раскладку (`02-data-model` диаграмма+таблица, `03-storage`, `consistency-model §4`); **дрейф `episodic_types`** в `07-consolidation` §«План» (стр. ~57) и таблице (стр. ~149): `[section, note]` → `[section, note, open_question, plan]`; строка Этапа 13 в `BASE-PROMPT.md`; свернуть §1.15/1.27/1.31 раздела 1 и тело §5 Этапа 12; VERSION/CHANGELOG/тег `v1.3.0`.
- **Среда/процедуры:** **torch/эмбеддинги РАБОТАЮТ** (пользователь переустановил) — пометка «torch не грузится» УСТАРЕЛА. Гейт типов: `python -m mypy` (без аргументов; предупреждение `[mypy-selftest_*]` — давнее, безвредно, exit 0). **14 селфтестов + `selftest_install.py` зелёные**, живой CLI-цикл чист. **Коммитить через Bash-heredoc** (PowerShell 5.1 ломает многострочный commit). `retrieve` с `embeddings: auto` грузит реальную модель (медленно) — в фикстурах/бенче ставить `embeddings: off`. Не-Claude-Code-среды — Этап 19.
