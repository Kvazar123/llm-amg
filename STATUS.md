# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** 0 — стабилизация ядра сверки (все задачи 1–11 выполнены; осталась закрывающая сессия)
- **Сделано в последней сессии (задачи 1–6, три коммита):** `reconcile.py` — stale requeue (`derived_from_hash != content_sha` ИЛИ `status: stale`; очередь пересоздаётся из состояния графа каждым `plan()` — самовосстановление после сбоя; счётчики `requeued_stale`/`pointer_refreshed`); `lineno`/`qualname` во frontmatter + `qualname`/`lineno`/`lang` в queue item (вход `amg-builder.md` синхронизирован); тихое обновление указателя и policy при дрейфе без смены хеша; пересборка structural-рёбер на `changed` (`_refresh_structural_edges`, пережившее ребро наследует `w`/`coact`, смысловые не трогаются; обновляются и `policy`/`source_path`); поле `origin` рёбер: `structural`/`semantic`/`synthesized`/`consolidation` по местам записи; `_merge_part_of` (темы накапливаются, новая берёт вес, симплекс-нормализация); перевод в `active` только при новой `summary` (иначе узел остаётся `stale` и в очереди); `consolidate.py` — `source_kind: synthesized` вместо `derived`, штамп `origin: consolidation`. Решения: `lang` узла = язык сводки, язык источника только в очереди (пометка к 2.3 п. 2); merge-режим part_of «новое значение побеждает» (пометка к 1.6). Задачи 7–8: обнаружение переносов (`_detect_moves`/`_migrate_node`: earned-поля мигрируют на новый id, внутрифайловые цели рёбер и первичная тема переписываются, входящие ссылки перенаправляются, чистый перенос — без вызова модели; только mirror-узлы, absorb — сирота + новый узел; счётчик `moved`); резолвер imports (`_module_map`: точечный путь + суффиксы, неоднозначный суффикс не резолвится; внешние импорты висячие). Задачи 9–11: markdown-нарезатель игнорирует «заголовки» в код-ограждениях (```/~~~, чужой маркер внутри блока не закрывает); regression-тесты дополнены до полного списка задачи 10 (16 сценариев: + ограждения, + lineno в pack через реальный retrieve, + цепочка корня); `graph_store.resolve_amg_root` — корень по цепочке 4.9 (--root → AMG_AGENT_DIR → конфиг вверх от project_root, с пробой `<предок>/.claude/amg/config.yml` — чинит глобальную установку → каталог движка, только если его `amg/` существует → умолчание `.claude`), литералы `.claude` в reconcile/consolidate устранены, обе CLI принимают `--root`. `selftest_reconcile.py` — 16 сценариев; все 5 селфтестов зелёные (в stage2 прежние SKIP pdf/docx/xlsx). Ранее в сессии: релиз v0.1.0 (VERSION/CHANGELOG, политика версионирования, тег)
- **Следующий шаг:** закрывающая сессия Этапа 0 (порядок CLAUDE.md §4.6: закрыть пункты раздела 2 этапа в 04/05-доках и 02/03/07-пометках → DoD этапа + селфтесты → свернуть этап и его пункты разделов 1–2 → выпуск v0.2.0: VERSION, CHANGELOG, `chore(release)`, тег, пуш)
- **Открытые вопросы / блокеры:** мелкое расхождение — `STATUS.md` есть в карте репозитория CLAUDE.md §2, но отсутствует в каноническом списке 4.9 (свернуть при ближайшей правке 4.9)
- **Песочница `../amg-testbed`:** не создана (существует пустой каталог; наполнение — Этап 6)
- **Заметки для следующей сессии (закрывающей):** DoD-пункт «pack не содержит `:None`» уже покрыт тестом 15; при сверке 05-reconcile.md учесть новые случаи диффа (`moved`, requeue, `pointer_refreshed`) и поля очереди; в репозитории не держать каталог `amg/` (пустой артефакт удалён в этой сессии — иначе шаг 4 резолвера укажет на него в дев-раскладке; тест 16 терпим к обоим исходам)
