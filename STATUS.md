# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап: Этап 19 — все 10 задач кода ВЫПОЛНЕНЫ за сессию 1 (три коммита: группы A+C `8141ac2`, D `081847a`, B `4b9f6eb`); осталась ЗАКРЫВАЮЩАЯ сессия.** Сделано: детерминированный резолвер рёбер (`_build_symbols`/`_resolve_symbol` — `calls` только через привязки импортов файла, builtins не эмитятся; backbone `defines` w 1.0; новый rel `inherits` β 0.8/w 0.8), нормализация целей уникальным суффиксом пути (apply + ветки + свип всего графа, порядко-нечувствительное сравнение — старый граф лечится одним bootstrap, устоявшийся строгий no-op), per-item валидация apply (`_sanitize_item`, авто-починка swap `confidence`↔`edges` и двойного префикса, `skipped_invalid`+причины), гейт связности (`reconcile.graph_metrics` + CLI `metrics` + блок в `/amg status`; internal ≠ external `imports`; пороги `connectivity_gate`, advisory), derivation-cache (`cache/derivations/`, ключ sha+`DERIVATION_CONTRACT`+lang; авто-restore в `bootstrap`/`plan`, команда `apply-cached`, ключ `derivation_cache`), глобальный линковщик (`link_candidates.py`: ANN по кэшу эмбеддингов / лексический фолбэк, уже-связанные не переноминируются, stale пропускаются; `--hubs` → детерминированные якоря хабов; новый субагент `agents/amg-linker.md`, роль `module_summary` в `install.ROLE_AGENTS`), промпты билдера/синта обновлены (documents обязателен; synth анкерится к якорям, хабы ДО линковки), SKILL-workflow 8 шагов с приёмочным гейтом.
- **Закрывающей сессии осталось:** (1) приёмка на живом dogfood-графе (пересборка → метрики связности до/после, включая старый фрагментированный граф — свип должен починить префиксы); (2) доки: THEORY §4.2/§6 (новая теория — детерминированные рёбра до LLM выполнены полностью; глобальная линковка поверх summary; кандидаты по эмбеддингам), 02-data-model (`defines`/`inherits`, w-таблица рёбер), 04-ingest (bindings/dotted-цепочки/bases/defines в нарезателях), 05-reconcile (резолвер/нормализация/валидация/кэш/`metrics`; «text только у бинарных» — уже неверно и для CSV/log/chat), 06-retrieval (`inherits` в priors; переиспользование эмбеддингов кандидатами), 08-agents-skills (линковщик/якоря/порядок/workflow), 09-config (`connectivity_gate`/`derivation_cache`/`linker`/`inherits`), 10-eval-tools (метрики связности), GUIDE/READMEs; (3) сворачивание §1.40–§1.46 и тела этапа; (4) выпуск v1.10.0. **Решения, принятые сверх/вместо буквы roadmap (внесены пометками в задачи):** линковщик хабы НЕ рождает (только synth по якорям — иначе вернулся бы недетерминизм §1.46); кэш-restore автоматический в bootstrap; резолв цепочек ТОЛЬКО через импорты (не по совпадению имён).
- **Замечания пользователя (сессия 1, применять всегда):** (1) поставляемые артефакты (шаблон `config.yml`, промпты) НЕ ссылаются на этапы roadmap — шаблон вычищен, у Этапа 21 пункт (в)/(г) про пересмотр конвенции для комментариев скриптов; (2) хардкод весов/порогов — только осознанный (текущие: defines 1.0, inherits 0.8/β0.8, linker top_k 5/min_sim 0.35/batch 40, gate 0.9/0 — обоснования в пометках задач); (3) экономия токенов — главное для Этапа 20, здесь — корректность/полнота/качество.
- **Два dogfood-пункта Этапа 17 по-прежнему отложены на живой проект ПОСЛЕ Этапов 18–20:** (1) реальная эмиссия паттернов живым `amg-synth`; (2) флип `apply_hebbian`→true на реальном `usage.log`. Оба — не в dev.
- **Стенды (НЕ удалять):** `../amg-bigtest`, `../amg-testbed`. Реальный dogfood-граф (~1 МБ проект) — приёмочный кейс Этапов 18–20; путь у пользователя (не в репо).
- **Тесты:** 22 селфтеста движка (+`selftest_build` — конвейер с заглушкой-билдером, 7 проверок) + `selftest_install` (12, теперь 6 агентов) + mypy(18, +`link_candidates.py`) + eval-демо pack-recall 1.00 — зелёные.
- **Среда/процедуры (durable):** torch/эмбеддинги РАБОТАЮТ, но ST-бэкенд нестабилен под тяжёлым пере-кодированием (изолировать в subprocess). Гейт типов: `python -m mypy` без аргументов → 18 модулей. **Коммитить через Bash-heredoc**; пуш — `git push --follow-tags`. **Большие правки roadmap — частями, точным копированием из свежего Read; для удаления/замены блоков — python по маркерам**. **В батарее селфтестов НЕ глотать вывод** (`out=$(python "$f" 2>&1)` и печатать при провале): разовый «FAIL» selftest_export оказался невоспроизводимой гонкой первого прогона (Windows AV/индексатор на свежем большом HTML в C:\Temp), вывод был потерян из-за `>/dev/null`. `BASE-PROMPT.md` — gitignored. Не-Claude-Code-среды — Этап 22.
