# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** 0 закрыт (v0.2.0, 2026-06-13); следующий — Этап 1 (стабилизация модели данных), не начат
- **Сделано в последней сессии:** закрывающая сессия Этапа 0 по протоколу §4.6 — документация синхронизирована с кодом (05-reconcile полностью: случаи диффа `moved`/requeue/дрейф, `origin`, очередь, merge `part_of`, гейт `active`, резолвер imports, цепочка корня; 04/03/02/07/08 — точечно); закрыты пункты реестра 2.6 (целиком), 2.3 п. 9, 2.4 п. 5, 2.5 п. 7, 2.8 п. 8, 2.9 п. 2; DoD этапа прогнан полностью, все 5 селфтестов зелёные (16 reconcile-сценариев; в stage2 прежние SKIP pdf/docx/xlsx — опциональные библиотеки не установлены); roadmap свёрнут (тело Этапа 0 → однострочная сводка; пункты 1.1–1.3, 1.5–1.7, 1.19, 1.24 → строки «исправлено на Этапе 0 → ссылки»); выпуск v0.2.0 (VERSION, CHANGELOG, аннотированный тег). До закрытия в той же серии сессий выполнены все 11 задач этапа (коммиты c21dbdf…171b819)
- **Следующий шаг:** Этап 1, задачи 1–4 (обновить 02-data-model.md; закрепить канон `type`; убрать `type: derived`; закрепить `source_kind`) — загрузка по таблице базового промпта (skills/amg-bootstrap/scripts/* + `retrieve.py`, `consolidate.py` как потребители схемы)
- **Открытые вопросы / блокеры:** мелкое расхождение — `STATUS.md` есть в карте репозитория CLAUDE.md §2, но отсутствует в каноническом списке 4.9 (свернуть при ближайшей правке 4.9)
- **Песочница `../amg-testbed`:** не создана (существует пустой каталог; наполнение — Этап 6)
- **Заметки для следующей сессии:** Этап 0 уже дал Этапу 1: поле `origin` реализовано (разметка legacy-рёбер — в задаче 7 миграции), `lineno`/`qualname` в схеме и документации, решение по `lang` (пометка к 2.3 п. 2: язык источника при будущей нужде — отдельное поле `source_lang`), вопрос бакета для узлов `summarize_episodes` отложен в 2.8 п. 5 (Этап 3); в репозитории не держать каталог `amg/` — иначе шаг «каталог движка» в `resolve_amg_root` укажет на него в дев-раскладке (тест 16 терпим к обоим исходам)
