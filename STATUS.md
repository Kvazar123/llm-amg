# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап: Этап 18 (развёртывание и разрешение store) — все 6 задач выполнены за одну сессию тремя группами-коммитами** (`25c3af2` резолвер+диагностика, `299983b` установщик+слои конфига, `99d4277` документация). Осталась **закрывающая сессия Этапа 18**: DoD + все селфтесты, сворачивание (§1.37/§1.38/§1.39/§1.49/§1.50 + части §1.52, тело этапа в §5), проверка строки Этапа 19 в таблице загрузки BASE-PROMPT, выпуск v1.9.0 (VERSION+CHANGELOG+тег+пуш). Доки этапа УЖЕ синхронизированы в рабочей сессии (12-install/INSTALL/09-config/03-storage/01-overview/GUIDE/READMEs) — закрывающей сессии остались только шаги 2–4 протокола; пометки для неё — в теле Этапа 18 roadmap.
- **Ключевые решения этапа (кратко; полнее — в отметках задач Этапа 18):** (1) резолвер: вето по сигнатуре движка (`skills/`/`agents/`/`install.py` в кандидате = чекаут, даже «инициализированный»), пресеты `{.claude,.agents}` первыми и БЕЗ требования init (иначе ломается ручная установка), «голый» `amg/` — только инициализированный; (2) **два решения сверх буквы задач** — уровень дома пропускается поиском (глобальный конфиг ≠ store) и шаг «расположение движка» требует инициализированный store (иначе задача 4 воспроизводила §1.39 на уровне дома); (3) слои конфига: глобальный = блоки `models`+`retrieval.embeddings` целиком, локальный при глобальной установке пишется БЕЗ них (`_strip_*`), **наследование включает ключ `agent_dir`** локального конфига (фикстуры герметичны — чужой глобальный конфиг машины не подмешивается в селфтесты); (4) §1.50 — SystemExit с именем корня, намеренно не молчаливый `{}`; (5) ссылки «Этап 19 (среды)» в шипнутых доках заменены неявными формулировками (предпочтительный вариант списка Этапа 21).
- **Новое в Этапе 21 (по вопросу пользователя):** пункт «Ссылки на дорожную карту в управляющем слое» — в исполняемых промптах ссылок на этапы быть не должно (осталось ≈3 «Stage 13/14» в `skills/amg-retrieve/SKILL.md`); в комментариях скриптов прошлая атрибуция (Stage N / audit 1.XX) — допустимая конвенция провенанса (номера стабильны), будущих обещаний не осталось (греп подтвердил).
- **Тесты:** 21 селфтест движка + `selftest_install` (12 проверок, +test_nested_set_and_absorb_once, test_global расширен слоями) + mypy(17) зелёные; e2e-дым: глобальная установка с фиктивным HOME → bootstrap установленным движком из cwd проекта кладёт узлы в ЛОКАЛЬНЫЙ store, домашний слой остаётся config-only, неустановленный сосед получает диагностику «AMG is not installed» с именем корня.
- **Два dogfood-пункта Этапа 17 по-прежнему отложены на живой проект ПОСЛЕ Этапов 18–20:** (1) реальная эмиссия паттернов живым `amg-synth` (зависит от линковщика Этапа 19); (2) флип `apply_hebbian`→true — ждёт реального `usage.log` с чисто пересобранного графа. Оба — не в dev.
- **Стенды (НЕ удалять):** `../amg-bigtest` (`_measure_lazy.py` + `_measure_big.py`, README с находками), `../amg-testbed` (dogfood-песочница). Реальный dogfood-граф (~1 МБ проект) — приёмочный кейс Этапов 18–20; путь у пользователя (не в репо).
- **Среда/процедуры (durable):** torch/эмбеддинги РАБОТАЮТ, но ST-бэкенд нестабилен под тяжёлым пере-кодированием (сегфолт воркера — изолировать в subprocess, замеры по BM25+model2vec). Гейт типов: `python -m mypy` без аргументов → 17 модулей (exit 0; install.py вне гейта — решение Этапа 12). **Коммитить через Bash-heredoc** (PowerShell 5.1 ломает многострочный commit); пуш — `git push --follow-tags`. **Большие правки roadmap — частями, точным копированием из свежего Read; для удаления блоков — `sed` по номерам строк** (с `cp` бэкапом). `BASE-PROMPT.md` — gitignored (несёт дозаточенные аудитом строки загрузки Этапов 18–20). Не-Claude-Code-среды — Этап 22.
