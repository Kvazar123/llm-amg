# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** 1 (стабилизация модели данных), в работе: задачи 1–6 выполнены, остались 7–8 (кодовые; согласовано вести отдельной сессией)
- **Сделано в последней сессии:** задачи 1–4 Этапа 1 — канон `type` и `source_kind` закреплён: в 02-data-model.md новый раздел «Типы узлов (`type`)» (канон по классам `source_kind`, tree-sitter-типы как допустимое расширение до задачи 8, потребители канона) и строка `type` таблицы полей переведена на ссылку к нему; `type: derived` убран из промпта `agents/amg-synth.md` и § `amg-synth` в 08-agents-skills.md (обзор — `type: overview`, хаб — `type: hub`, происхождение — `source_kind: synthesized` при `apply`); кодовых правок не потребовалось — `retrieve.TIER_OF_TYPE`, `consolidate._branch_members`/`protect_types`/`episodic_types`, `apply_derivation` и демо-граф уже соответствовали канону (сверено чтением). Закрыты пункты реестра 2.3 пп. 1 и 4, 2.9 п. 1; задачи отмечены в §5. Все 5 селфтестов зелёные (stage2 — прежние SKIP pdf/docx/xlsx). Тем же заходом выполнены задачи 5–6 (документация без правок кода): `branch_budget` добавлен в таблицу полей 02-data-model.md, planned-поля `confidence`/`provenance`/`verification` — подразделом «Запланированные поля» с привязкой к Этапу 13; пометка к 2.3 п. 2 (закрывается после задачи 7)
- **Следующий шаг:** Этап 1, задачи 7–8 (миграционный скрипт старых узлов — задача 7: `source_kind: derived` → `synthesized`, `type: derived` у хабов → `hub`/`overview`, разметка `origin` legacy-рёбер, по возможности восстановить `lineno`; канонизация tree-sitter `kind` при извлечении либо расширение `TIER_OF_TYPE`/`CODE_TYPES` — задача 8, см. 1.25). В той же сессии задачи 5–6 уже не делать — выполнены (branch_budget в таблице полей; planned-поля отдельным подразделом 02-data-model.md)
- **Открытые вопросы / блокеры:** мелкое расхождение — `STATUS.md` есть в карте репозитория CLAUDE.md §2, но отсутствует в каноническом списке 4.9 (свернуть при ближайшей правке 4.9)
- **Песочница `../amg-testbed`:** не создана (существует пустой каталог; наполнение — Этап 6)
- **Заметки для следующей сессии:** для задачи 7 (миграция): legacy-разметка `origin` рёбер (`imports`/`calls` → `structural`, прочие → `semantic`), `source_kind: derived` → `synthesized`, `type: derived` у хабов → `hub`/`overview`, по возможности восстановить `lineno`; для задачи 8 учесть, что в канон 02-data-model tree-sitter-типы вписаны как временное расширение — после канонизации обновить этот абзац; решение по `lang` (источник — отдельное поле `source_lang` при будущей нужде) — пометка к 2.3 п. 2; вопрос бакета `summarize_episodes` отложен в 2.8 п. 5 (Этап 3); в репозитории не держать каталог `amg/` — иначе шаг «каталог движка» в `resolve_amg_root` укажет на него в дев-раскладке (тест 16 терпим к обоим исходам)
