# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап: Этап 21; подготовка ЗАКРЫТА ЦЕЛИКОМ (сессии 1–3, релиз v1.11.2, запушен). Остался ровно ПЕРЕВОД по про-файловому протоколу** — он зафиксирован в теле Этапа 21 блоком «Оставшийся объём этапа» со ВСЕМИ требованиями пользователя: шаг 1 — вычитка русского файла ДО перевода (чеклист §7 + регистр §3.4 «живое объяснение», зачистка §3.1/§3.2-рецидивов, сверка чисел/дефолтов с кодом, нейтрализация `.claude/*` гибридом + ведущая оговорка, привязка опережающих описаний); шаг 2 — перевод уровня носителя строго по глоссарию `docs/en/glossary.md` (термины дописывать на месте; ссылок на глоссарий из доков НЕТ), ни одной потерянной детали, без калек и дословных русских фраз, Mermaid переводятся, ссылки en→en / ru→ru, раскладка `docs/en/*` зеркальна `docs/ru/*`. Порядок (рекомендация): architecture/README → 01–10, 12 → THEORY → GUIDE → до-синхронизация README.md. INSTALL уже переведён (en в корне + INSTALL_RU.md); consistency-model уже английский. По `11-roadmap.md` решить в переводной сессии: перевод целиком или английский снимок при закрытии.
- **Раздел 1 аудита: открытых пунктов НЕТ.** §1.53 закрыт standalone-фиксом v1.11.2 (гитигнор `!`, «последнее правило побеждает», `selftest_ignore.test_gitignore_negation`; упрощение задокументировано в 04-ingest); §1.54 (dogfood-установка: модель задала 4/12 вопросов; dev-CLAUDE.md чекаута в контексте установки) исправлен на месте — INSTALL ужесточён (осмотр проекта + классификация папок, каждый вопрос по одному, KEY у языка/эмбеддингов, раздел «что менять потом дёшево»), scope-guard в dev-CLAUDE.md; живая проверка нового опроса — следующая dogfood-установка пользователя. Тела §1.53/§1.54 свёрнуты/пометены; при закрытии этапа §1.54 ужать до однострочника.
- **Сделано в сессиях 1–3 (сжатая опись; полный след — CHANGELOG v1.11.2 и тело Этапа 21):** глоссарий (~190 пар); §1.33–§1.35 закрыты (язык-универсальность, английские примеры, привязка словесных триггеров к «память/граф памяти/AMG» везде); свип roadmap-ссылок из ВСЕГО поставляемого кода (~247 замен; греп `Stage \d|stage \d|audit 1\.|roadmap` по skills/agents/entrypoint/config.yml/install.py чист — заново не вносить); перенумерация 18→21/19→22 в CLAUDE.md/CHANGELOG; полнота GUIDE (все флаги всех скриптов) + INSTALL сверен с флагами install.py + 09-config сверен с шаблоном и кодом (легаси `sources:` дописана); READMEs реструктурированы (карта в конец, «Что реализовано» списком, +3 преимущества); INSTALL.md → английский управляющий слой + корневой INSTALL_RU.md (решение пользователя), ссылки перенаправлены.
- **Поведение при смене конфига (сверено по коду, для GUIDE-ответов):** конфиг читается заново каждым прогоном (слежения нет) — новые пути/политики подхватывает следующий `/amg sync`/петля; эмбеддинг-кэш самолечится, но для линковки после включения повторить sync; смена `working_language` задним числом НЕ пересводит (граф двуязычится) — потому KEY; тиринг `models` вступает в силу переустановкой.
- **Решения пользователя (действуют):** глоссарий сохранить, ссылок на него из документации нет (только roadmap); живой замер ×20–30 и связности — совместная приёмка Этапов 19–20 на dogfood-стенде, вне dev-сессий; два dogfood-пункта Этапа 17 (эмиссия паттернов, флип `apply_hebbian` на реальном usage.log) — на живом проекте после 18–20.
- **Замечания пользователя (действуют всегда):** поставляемые артефакты и ЛЮБЫЕ комментарии скриптов без ссылок на этапы/аудит (легаси вычищено — не вносить заново); хардкод весов/порогов — только осознанный; документацию писать регистром живого объяснения (§3.4 / CLAUDE.md §7 п.7); английское зеркало — native-level без калек.
- **Стенды (НЕ удалять):** `../amg-bigtest`, `../amg-testbed`. Реальный dogfood-граф — путь у пользователя (не в репо).
- **Тесты:** 22 селфтеста движка (в т. ч. новый `test_gitignore_negation`) + `selftest_install` + mypy(18) — зелёные на v1.11.2.
- **Среда/процедуры (durable):** torch/эмбеддинги работают, но ST-бэкенд нестабилен под тяжёлым пере-кодированием (изолировать в subprocess). Гейт типов: `python -m mypy` без аргументов → 18 модулей. Коммитить через Bash-heredoc; пуш — `git push --follow-tags`. Большие правки roadmap — python по маркерам с бэкапом в scratchpad. В батарее селфтестов НЕ глотать вывод (`out=$(python "$f" 2>&1)`, печатать при провале). `BASE-PROMPT.md` — gitignored. Не-Claude-Code-среды — Этап 22.
