# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап: Этап 18 ЗАКРЫТ (v1.9.0, 2026-07-03) → следующий — Этап 19 (корректная и связная сборка графа, §1.40–§1.46).** Закрывающая сессия прошла по протоколу: доки досверены (05-reconcile/06-retrieval + заголовки `reconcile.py`/`consolidate.py` — старые формулы цепочки резолвера заменены), DoD и все селфтесты зелёные, §1.37/§1.38/§1.39/§1.49/§1.50 и тело этапа свёрнуты, §1.52 сжат до оставшегося дрейфа (07-consolidation `log.md` → Этап 21), VERSION/CHANGELOG/тег v1.9.0. Перед Этапом 19 читать ПЕРВЫМ вводку «Этапы 18–20» в §5 roadmap; строка Этапа 19 в BASE-PROMPT дополнена состоянием repo после Этапа 18 (правила резолвера, слои конфига, герметичность фикстур без `agent_dir`, обязательный `config.yml` в фикстурах — §1.50).
- **Ключевое из Этапа 18, что действует дальше:** чекаут AMG (сигнатура `skills/`/`agents/`/`install.py` в кандидате-`amg/`) никогда не резолвится как store; «голый» `amg/` и «расположение движка» — только инициализированные; уровень дома = слой глобального конфига (`~/<agent_dir>/amg/config.yml` — блоки `models`+`retrieval.embeddings`), мержится ПОД локальный по ключам, включается ключом `agent_dir` локального конфига; установка ведётся из каталога вне проекта (`--target`), поток показывает существующий конфиг (`in force:`), `--set` умеет точечные пути, флаги `--absorb-once`/`--set-global`.
- **Хвост на Этап 21 (заведён пунктами):** ссылки на roadmap в управляющем слое (в промптах — убрать все, осталось ≈3 «Stage 13/14» в `skills/amg-retrieve/SKILL.md`; в комментариях кода прошлая атрибуция допустима, будущих обещаний нет — греп подтвердил) + §1.36/§1.52-остаток (лог `.log` + абзац в 07-consolidation) + §1.33/§1.34/§1.35 (языковые проходы).
- **Два dogfood-пункта Этапа 17 по-прежнему отложены на живой проект ПОСЛЕ Этапов 18–20:** (1) реальная эмиссия паттернов живым `amg-synth` (зависит от линковщика Этапа 19); (2) флип `apply_hebbian`→true — ждёт реального `usage.log` с чисто пересобранного графа. Оба — не в dev.
- **Стенды (НЕ удалять):** `../amg-bigtest` (`_measure_lazy.py` + `_measure_big.py`, README с находками), `../amg-testbed` (dogfood-песочница). Реальный dogfood-граф (~1 МБ проект) — приёмочный кейс Этапов 18–20; путь у пользователя (не в репо).
- **Тесты:** 21 селфтест движка + `selftest_install` (12) + mypy(17) + eval-демо pack-recall 1.00 — зелёные на закрытии.
- **Среда/процедуры (durable):** torch/эмбеддинги РАБОТАЮТ, но ST-бэкенд нестабилен под тяжёлым пере-кодированием (сегфолт воркера — изолировать в subprocess, замеры по BM25+model2vec). Гейт типов: `python -m mypy` без аргументов → 17 модулей (exit 0; install.py вне гейта — решение Этапа 12). **Коммитить через Bash-heredoc** (PowerShell 5.1 ломает многострочный commit); пуш — `git push --follow-tags`. **Большие правки roadmap — частями, точным копированием из свежего Read; для удаления/замены блоков — python/sed по маркерам строк** (с `cp` бэкапом). `BASE-PROMPT.md` — gitignored. Не-Claude-Code-среды — Этап 22.
