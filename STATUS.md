# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап: Этап 20 ЗАКРЫТ (v1.11.0, 2026-07-05) → следующий — Этап 21 (перевод документации на английский + финальный проход качества; первая сессия — глоссарий ru→en `docs/en/glossary.md`).** Закрывающая сессия прошла по протоколу: THEORY получила продолжение §4.1 (авто-сводка тривиальных — граница «детерминированное прежде модели» доведена до сводок) и новый §4.4 («Экономика и устойчивость сборки»: ресенд-экономика, долговечные порции, завершение как проверяемое утверждение); синхронизированы 01/02/04/05/08/09 + GUIDE («Качество и стоимость сборки» — экономность и устойчивость) + READMEs; DoD (headless) и все тесты зелёные; §1.47/§1.48/§1.51 и тело этапа свёрнуты; VERSION/CHANGELOG/тег v1.11.0, пуш с тегами.
- **Ключевое из Этапа 20, что действует дальше:** текст юнита в очереди у ВСЕХ нарезателей (`queue_text_max_chars` 20000, 0 = указатели; + `line_end`); авто-сводка тривиальных функций (`trivial_unit_max_lines`: код 0 / шаблон 3 — третий «код ≠ шаблон»; guard `_SIGNIFICANT_DUNDERS`; кэш дериваций ПОБЕЖДАЕТ шаблон; счётчик `auto_summarized`); капы партий `builder.batch_units` 40 / `batch_max_chars` 120000 (части `queue-<part>-NN.json`, ре-запуск заменяет набор); чекпоинты `derived-*-pNN.json` без нового кода движка (apply пофайловый, freshness `content_sha`); протокол `BATCH|SYNTH COMPLETE|PARTIAL: N/M` + сверка счётчиков в SKILL; `inspect_queue` → `progress.derived_percent`; DERIVATION_CONTRACT НЕ поднят (кэш валиден). Краевой случай: файл из одной функции даёт модулю и функции один sha → кэш-гард шлёт функцию на restore (безвредно).
- **ВАЖНО (решение пользователя, действует):** живой замер ×20–30 по токенам/времени И связности Этапа 19 — совместная приёмка Этапов 19–20 на реальном dogfood-стенде (~1 МБ проект; путь у пользователя), НЕ в dev-сессии. Headless-DoD Этапов 19–20 выполнен.
- **Standalone-фикс v1.11.1 (после закрытия, по ревью пользователя):** §1.36+§1.52 ВЫПОЛНЕНЫ — журнал действий теперь плоский `actions.log` (формат `[<ts>] <txid> <source> | <msg>`, ротация `archive/actions-*.log`, легаси `log.md` перенимается при первой записи со срезом `## `); итоговый аудит промптов нашёл и починил три дрейфа (SKILL консолидации заявлял хеббов decay при дефолте off; в generic/codex-блоках отсутствовал глобальный линковщик/шаг связывания; список полей `status` в `commands/amg.md` отставал; generic-блок теперь зовёт `lifecycle.py status/on/off` вместо ручной правки конфига); READMEs реструктурированы (список «Чем AMG выделяется» в «Что это», «Что реализовано» перенесён к лицензии); §3.2 расширена (батч/ресенд/чекпоинт/резюмируемость/линковка + рецидив «деривации»); Этап 21 дополнен тремя пунктами по ревью (полнота GUIDE со списком известных пробелов флагов; прозаический регистр §3.4 «как объяснение в чате»; зеркальная нативность английского перевода).
- **Замечания пользователя (действуют всегда):** поставляемые артефакты (шаблон `config.yml`, промпты) И НОВЫЕ комментарии скриптов не ссылаются на этапы/аудит roadmap — «почему» пишется по смыслу на месте (повторное замечание на Этапе 20; внесённые сессией 1 ссылки вычищены закрывающей); легаси-атрибуции скриптов — единый проход (в)/(г) Этапа 21; хардкод весов/порогов — только осознанный, с обоснованием; **документацию писать регистром живого объяснения (как в чате), не конспектом** — §3.4/CLAUDE.md §7 п.7.
- **Хвост на Этап 21 (заведён пунктами):** ссылки на roadmap в управляющем слое (≈3 «Stage 13/14» в `skills/amg-retrieve/SKILL.md`; легаси-комментарии скриптов — по (в)/(г), греп `stage |Stage |audit 1\.|roadmap` по `skills/ agents/`) + §1.33/§1.34/§1.35 (языковые проходы) + полнота GUIDE + прозаический регистр + зеркальный английский (три новых пункта). В глоссарий: авто-сводка → auto-summary, чекпоинт/контрольная запись → checkpoint (part), партия → batch, тонкая оркестрация → thin orchestration.
- **Два dogfood-пункта Этапа 17 по-прежнему отложены на живой проект ПОСЛЕ Этапов 18–20:** (1) реальная эмиссия паттернов живым `amg-synth`; (2) флип `apply_hebbian`→true на реальном `usage.log`. Оба — не в dev.
- **Стенды (НЕ удалять):** `../amg-bigtest`, `../amg-testbed`. Реальный dogfood-граф — совместный приёмочный кейс Этапов 19–20; путь у пользователя (не в репо).
- **Тесты:** 22 селфтеста движка + `selftest_install` + mypy(18) + eval-демо pack-recall 1.00 — зелёные на закрытии.
- **Среда/процедуры (durable):** torch/эмбеддинги РАБОТАЮТ, но ST-бэкенд нестабилен под тяжёлым пере-кодированием (изолировать в subprocess). Гейт типов: `python -m mypy` без аргументов → 18 модулей. **Коммитить через Bash-heredoc**; пуш — `git push --follow-tags`. **Большие правки roadmap — python по маркерам с бэкапом в scratchpad.** **В батарее селфтестов НЕ глотать вывод** (`out=$(python "$f" 2>&1)`, печатать при провале). `BASE-PROMPT.md` — gitignored. Не-Claude-Code-среды — Этап 22.
