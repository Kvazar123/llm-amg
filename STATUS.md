# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап: Этап 19 ЗАКРЫТ (v1.10.0, 2026-07-04) → следующий — Этап 20 (экономная сборка ×20–30 без потери качества, §1.47/§1.48/§1.51).** Закрывающая сессия прошла по протоколу: THEORY получила §4.1–§4.3 (детерминированное прежде модели; линковка поверх слоя сводок, хабы до линковки; воспроизводимость кэшем), архитектура 01/02/04/05/06/08/09/10/12 + GUIDE («Качество и стоимость сборки») + READMEs синхронизированы; DoD (headless) и все тесты зелёные; §1.40–§1.46, баннер §4.2 и тело этапа свёрнуты; VERSION/CHANGELOG/тег v1.10.0, пуш с тегами.
- **Ключевое из Этапа 19, что действует дальше:** `calls`/`inherits` резолвятся ТОЛЬКО через `import_bindings` своего файла (builtins не эмитятся); хребет `defines` w1.0, `inherits` β0.8/w0.8; канонизация целей суффиксом (apply + свип, `edges_refreshed`); per-item валидация apply (`skipped_invalid`); гейт `reconcile.py metrics` + блок `connectivity` в `/amg status` (advisory; внутренние висячие ≠ внешние `imports`); derivation-cache с авто-restore в `bootstrap` (`restored_from_cache`); линковщик `amg-linker` по пачкам `link_candidates.py` (уже-связанные не переноминируются; stale пропускаются), хабы — только synth по якорям `--hubs`; SKILL 8 шагов; mypy=18; `selftest_build.py` — каркас конвейер-фикстуры (расширять на Этапе 20).
- **ВАЖНО для Этапа 20:** приёмка на живом dogfood-графе НЕ выполнялась — решение пользователя: мерить одним стендом ПОСЛЕ Этапа 20 (и стоимость ×20–30, и связность Этапа 19: один компонент, ~0 внутренних висячих). Пачки линковщика уже ограничены (`linker.batch_nodes`) — чанкинг задачи 4 остаётся про билдеров и synth; очередь (`_queue_item`) НЕ тронута — задача 1 (текст юнита) свободна; кэш дериваций уже даёт резюмируемость между прогонами.
- **Замечания пользователя (действуют всегда):** поставляемые артефакты (шаблон `config.yml`, промпты) не ссылаются на этапы roadmap; конвенцию для комментариев скриптов ужесточить проходом Этапа 21 (пункты (в)/(г) задачи о ссылках); хардкод весов/порогов — только осознанный, с обоснованием.
- **Хвост на Этап 21 (заведён пунктами):** ссылки на roadmap в управляющем слое (≈3 «Stage 13/14» в `skills/amg-retrieve/SKILL.md`; комментарии скриптов — пересмотр по (в)/(г)) + §1.36/§1.52-остаток (лог `.log` + абзац в 07-consolidation) + §1.33/§1.34/§1.35 (языковые проходы).
- **Два dogfood-пункта Этапа 17 по-прежнему отложены на живой проект ПОСЛЕ Этапов 18–20:** (1) реальная эмиссия паттернов живым `amg-synth` (теперь зависит от живого качества линковщика); (2) флип `apply_hebbian`→true на реальном `usage.log`. Оба — не в dev.
- **Стенды (НЕ удалять):** `../amg-bigtest`, `../amg-testbed`. Реальный dogfood-граф (~1 МБ проект) — совместный приёмочный кейс Этапов 19–20; путь у пользователя (не в репо).
- **Тесты:** 22 селфтеста движка (+`selftest_build`) + `selftest_install` (6 агентов) + mypy(18) + eval-демо pack-recall 1.00 — зелёные на закрытии.
- **Среда/процедуры (durable):** torch/эмбеддинги РАБОТАЮТ, но ST-бэкенд нестабилен под тяжёлым пере-кодированием (изолировать в subprocess). Гейт типов: `python -m mypy` без аргументов → 18 модулей. **Коммитить через Bash-heredoc**; пуш — `git push --follow-tags`. **Большие правки roadmap — python по маркерам с бэкапом в scratchpad.** **В батарее селфтестов НЕ глотать вывод** (`out=$(python "$f" 2>&1)`, печатать при провале): разовый «FAIL» selftest_export был невоспроизводимой гонкой первого прогона (Windows AV/индексатор на свежем большом HTML), вывод потерян из-за `>/dev/null`. `BASE-PROMPT.md` — gitignored. Не-Claude-Code-среды — Этап 22.
