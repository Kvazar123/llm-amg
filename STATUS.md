# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** Этап 10 (упаковка и установка; закрытие = **v1.0.0**) — **В РАБОТЕ**. Группа 1 (параметризация среды + игнор) выполнена; следующая — Группа 2 (установщик `install.py`), затем Группа 3 (доки + закрытие).
- **Развилки, согласованные с пользователем (эта сессия):**
  - **Игнор по намерениям** — **параллельные ключи** `mirror_exclude` / `absorb_exclude` (+ глобальный `exclude`), не вложенный словарь.
  - **`.gitignore`** — **явный источник важнее:** если корень `mirror_path`/`absorb_path` сам подпадает под `.gitignore`, он всё равно индексируется (намеренный opt-in); мусор внутри широких источников (`mirror_path: .`) `.gitignore` фильтрует. Флаг `respect_gitignore` (умолчание `true`; `false` = игнор полностью из конфига, без git). `DEFAULT_IGNORE_DIRS` остаётся жёстким и НЕ отдаётся под «явный источник важнее» (защита от самоиндексации графа).
  - **Установщик `install.py`** — **неинтерактивный**, опирается на конфиг/флаги; вопросы пользователю задаёт **МОДЕЛЬ** в диалоге (в агенте `input()` не работает).
- **Группа 1 — выполнено (код + 12 селфтестов зелёные, добавлен `selftest_ignore.py`):**
  - `extract_structure.py`: `_excludes_for_policy` (global+per-intent, аддитивно; `_norm_globs` срезает хвостовой `/` — фикс и для существующего `exclude`); `respect_gitignore` (умолчание true); `_gitignore_for_source` (явный корень-источник обходит `.gitignore`, мусор глубже — фильтруется); `_effective_ignore_dirs` (+`.agents` в базовый набор, + настроенный agent_dir от `amg_root.parent.name` — нет самоиндексации и для custom-имён); `iter_source_files(…, ignore_dirs)` новый параметр; per-source `by_source` в `--stats` (`{policy, found, files}` — молчаливый промах игнора виден, перекликается с 1.29/1.30); `main` дефолт amg_root через `resolve_amg_root` (lazy import).
  - **Задача 11 (найденный дефект, выполнено):** `graph_store.main` → `resolve_amg_root(start=cwd)` (+`--root`, `AMG_ROOT` как override; мёртвый `_default_root` удалён); `retrieve._default_store` и `inspect_graph` — восходящий поиск от cwd. Эффект: **глобальный движок лечит/читает ЛОКАЛЬНЫЙ граф проекта**. CLI-смоук пройден.
  - `config.yml`: блок «What gets ignored» (3 слоя) + ключи `respect_gitignore`/`mirror_exclude`/`absorb_exclude`.
  - Roadmap: добавлены задачи **10** (игнор) и **11** (резолв корня), вопрос про игнор — в задачу 3; пометки «Группа 1 выполнено» к 10/11.
- **Группа 2 (следующая) — установщик `install.py` (растёт из `sync_testbed.py`):** копировать движок (`skills/`+`agents/`); рендерить шаблоны `entrypoint/CLAUDE.md`/`settings.json`/`commands/amg.md` по `agent_dir`/`entrypoint` (литерал `.claude/` → `<agent_dir>/`, `@.claude/amg/digest.md` → `@<agent_dir>/…`); влить блок в точку входа между `<!-- AMG:BEGIN/END -->` (reinstall заменяет только блок); **слить** `settings.json` (не затереть); засеять `digest.md`; писать `config.yml` (local всегда; global → ещё и глобальный; мерж глобальный→локальный по ключам); local/global (**глобальный блок несёт абсолютный путь к движку `~/<agent_dir>/skills/…`, граф всегда локален**); reinstall/uninstall; ставить deps + `requirements.txt`/группы (base/embeddings/text-extraction/tree-sitter); прогон `verify --repair`+`bootstrap`; вопрос установщика про игнор пишет `exclude`. Среды без хуков/команд — петля + словесные триггеры. Соседи: `sync_testbed.py`, `entrypoint/*`, `09-config.md`.
- **Группа 3 (доки, закрывающая):** INSTALL.md (ручная+авто, reinstall/uninstall, local/global, ключи); сквозной проход 2.15 (`.claude`/`CLAUDE.md` — умолчания); `09-config.md` (новые ключи игнора; `agent_dir`/`entrypoint` стали реальны); `entrypoint/CLAUDE.md`→шаблон; THEORY §11 (per-intent игнор = «пользователь добавляет своё») / GUIDE. **Запрос пользователя:** в `README_RU.md` + зеркальном `README.md` (носительский технический английский, без калек) пересмотреть на полноту **«Быстрый старт»** (переделать на команды `/amg` Этапа 8 вместо ручных скриптов) и **«Установка»** (дополнить + описать основные ключи конфига); **GUIDE дописать/переписать**; архитектуру — тоже. Стиль — CLAUDE.md §7 + §3.4. Затем сворачивание roadmap (этап + задачи 10/11), VERSION→`1.0.0`, CHANGELOG, аннотированный тег `v1.0.0`, таблица BASE-PROMPT для Этапа 11.
- **Открытые вопросы / среда:** 12 селфтестов зелёные. `mypy --strict` — Этап 12 (1.31). Известная граница: источник, чьё имя совпадает с `DEFAULT_IGNORE_DIRS` (`build`/`out`/`env`/…), пока не индексируется даже при явном указании (DEFAULT — жёсткий слой; задокументировать; возможный override DEFAULT — будущее).
