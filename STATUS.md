# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** Этап 12 — «производительность и масштабирование» — **В РАБОТЕ** (Группа 1 начата). Этап 11 закрыт ранее (v1.2.0, запушен). Следующий после 12 — Этап 13 (provenance/verification).
- **Решения по развилкам этапа (зафиксированы с пользователем):**
  - **Индекс (задачи 1–3):** строить SQLite-индекс СРАЗУ (не ждать бенчмарков). Жизненный цикл — эжер-обновление писателями + защитная валидация в читателе: `retrieve.load_nodes` сверяет дешёвую сигнатуру `nodes/` (stat путь→mtime+size), при несовпадении/порче — скан + пересборка, скан-fallback безусловен (индекс никогда не source of truth; «удалить и пересобрать» автоматизировано). Кладётся в `cache/index.sqlite`. BM25 оставить Python-овским поверх хранимого текста (не FTS5 `bm25()`), чтобы не сдвинуть eval.
  - **Задача 7 (lazy-деривация):** строительные блоки сейчас (структурный-граф-первым уже есть; scoping даёт `partition_queue.py`; токен-выигрыш — задача 13), скрытый переключатель `derivation` НЕ вводить (спекулятивно без индекса+eval). Полная on-demand/фоновая лень — **детально расписана в roadmap §4.10**, дом реализации — Этап 17 задача 7. Сделано в этой сессии.
  - **Задача 8 (тиринг):** per-role дефолты + `reasoning_effort` + задокументированный протокол замера; живой (платный) замер отложить (как Хебб/Этап 19).
- **Сделано в этой сессии:**
  - **§4.10 roadmap** — полный план доводки ленивой деривации (честный разбор «лень стоит качества до первого касания», предпосылки, строительные блоки Этапа 12, фазы A–D, предохранители, DoD); задача 7 Этапа 12 переформулирована (блоки сейчас → §4.10), добавлена задача 7 в Этап 17 + строка DoD.
  - **Задача 14 (транзакционный `log.md`, §1.15) — ВЫПОЛНЕНА.** `GraphStore.append_log(source, msg, txid)` (`graph_store.py`): транзакц. запись (весь лог — один блоб, идемпотентно на replay), дедуп по `txid`, ротация (перелив в `archive/log-<время>.md`, хвост в живом). `consolidate._log` перенаправлен; `reconcile` пишет итог `bootstrap`/`apply` под гейтом `if txid` (неизменный прогон не логирует — идемпотентность). Источник в строке различим (`lifecycle._last_consolidation` фильтрует по `" consolidate |"` — `/amg status` не сломан). Доки `03-storage` («Журнал действий — транзакционный аудит-след») и `consistency-model §11` + строка раскладки синхронизированы. Regression — `selftest_graph_store.case_action_log`. Сквозная проверка: bootstrap пишет `reconcile`-строку, повтор не задваивает.
- **Осталось в Группе 1:** задача 12 (`mypy --strict` единым проходом — поставить `types-PyYAML`), задача 4 (подтвердить embed-кэш хеш-валидацию — уже есть в `embed.node_embeddings`, добавить тест), задача 10 (барьер — уже в `entrypoint/CLAUDE.md`/`SKILL.md`, выверить), задача 9 (каркас бенчмарков + масштабировать `../amg-bigtest/gen_big.py`). Дальше Группы 2 (индекс), 3 (near-dup §1.27 / resume §13 / хелперы 5–6 / lazy-блоки 7), 4 (тиринг 8).
- **К синхронизации при ЗАКРЫТИИ этапа (зафиксировать, не потерять):**
  - остаточные упоминания «best-effort log» вне load-bearing доков (уже синхронизированных): `02-data-model` (диаграмма раскладки + строка таблицы `log.md`), `07-consolidation §93` (предложение про `log.md`), `THEORY §9` (мельком «best-effort») — свести при doc-sync закрытия;
  - **дрейф `episodic_types`:** `07-consolidation` §«План» и таблица (стр. ~57/149) пишут `[section, note]`, а код/`09-config`/`config.yml` — `[section, note, open_question, plan]`; поправить при правке `07`.
- **Среда/процедуры:** база **13/13 селфтестов + `selftest_install.py` зелёные**. Python 3.14, mypy 2.1.0 есть, **`types-PyYAML` НЕ установлен** (нужен для задачи 12), sqlite3 3.50.4. **Коммитить через Bash-heredoc** (PowerShell 5.1 ломает многострочный `git commit -m @'…'@`). Работа Группы 1 пока **НЕ закоммичена** (коммит на границе группы по конвенции пользователя). Не-Claude-Code-среды (Codex/Qwen) — Этап 19.
