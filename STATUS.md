# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** ПОДЭТАП между Этапами 10 и 11 — «структурный `models` (аудит 1.14) + env (Codex) + переносимость промптов (1.32)». Этап 10 закрыт (v1.0.0). **Сессии A, B, C ВЫПОЛНЕНЫ** (код + доки + READMEs + `12-install.md` + сворачивание §1 + `VERSION` 1.1.0 + `CHANGELOG`). **Осталось только: коммит + аннотированный тег `v1.1.0` + пуш** — по явному «пушь» пользователя (необратимое наружу). Этап 11 (ingest) — следующий. Есть Этап 19 (тест не-Claude-Code-сред).
- **Подэтап models/env — решения и ресёрч записаны в roadmap §1.14 + §1.32 (авторитетный источник; память `models-1.14-and-env-research` — лёгкий мостик).** Веб-ресёрч ЗАВЕРШЁН (проверен по первоисточникам), решения СОГЛАСОВАНЫ. Ключевое: ключ **`reasoning_effort`** (не `thinking`; `minimal|low|medium|high|xhigh|max`, кламп по среде, без `off`; незадан→опустить); `model:` — непрозрачный пас-сквозь (алиас/`claude-opus-4-8`/`gpt-5.5`); мульти-провайдер = уровень развёртывания (gateway/Bedrock/Vertex), AMG не маршрутизирует; баг апстрима **[#44385](https://github.com/anthropics/claude-code/issues/44385)** (frontmatter `model:` игнорируется без явного параметра спавна; `effort` работает) → рендерим frontmatter + документируем обход; **Codex НЕ skill-less** (скиллы `.agents/skills`, субагенты TOML `.codex/agents`, конфиг `~/.codex/config.toml`) → строим **3-й режим `--env codex`** (детальные тесты на живом Codex — Этап 19), `generic` skill-less СОХРАНЯЕТСЯ. ВСЕ открытые §1 привязаны (1.15→12, 1.16→7, 1.27→12, 1.28→7, 1.29→11, 1.30→10+11, 1.31→12, 1.32→подэтап).
- **Сессия A (ВЫПОЛНЕНО):** структурный парсер `models` (плоская строка ∪ `{model, reasoning_effort}`) + карта ролей→агенты + рендер `model`/`effort` во frontmatter `agents/amg-*.md` из `config.yml` при install/reinstall (Claude Code), кламп `reasoning_effort` по среде. Код: `install.py` (`render_agent_models`/`_resolve_role`/`_clamp_effort`/`_set_agent_field`/`_read_models` + вызов после `write_config`, гейт `claude-code` и `not project_only`); `config.yml` (комментарий блока `models` + пример структурной формы); `selftest_install.py` (`test_models_render`: дефолтная плоская форма + структурная с клампом `minimal`→`low` + сохранность при reinstall). **13 селфтестов зелёные.**
- **Сессия B (ВЫПОЛНЕНО):** (1) фиксы §1.32 — `place_engine` РЕНДЕРИТ промпты движка (`SKILL.md` + `agents/*.md`) через `render_control_text`, а не копирует дословно (источники остаются с `.claude` как плейсхолдером); `resolve_amg_root` пробует `.claude`+`.agents` (`graph_store`+`retrieve`); `eval_gate.cases` переносим (`consolidate` дефолт от корня хранилища + рендер значения в `write_config`). (2) `--env codex` — 3-way классификатор `_env_kind` (claude-code/codex/generic); codex: скиллы→`.agents/skills`, субагенты→TOML `.codex/agents` (`render_codex_agents`: name/description/developer_instructions + `model`+`model_reasoning_effort` из `models`, кламп по среде, Claude-алиасы для codex опускаются, body+description рендерятся в `.agents`), skill-aware блок `entrypoint/AGENTS.codex.md`; `generic` skill-less СОХРАНЁН; reinstall/uninstall env-aware (чистит `.codex/agents/amg-*.toml`); `agent_dir`/`entrypoint` по умолчанию по среде (codex/generic→`.agents`/`AGENTS.md`). Тесты: `test_codex_env` (новый), `test_generic_env`→`--env generic`, `test_agents_env`+рендер промптов/eval_cases, `selftest_reconcile` root +`.agents`. **13 селфтестов зелёные; Codex-TOML парсится `tomllib` начисто.**
- **Сессия C (ВЫПОЛНЕНО):** доки синхронизированы — `09-config.md` (блок `models` рабочий: структурная форма, `reasoning_effort`, кламп, баг #44385; planned-строка убрана), `08-agents-skills.md` (3 env-режима + рендер промптов 1.32), `GUIDE.md` («Выбор модели» → `reasoning_effort`; раздел «Установка» переписан в ПОЛНУЮ форму — не «кратко»; оговорка про среды), `03-storage.md` (пробел резолва `.claude`+`.agents`); **новый `docs/ru/architecture/12-install.md`** (архитектура установщика) + в карте `architecture/README.md`. **READMEs RU + EN:** раздел «Установка» переработан (deps → способы [модель/команда] → ключи конфига кодом → «Другие среды (`--env`)»), Codex НЕ skill-less + `.codex`, блок переносимости перенесён из «Режимы» в «Установку», добавлена строка «что игнорируется» (ключ `exclude` НЕ устарел — глобальный, +mirror/absorb_exclude). **§1 свёрнут** (1.14/1.16/1.28/1.32 в одну строку) и ВСЕ открытые привязаны: 1.15→12 (задача 14 добавлена), 1.27/1.31→12, **1.29/1.30→11 (задачи 10/11 добавлены)**. Этап 12 задача 8 помечена «механизм 1.14 готов». `BASE-PROMPT.md` строка Этапа 11 дополнена. `VERSION`→1.1.0, `CHANGELOG` v1.1.0. **13 селфтестов зелёные, Codex-TOML парсится.**
- **Что сделано на Этапе 10 (4 группы + релиз):**
  - **Группа 1** (`a3516fc`): игнор по намерениям (`mirror_exclude`/`absorb_exclude`, `respect_gitignore`, explicit-source-beats-gitignore, `_effective_ignore_dirs` +`.agents`/agent_dir, per-source `--stats`) + портативный резолв корня (`graph_store`/`retrieve`/`inspect_graph` через `resolve_amg_root`). `selftest_ignore.py`.
  - **Группа 2** (`14f4eee`): установщик `install.py` — local/global, рендер шаблонов, блок между маркерами, мерж `settings.json`, `config.yml` из шаблона, deps, `verify` без авто-build; `--build`/`--project-only`/`--uninstall [--scope global] [--purge-graph]`; копирует только `amg-*`. `requirements.txt`. `selftest_install.py`.
  - **Группа 3** (`9e81fcc` docs + релиз `0db3af7`): доки синхронизированы (INSTALL переписан; README*/GUIDE «Быстрый старт»; `/amg on` ≠ build; 04/09/08/03/06/THEORY §11; сквозной 2.15); roadmap свёрнут; VERSION→`1.0.0`; CHANGELOG.
  - **Группа 4 — env-адаптация** (`00433fb`): флаг `install.py --env` — `claude-code` (умолчание, **байт-в-байт прежнее**, проверено) vs `generic` (любая AGENTS.md-среда: Codex, Qwen Coder и др.) → отдельный **блок без скиллов** из `entrypoint/AGENTS.md` (петля прямыми вызовами скриптов, `agents/*.md` как инструкция, дайджест читается, исцеление/консолидация — дисциплиной петли), хуки/команду не пишет. `selftest_install.py` → `test_generic_env` (9 кейсов, 13/13 зелёные). Доки: README* (заметная оговорка ВВЕРХУ заглавными «ПРОВЕРЕНО ТОЛЬКО В CLAUDE CODE» + блок переносимости), INSTALL («Другие среды» + `--env`), GUIDE, 08 (полное описание петли без скиллов), 09. Термин «бесскилловый» → «без скиллов» (по фидбеку). В `AGENTS.md`-блоке дев-ссылок (roadmap/этапы) НЕТ — он поставляется в проект (правило 2.16).
- **Ключевые решения Этапа 10:** per-intent ignore = параллельные ключи; явный источник важнее `.gitignore`; `respect_gitignore`; `/amg on` ≠ build; установщик неинтерактивный (модель ведёт Q&A), не индексирует молча, опц. `--build`; `place_engine` только `amg-*`; global-блок — абсолютный путь к движку, `@digest`→заметка, граф локален; **`--env` адаптирует механизм под среду** (Claude Code vs универсальный AGENTS.md-блок без скиллов).
- **Релиз отправлен:** 5 коммитов Этапа 10 (`a3516fc`/`14f4eee`/`9e81fcc`/`0db3af7`/`00433fb`) и тег `v1.0.0` — в `origin/main`. Таблица загрузки **Этапа 11** в `BASE-PROMPT.md` обновлена; старт Этапа 11 — со сверки её с фактическим состоянием repo.
- **Открытые вопросы / среда:** 13 селфтестов зелёные. **Не-Claude-Code-среды (Codex, Qwen Coder и др.) НЕ протестированы — стабильность не гарантирована; проверка = Этап 19.** `mypy --strict` — Этап 12. Этап 18 дополнен задачами: нейтрализация `.claude/*` в доках (гибрид) + применение §7-чеклиста (особенно п.7 «регистр прозы») ко всей документации — до перевода. Границы: имя источника из `DEFAULT_IGNORE_DIRS` не индексируется даже явно (04-ingest); global `@digest`-импорт пер-проектно не резолвится (заметка). PowerShell 5.1 ломает многострочный `git commit -m @'…'@` — коммитить через Bash-heredoc.
