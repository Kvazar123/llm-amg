# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** 0 — стабилизация ядра сверки (задачи 1–4 выполнены)
- **Сделано в последней сессии:** задачи 1–2 Этапа 0 в `reconcile.py`: stale requeue (условие `derived_from_hash != content_sha` ИЛИ `status: stale`; очередь пересоздаётся из состояния графа каждым `plan()` — самовосстановление после сбоя; счётчик `requeued_stale`); `lineno`/`qualname` во frontmatter при created/changed; `qualname`/`lineno`/`lang` в queue item; сверх буквы задачи — тихое обновление указателя при сдвиге единицы без смены хеша (счётчик `pointer_refreshed`); решение по `lang`: поле узла остаётся языком сводки, язык источника — только в очереди (обоснование — пометка к 2.3 п. 2 roadmap); вход `amg-builder.md` синхронизирован с полями очереди (пометка к 2.9 п. 2); новый `selftest_reconcile.py` (6 сценариев); все 5 селфтестов зелёные (в `selftest_stage2.py` прежние SKIP pdf/docx/xlsx — опциональные библиотеки не установлены). Релиз v0.1.0: добавлены VERSION/CHANGELOG, политика версионирования (CLAUDE.md §10, roadmap §5), аннотированный тег `v0.1.0` запушен
- **Сделано в той же сессии (задачи 3–4):** пересборка structural-рёбер на `changed` (`_refresh_structural_edges`: заменяются `origin: structural` и legacy-`imports`/`calls` без origin; пережившее ребро наследует `w`/`coact`; смысловые рёбра не трогаются); на `changed` обновляются также `policy`/`source_path`, дрейф policy без смены контента подхватывается drift-веткой; поле `origin` рёбер штампуется на всех местах записи (извлечение → `structural`, apply-обновления → `semantic`, хабы → `synthesized`, `summarize_episodes` → `consolidation`); `lang` узла на `changed` намеренно не трогается (самокоррекция через передеривацию, пометка в 1.3); в Этап 1 задачу 7 добавлена разметка origin для legacy-рёбер; +3 сценария в `selftest_reconcile.py` (всего 9), reconcile/graph_store/consolidate зелёные
- **Следующий шаг:** Этап 0, задачи 5–6 (нормализовать `source_kind`; исправить `apply_derivation`: merge `part_of`, перевод в `active` только при новой summary, несколько items на узел)
- **Открытые вопросы / блокеры:** мелкое расхождение — `STATUS.md` есть в карте репозитория CLAUDE.md §2, но отсутствует в каноническом списке 4.9 (свернуть при ближайшей правке 4.9)
- **Песочница `../amg-testbed`:** не создана (существует пустой каталог; наполнение — Этап 6)
- **Заметки для следующей сессии:** —
