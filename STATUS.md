# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап:** Этап 12 — «производительность и масштабирование» — **В РАБОТЕ**. **Группы 1–2 закрыты** (1: задачи 12 mypy, 14 log, 4 embed-cache, 9 bench, 10 барьер; 2: индекс под `load_nodes`). **Группы 3–4 не начаты.** НЕ создавать отдельный подэтап перед 13: остаток — это незакрытый Этап 12, он весь ниже.
- **Готово в этой сессии:**
  - **Группа 1 хвост:** `bench.py`+`selftest_bench.py` (задача 9); барьер «Boundaries» в 3 блоках + 08 (задача 10, реестр 2.16 п.6 закрыт); задача 4 подтверждена. Коммиты `291b082`, `a44f21a`.
  - **Группа 2 — ИНДЕКС (задачи 1–2):** новый `skills/amg-retrieve/scripts/index_store.py` + `selftest_index.py` (5 проверок). `cache/index.sqlite` под `retrieve.load_nodes`: хранит `id`/`type`/`status`/`summary`/`source_path`/`lineno`/`text`/`edges`(json)/`part_of`(json)/`body`; читатель сверяет сигнатуру (stat-обход `nodes/`, хранится в мета-таблице sqlite) — свежо→читать индекс, иначе безусловный скан+пересборка (сигнатура ДО скана — гонко-безопасно); эжер-инкрементальный `refresh_after_commit` у ВСЕХ 6 писателей через новый `graph_store.Transaction.node_paths()`. Форма `load_nodes` идентична (общий `_node_from_meta`; BM25/adjacency/assemble не тронуты). Bench-индекс-замеры дописаны. **Измерено: 7600 узлов load 7.9с→0.52с (15×)**; eval-демо hop-recall 1.00; 16 селфтестов; mypy 13 модулей. Коммит — этой сессией (см. git log).
- **Уточнения по развилкам (изучением/измерением) — отступления от STATUS, зафиксировать:**
  - **Сигнатура индекса — ВНУТРИ sqlite** (мета-таблица), не отдельный `index.meta`: атомарность, нет рассинхрона.
  - **Тело узла ХРАНИТСЯ** в индексе (тела почти всегда пустые; форма `load_nodes` идентична, `assemble_pack` не тронут).
  - **Задача 3 (adjacency-кэш) ОТЛОЖЕНА по измерению:** `build_adjacency` 23–49 мс vs `load_nodes` 4–8 с (<1%); сериализация adjacency-блоба рискует быть net-negative. Индекс уже убрал доминирующую стоимость. Вернуть только если замер на 10⁴⁺ опровергнет (тогда бинарный формат). **Согласовать с пользователем при желании.**
- **Решения по развилкам (ранее зафиксированы):**
  - **Задача 7:** скрытый переключатель `derivation` НЕ вводим; блоки дают 5/6/13; полная лень → §4.10 / Этап 17.
  - **Задача 8:** per-role дефолты + `reasoning_effort`; живой замер отложить.
  - **mypy селфтесты:** ИСКЛЮЧЕНЫ из гейта. Гейт: `python -m mypy` без аргументов → 13 модулей движка.
- **ОСТАЛОСЬ в Этапе 12:**
  - **Группа 3:** задача 11 (`consolidate.make_plan`: Жаккар сузить с ВСЕХ пар до `episodic_types` — `ids`/`toks` по эпизодическим, §1.27, ~2 строки + регресс в `selftest_consolidate`); задача 13 (resume: `agents/amg-builder.md` эхоит `content_sha` в derived-элемент [сейчас `{id,summary,lang,edges}`], `reconcile.apply_derivation` сверяет `content_sha`↔текущий `source_hash`, skip протухшего `skipped_stale`; SKILL bootstrap «применить уцелевшие `derived-*.json` ПЕРВЫМИ»); задачи 5–6 (`partition_queue.py`/`inspect_queue.py` в `amg-bootstrap/scripts`, замена ad-hoc нарезки очереди в SKILL шаг 3); задача 7 (блоки — см. выше).
  - **Группа 4:** задача 8 — в `config.yml` блок `models` добавить структурную форму с `reasoning_effort` (низкий discovery, высокий synthesis) + задокументировать протокол замера; живой замер отложить.
- **К ЗАКРЫТИЮ этапа (doc-sync + сворачивание + релиз v1.3.0):** синхр. `03`/`06`/`09`/`10` + THEORY/GUIDE/READMEs под индекс/задачи; **индекс в раскладку** (`02-data-model` диаграмма+таблица: `cache/index.sqlite` disposable; `03-storage` раздел про индекс; `consistency-model §4` layout; `06-retrieval` про `load_nodes`-индекс); индекс **без конфиг-ключа** (полностью автоматический/disposable — отметить в 09); **adjacency-кэш отложен** — отразить в §4.1/06; **дрейф `episodic_types`** в `07-consolidation` §«План» (~стр. 57) и таблице (~стр. 149): `[section, note]` → `[section, note, open_question, plan]`; строка Этапа 13 в `BASE-PROMPT.md`; свернуть §1.15/1.27/1.31 раздела 1 и тело §5 Этапа 12; VERSION/CHANGELOG/тег `v1.3.0`.
- **Среда/процедуры:** **torch/эмбеддинги РАБОТАЮТ**. Гейт типов: `python -m mypy` (без аргументов; предупреждение `[mypy-selftest_*]` — давнее, безвредно, exit 0). **16 прогонов селфтестов зелёные** (15 движка + `selftest_install.py`), живой CLI-цикл чист, `eval --make-demo` hop-recall 1.00. **Коммитить через Bash-heredoc** (PowerShell 5.1 ломает многострочный commit). В фикстурах/бенче `embeddings: off` (иначе грузит модель). Не-Claude-Code-среды — Этап 19.
