# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** Этап 11 — «расширение ingest». **КОД ВЫПОЛНЕН** (группы A–E закоммичены, 13 селфтестов зелёные). Осталась **закрывающая сессия**: синхронизация документации (THEORY/GUIDE/architecture/READMEs + пункты §2), DoD, сворачивание (§5 Этапа 11 + §1.29/1.30), выпуск (VERSION→1.2.0 + CHANGELOG + аннотированный тег + пуш) — по протоколу §4.6, по сигналу «пушь» пользователя. Этап 10 закрыт (v1.0.0), подэтап models/env закрыт (v1.1.0, **уже запушен** — прежняя пометка «осталось пушнуть» устарела). Следующий — Этап 12 (производительность); есть Этап 19 (тест не-Claude-Code-сред).
- **Сделано на Этапе 11 (5 групп, все в `origin`-ветке `main` локально, НЕ запушены):**
  - **A** (`ebe0854`) — структурные нарезатели: `_ndjson_units` (запись/строку), `_csv_units` (один структурный `sheet`-юнит, как XLSX), `_log_units` (эпизоды-окна `log_group_lines` по timestamp), `_rst_units` (подчёркивания/надчёркивания RST → `section`), `_pptx_units` (слайд → `section`, опц. `python-pptx`, мягкий пропуск). `.ndjson/.csv/.tsv/.log/.rst` уведены из запасных путей; `.pptx` выведен из `BINARY_EXT`. Новые нарезатели маппятся на КАНОНИЧЕСКИЕ типы (`record/sheet/block/section`) — потребители (`retrieve.TIER_OF_TYPE`, `consolidate.episodic_types`) не трогаются. `selftest_chunkers.py` (новый) + PPTX в `selftest_stage2.py`.
  - **B** (`830d8e7`) — recursive JSON: `_data_units` дробит крупный вложенный контейнер по пути ключей (`_json_descend`/`_json_qual`/`_has_child_container`), мелкое/плоское — прежняя одна запись и ХЕШ (обратная совместимость), плоский список скаляров не взрывается. Ключи `json_max_depth`/`json_recurse_min_chars`/`json_max_nodes`/`log_group_lines`.
  - **C** (`9132dcc`) — chat chunker: `_chat_units` (JSON-массив/`{messages}`/NDJSON; синонимы ключей; content-блоки → вложения) → `section`/сообщение, роль/время/тред в тексте, стабильный id из поля сообщения, слабое ребро **`follows`** к предыдущему ходу того же треда. Детект над json/ndjson; плоский маркерный дамп (`=== Human ===`) во внешнем источнике → `_session_units` (`_has_role_markers`). `reconcile._structural_edges` эмитит `follows` (origin structural, w 0.3); `retrieve`/`config` — β `follows` 0.4.
  - **D** (`f34d566`) — гигиена источников: `detect_policy_conflicts` (1.29: id под >1 политикой → `policy_conflicts` в `plan`, `overlapping_sources`+hint в `--stats`); `plan` выводит `missing_sources` (1.30). Приоритет на пересечении: `absorb_once` > `absorb` > `mirror` (последний в `resolve_sources` выигрывает дедуп).
  - **E** (`196c37d`) — `absorb_once`: новая политика (ключ `absorb_once_path`) — впитать один раз и заморозить (изменения игнорируются, удаление не вычищает). Гейт в `reconcile.plan` (счётчик `frozen`); `resolve_sources` чинит гейт для конфигов только с `absorb_once_path`.
- **Semantic drift (задача 2) — ОТЛОЖЕНА** по согласованию: не реализуется на Этапе 11, не блокирует DoD. Развёрнутая спецификация + условия включения (измеренная боль, последним, под eval-гейтом) — в roadmap **§4.6** (дополнено), задача 2 §5 помечена отложенной.
- **Ключевые решения этапа:** новые нарезатели → канонические типы (нулевой риск для потребителей); CSV/PPTX несут `text` в юните (сборщик не открывает файл повторно, как PDF/DOCX/XLSX); chat-смежность — ОДНО ребро `follows` (build_adjacency симметризует); вариант с новыми полями схемы (timestamp/thread) НЕ выбран — метаданные в тексте (без инертной схемы); `absorb_once` — полная инертность после первого ингеста; приоритет пересечения = наиболее сохраняющая политика.
- **Долг закрывающей сессии (синхронизировать док, влить эти построчные пометки):**
  - `04-ingest.md` (2.5, ОСНОВНОЙ) — описать нарезатели ndjson/csv/log/rst/pptx + recursive JSON + chat (`_chat_units`/`follows`/плоский дамп→session); раздел «Развитие» — снять reализованные заглушки (RST/NDJSON/CSV/log/PPTX/recursive-JSON), оставить semantic-drift (привязка §4.6); реестр `CHUNKERS` пополнить.
  - `05-reconcile.md` (2.6) — кейс `frozen` (absorb_once); `follows` как структурное ребро; диагностика `policy_conflicts`/`missing_sources` в `plan`; правило «mirror и absorb при изменении одинаковы» дополнить третьей политикой absorb_once (freeze меняет это для неё).
  - `09-config.md` (2.10) — новые ключи `json_max_depth`/`json_recurse_min_chars`/`json_max_nodes`/`log_group_lines`, `absorb_once_path`; β `follows` 0.4 в списке relation_priors; перенести `absorb_once` из «Планируется» в активные.
  - `02-data-model.md` — ребро `follows` (origin structural); подтвердить, что chat/log/csv НЕ вводят новых полей схемы (метаданные в тексте); политика `absorb_once`.
  - `THEORY.md` — §11 (новые домены/нарезатели), §12 (absorb_once = «впитать и заморозить», третья точка на оси mirror↔absorb), §13 (chat-смежность `follows` как эпизодический скелет диалога).
  - `GUIDE.md` — как подавать чат-экспорты/логи/CSV/RST/PPTX; recursive-JSON тюнинги; `absorb_once_path` (приём «снимок, который не пересинхронизируется»); пересечение источников и диагностика путей.
  - **READMEs** (RU + зеркально EN) — коротко: расширенные форматы входа (чат-экспорты, логи, CSV, RST, PPTX, глубокий JSON), `absorb_once`.
  - `BASE-PROMPT.md` — строку Этапа 12 сверить с фактическим repo (появились `_chat_units`/`follows`/`absorb_once`/`detect_policy_conflicts`), дополнить.
  - Версия: при закрытии **v1.2.0** (новые ключи конфига + политика `absorb_once` + ребро `follows` = аддитивный MINOR-контракт).
- **Открытые вопросы / среда:** 13 селфтестов зелёные. `python-pptx` на этой машине не импортируется (сломан `_imaging` Pillow на Python 3.14) → PPTX-ветка корректно SKIP/мягкий пропуск (тот же класс, что torch в прошлых сессиях). `mypy --strict` — Этап 12. **Коммитить через Bash-heredoc** (PowerShell 5.1 ломает многострочный `git commit -m @'…'@`). Не-Claude-Code-среды — Этап 19.
