# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** Этап 10 (упаковка и установка) **ЗАКРЫТ** (v1.0.0, 2026-06-16). Релиз-коммит сделан; **аннотированный тег `v1.0.0` и пуш ждут «ок» пользователя** (это шаг наружу). Следующий — **Этап 11 (расширение ingest)**, не начат.
- **Что сделано на Этапе 10 (3 группы + релиз):**
  - **Группа 1** (`a3516fc`): игнор по намерениям (`mirror_exclude`/`absorb_exclude`, `respect_gitignore`, explicit-source-beats-gitignore, `_effective_ignore_dirs` +`.agents`/agent_dir, per-source `--stats`) + портативный резолв корня (`graph_store.main`/`retrieve`/`inspect_graph` через `resolve_amg_root` → глобальный движок лечит локальный граф). `selftest_ignore.py`.
  - **Группа 2** (`14f4eee`): установщик `install.py` (преемник `sync_testbed.py`) — local/global, рендер шаблонов по `agent_dir`/`entrypoint`, блок между маркерами, мерж `settings.json`, `config.yml` из шаблона, deps, `verify` без авто-build; `--build`/`--project-only`/`--uninstall [--scope global] [--purge-graph]`; копирует только `amg-*` (чужие скиллы целы). `requirements.txt`. `selftest_install.py`.
  - **Группа 3** (`9e81fcc` docs + релиз-коммит): доки синхронизированы (INSTALL переписан под установщик; README*/GUIDE «Быстрый старт» → установка→`/amg status`→просто спрашивайте; `/amg on` ≠ build; `04-ingest`/`09-config`/`08`/`03`/`06`/THEORY §11; сквозной 2.15 — оговорка «`.claude`/`CLAUDE.md` — умолчания Claude Code» во всех доках); roadmap свёрнут (Этап 10 → «Выполнено»; 2.13/2.15 закрыты, 2.16 п.5; Stage 6 ledger → done); VERSION→`1.0.0`; CHANGELOG (Added/Changed/Fixed).
- **Ключевые решения Этапа 10:** per-intent ignore = **параллельные ключи** (не вложенный словарь); **явный источник важнее `.gitignore`** (корень в gitignore → всё равно индексируется); `respect_gitignore` (false = из конфига, без git); **`/amg on` ≠ построение графа** (только флаг; строит петля/`/amg sync`); установщик неинтерактивный, **модель ведёт Q&A**, активацию не делает молча, опц. `--build`; `place_engine` трогает **только `amg-*`** (фикс: иначе снёс бы чужие скиллы в `~/.claude`); global-блок — абсолютный путь к движку, `@digest`-импорт → заметка (пер-проектно не резолвится), граф/конфиг локальны.
- **СЛЕДУЮЩЕЕ ДЕЙСТВИЕ (после «ок»):** `git tag -a v1.0.0 -m "v1.0.0 — stage 10 closed"` + `git push --tags` (и `git push`); затем сверить/обновить таблицу загрузки **Этапа 11** в `BASE-PROMPT.md` (корень, не в git).
- **Открытые вопросы / среда:** 13 селфтестов зелёные. `mypy --strict` — Этап 12 (1.31). Известные границы: (1) имя источника, совпадающее с `DEFAULT_IGNORE_DIRS` (`build`/`out`/`env`/…), не индексируется даже при явном указании — жёсткий слой (задокументировано в 04-ingest); (2) global `@digest`-импорт не резолвится пер-проектно — в global-блоке заменён заметкой, дайджест несёт петля/`/amg status`. Окончания строк LF/CRLF — git нормализует. PowerShell 5.1 ломает многострочный `git commit -m @'…'@` — коммитить через Bash-heredoc (`git commit -F -`).
