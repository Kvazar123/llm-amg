# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** Этап 6 (интеграция `amg-classifier`) — все задачи **1–7 выполнены**, этап **НЕ закрыт**. Следующее — **закрывающая сессия Этапа 6**.
- **Сделано в этой сессии (2026-06-15):**
  - **Группа A — overrides классификатора (задачи 1–5, коммит 736b034).** `extract_structure.py`: `load_overrides`/`_route_override`/`_classify_path` читают `work/classification-overrides.json` до fallback; override побеждает даже известное расширение; маршрутизация `code+python`→ast, `code+grammar`→treesitter, `data`→json, `doc`→paragraphs; missing/malformed → пустая карта. `--stats`: `resolved_by_override` + `ambiguous_files` (только неразрешённые) + `classifier_hint`. `reconcile.plan` пробрасывает `amg_root`. Workflow: `amg-bootstrap/SKILL.md` шаг 2 + `agents/amg-classifier.md`. Тест: `selftest_extract_overrides.py` (9 селфтестов зелёные).
  - **Группа B — песочница + sync (задача 6, коммит 8c0d351).** `sync_testbed.py` (корень репо): идемпотентно копирует `skills/`+`agents/` → `<testbed>/.claude/`, `entrypoint/CLAUDE.md` → `<testbed>/CLAUDE.md`; `notes.py` входит автоматически; граф/конфиг/источники не трогает. Песочница `../amg-testbed` (delivery-service: billing/jobs/notify/auth + docs mirror, data absorb, неоднозначный `src/run`). Цикл классификации воспроизведён синтезированным движком: `--stats` (ambiguous) → override `{code,bash}` → resolved → `bootstrap` (27 узлов, `src/run` в бакете code).
  - **Группа C — измерение Хебба (задача 7).** Граф песочницы деривирован **субагентами** `amg-builder`(sonnet)+`amg-synth`(opus) без подсказок про Хебб → 33 узла (27 + overview + 5 hub). 7 многошаговых cases (`cases.json`, gold за рёбрами). Журнал — 16 реалистичных запросов, накоплен естественно (max-пары = центральный hub/overview-костяк). Замер off vs on по 12 фолдам: `recall=0.4238, hop_recall=0.5833, pack_recall=1.0` — **идентично на всех фолдах**. Механизм работает (130/130 рёбер изменились, 82→1.0, 43 затухли; слабое ребро `charge_card→retry-policy` 0.5→1.0), но top-K не переупорядочился. Устойчиво к засеву: повтор с model2vec и ST `paraphrase-multilingual-MiniLM-L12-v2` (blend 0.5) — тот же плоский результат.
- **Ключевое решение Этапа 6 (держать в уме):** **`weights.apply_hebbian` остаётся `false`.** Измерено на реальном графе с реальным журналом: Хебб **не даёт выигрыша и не роняет** полноту (планка roadmap «on устойчиво лучше» не достигнута). Не ложноотрицательный: базлайн ненасыщен, метрики чувствительны (top-K, бюджет-независимы), веса доказуемо менялись, слабые рёбра усиливались. Причина — §8.1: реальный журнал на малом плотном графе равномерно заливает релевантный подграф к потолку весов (нет различающего сигнала). Эмбеддинги влияют на **seed**, Хебб — на **spread** → вердикт language/seed-независим. Оговорка: 33 узла малы/плотны; на большом разрежённом dogfood-графе пересмотреть (решение **подтверждает** дефолт, не закрывает навсегда). THEORY §8.1 это уже предсказывает.
- **Следующий шаг — закрывающая сессия Этапа 6 (CLAUDE.md §4.6, строго в порядке):** (1) синхронизировать доки уже реализованного: `04-ingest.md` — новый раздел про classifier-overrides рядом с «Классификация» (закрыть 2.5 п.4); `08-agents-skills.md` — петля «ambiguous→classifier→overrides→повтор» в § `amg-classifier`/§ `amg-bootstrap` (закрыть 2.9 п.3); свернуть 1.13 до «исправлено на Этапе 6». **Новая теория?** §8.1 уже описывает циркулярность — измерение её **подтверждает** (инструментальное обнажение, не новая теория): занести empirical-якорь (числа off/on, three-seed) одной фразой в §8.1 + строку GUIDE «веса по умолчанию не обновляются, дефолт подтверждён измерением». (2) DoD + 9 селфтестов. (3) Свернуть §5 Этапа 6 (тело → однострочник) + закрытые пункты. (4) Выпуск **v0.8.0** (VERSION, CHANGELOG, тег, пуш). (5) **BASE-PROMPT.md**: дописать строку загрузки Этапа 7 (единственный этап полной загрузки ВСЕЙ документации — THEORY/GUIDE/architecture 01–11 + код выборочно для сверки).
- **Открытые вопросы / среда:** sentence-transformers сегфолтит под нагрузкой (>~20 encode/процесс) даже после переустановки torch (Python 3.14) — ST-замеры вели снимками в свежих процессах (журнал/свёртка на BM25). Не блокер. mypy у пользователя — статус неизвестен (`--strict` отложен на Этап 12, п. 1.31).
- **Песочница `../amg-testbed`:** наполнена (33 узла, делив-сервис, субагентная деривация, `gap-report.md`, `cases.json` — 7 multi-hop), чистое off-состояние, конфиг `apply_hebbian:false`. Вне репо/гита; движок обновляется `sync_testbed.py`. Обслуживает этапы 8–10 (хуки, команды, сессии, установка).
