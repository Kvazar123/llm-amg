# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап: Этап 20 — задачи 1–6 ВЫПОЛНЕНЫ (сессия 1, 2026-07-04; коммиты `d116aef`/`b88642e`/`8218fca`/`0f13ace`) → следующая сессия — ЗАКРЫВАЮЩАЯ Этапа 20.** Порядок закрытия: (1) синхронизировать доки по списку «Закрытие (доки)» в теле этапа — `08-agents-skills` (текст-в-очереди/чекпоинты/тонкая оркестрация/сигнал/прогресс), `04-ingest`+`05-reconcile` (текст у всех нарезателей — снять «только бинарные»; авто-сводка тривиальных; реальные lineno у paragraphs §1.51), `09-config` (ключи `queue_text_max_chars`/`trivial_unit_max_lines`/блок `builder`; «код ≠ шаблон» у trivial: код 0 / шаблон 3), GUIDE («Качество и стоимость сборки» — дополнить устойчивостью/чекпоинтами/прогрессом), READMEs (кратко: сборка дешёвая и устойчивая к обрывам); THEORY скорее всего без правок (инженерная экономия, новой теории нет — проверить); (2) DoD+селфтесты; (3) свернуть §1.47/§1.48/§1.51 и тело этапа; (4) выпуск v1.11.0 + тег + пуш.
- **Что реализовано на Этапе 20 (действует дальше):** текст юнита прикрепляют ВСЕ нарезатели (модуль — целым файлом; дублирование модуль↔функции в батче принято осознанно), `_queue_item` кладёт его в очередь под `queue_text_max_chars` (дефолт-константа 20000; 0 = указатели) + `line_end`; тривиальные функции (диапазон ≤ `trivial_unit_max_lines`, guard `_SIGNIFICANT_DUNDERS`, кэш дериваций ПОБЕЖДАЕТ шаблон) получают детерминированную авто-сводку кодом одной строкой — счётчик `auto_summarized`, дефолт в коде 0 / в шаблоне 3 («код ≠ шаблон»); `partition_queue` капы `builder.batch_units` 40 / `batch_max_chars` 120000 (номинал юнита без текста 2000), части `queue-<part>-NN.json`, ре-запуск заменяет набор (кроме priority/deferred); чекпоинты БЕЗ кода движка — билдер пишет `derived-<батч>-pNN.json` частями, apply пофайловый; протокол `BATCH COMPLETE|PARTIAL: N/M` (+`SYNTH COMPLETE|PARTIAL`), SKILL сверяет счётчики apply с батчем; `inspect_queue` — блок `progress` (derived_percent по графу, сканом); SKILL раздел «Thin orchestration».
- **ВАЖНО (решение пользователя, действует):** живой замер ×20–30 по токенам/времени И связности Этапа 19 — совместная приёмка Этапов 19–20 на реальном dogfood-стенде (~1 МБ проект; путь у пользователя), НЕ в dev-сессии. Headless-DoD Этапа 20 выполнен.
- **Замечания пользователя (действуют всегда):** поставляемые артефакты (шаблон `config.yml`, промпты) не ссылаются на этапы roadmap; конвенцию для комментариев скриптов ужесточить проходом Этапа 21 (пункты (в)/(г) задачи о ссылках); хардкод весов/порогов — только осознанный, с обоснованием.
- **Хвост на Этап 21 (заведён пунктами):** ссылки на roadmap в управляющем слое (≈3 «Stage 13/14» в `skills/amg-retrieve/SKILL.md`; комментарии скриптов — пересмотр по (в)/(г)) + §1.36/§1.52-остаток (лог `.log` + абзац в 07-consolidation) + §1.33/§1.34/§1.35 (языковые проходы).
- **Два dogfood-пункта Этапа 17 по-прежнему отложены на живой проект ПОСЛЕ Этапов 18–20:** (1) реальная эмиссия паттернов живым `amg-synth`; (2) флип `apply_hebbian`→true на реальном `usage.log`. Оба — не в dev.
- **Стенды (НЕ удалять):** `../amg-bigtest`, `../amg-testbed`. Реальный dogfood-граф — совместный приёмочный кейс Этапов 19–20; путь у пользователя (не в репо).
- **Тесты:** 22 селфтеста движка (расширены `selftest_build` — тривиальные+кэш-гард+текст очереди, `selftest_queue` — капы+прогресс, `selftest_chunkers` — паритет хешей paragraphs) + `selftest_install` + mypy(18) + eval-демо pack-recall 1.00 — зелёные.
- **Краевой случай, найденный тестом (помнить):** файл из одной функции даёт модулю и функции ОДИН content_sha — кэш-гард тривиальных тогда шлёт функцию на restore (безвредно, но объясняет «лишний» юнит в очереди).
- **Среда/процедуры (durable):** torch/эмбеддинги РАБОТАЮТ, но ST-бэкенд нестабилен под тяжёлым пере-кодированием (изолировать в subprocess). Гейт типов: `python -m mypy` без аргументов → 18 модулей. **Коммитить через Bash-heredoc**; пуш — `git push --follow-tags`. **Большие правки roadmap — python по маркерам с бэкапом в scratchpad.** **В батарее селфтестов НЕ глотать вывод** (`out=$(python "$f" 2>&1)`, печатать при провале). `BASE-PROMPT.md` — gitignored. Не-Claude-Code-среды — Этап 22.
