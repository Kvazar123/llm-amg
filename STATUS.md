# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап:** между Этапом 17 (закрыт, v1.8.0) и **Этапом 18 (развёртывание и разрешение store)**. Последняя сессия — **полный аудит проекта перед Этапом 18** (код каждого модуля движка, все промпты, вся документация, сверка этапов 18–20 со стенограммой dogfood). Код НЕ менялся → селфтесты в состоянии v1.8.0 (21 зелёный + mypy(17)). Следующий шаг — Этап 18 по обновлённой строке таблицы загрузки BASE-PROMPT.
- **Итог аудита (детали — в roadmap):** этапы 18–20 подтверждены как полные и верные против стенограммы dogfood — ни одна проблема из неё не потеряна, отклонённые предложения модели-установщика (манифест id билдеру, мега-билдер) отклонены обоснованно, «направленность PPR» снята верно (`build_adjacency` симметризует). Найдено и занесено новое: **§1.50** (`extract_structure.load_config` падает `FileNotFoundError` без `config.yml` — нужна диагностика «AMG не установлен»; дом — Этап 18, новая задача 6), **§1.51** (`_text_units` ставит всем блокам `lineno: 1` — дом Этап 20, новая задача 6), **§1.52** (три дока-дрейфа: устаревший абзац о `log.md` в 07-consolidation → Этап 21 рядом с §1.36; `schema_version` 2→3 в 09-config и отсутствие `--env codex` в INSTALL.md → Этап 18). Дозаточены задачи: Этап 18 з.1 — **вето по сигнатуре движка** (`plan()`→`init()` «инициализирует» чекаут, проверка nodes/journal сама по себе обманывается), з.3 — `_set_scalar` только top-level (для `retrieval.embeddings.enabled` нужен вложенный сеттер; CLI без флага absorb_once); Этап 19 з.5 — summary-слой уже в `cache/index.sqlite`, мягкая деградация без эмбеддинг-бэкенда, з.8 — счётчик `skipped_invalid`, з.9 — метрики связности НЕ в доменно-слепом `graph_store` (в reconcile/инспекции + `lifecycle.status`), з.10 — ключ кэша + `working_language`; Этап 20 з.1 — `line_end` в очередь + порог крупного текста, з.2 — **ловушка requeue тривиальных юнитов** (нужна детерминированная авто-сводка, иначе вечный stale-цикл), з.4 — чанкинг и для synth/линковщика. Пост-аудитом (по вопросам пользователя): **Этап 22 поправлен** — Codex тестируется штатным `--env codex` (skill-aware, TOML-субагенты), а не `generic` (устаревшая формулировка до подэтапа 1.1.0); в Этап 21 добавлен **терминологический глоссарий ru→en** (первый артефакт этапа, двунаправленная нативность, предпочтительно остаётся постоянным справочником зеркал); в Этап 19 з.9 — **regression-фикстура «мини-проект» с детерминированной заглушкой-билдером** (конвейер целиком без модели, включая битые элементы для валидации apply).
- **Чистка по указанию пользователя выполнена:** из roadmap (§1.39–1.49, вводка этапов 18–20, задача 5 Этапа 19) убраны термины тестового проекта (`fastapi`/`starlette`, `tests/app`, «пример-контроллер») и точные счётчики узлов/рёбер тестового графа — заменены обобщёнными формулировками с сохранением сути (доли, «сотни компонент», «тысячи висячих»); стоимостной базлайн (~30 ч / ~70 Мтокенов на ~1 МБ) сохранён — он якорь DoD Этапа 20 (×20–30). CHANGELOG не трогался (историческая запись).
- **Два dogfood-пункта Этапа 17 по-прежнему отложены на живой проект ПОСЛЕ Этапов 18–20:** (1) реальная эмиссия паттернов живым `amg-synth` (зависит от линковщика Этапа 19); (2) флип `apply_hebbian`→true — ждёт реального `usage.log` с чисто пересобранного графа при удержании полноты. Оба — не в dev.
- **Стенды (НЕ удалять):** `../amg-bigtest` (`_measure_lazy.py` + `_measure_big.py`, README с находками), `../amg-testbed` (dogfood-песочница). Реальный dogfood-граф (~1 МБ проект) — приёмочный кейс Этапов 18–20; путь у пользователя (не в репо).
- **Среда/процедуры (durable):** torch/эмбеддинги РАБОТАЮТ, но ST-бэкенд нестабилен под тяжёлым пере-кодированием (сегфолт воркера — изолировать в subprocess, замеры по BM25+model2vec). Гейт типов: `python -m mypy` без аргументов → 17 модулей (exit 0). **Коммитить через Bash-heredoc** (PowerShell 5.1 ломает многострочный commit); пуш — `git push --follow-tags`. **Большие правки roadmap — частями, точным копированием из свежего Read; для удаления блоков — `sed` по номерам строк** (с `cp` бэкапом). `BASE-PROMPT.md` — gitignored (несёт дозаточенные аудитом строки загрузки Этапов 18–20). Не-Claude-Code-среды — Этап 22.
