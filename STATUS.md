# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап: Этап 21 (перевод документации на английский + финальный проход качества), сессия 1 выполнена.** Подготовительная (до-переводная) половина этапа в основном закрыта; сам перевод не начат — он финальный штрих после проходов качества.
- **Сделано в сессии 1 (v1.11.1 → HEAD), группами с коммитами:**
  1. **READMEs реструктурированы по запросу пользователя:** «Карта документации» — последний раздел (после «Лицензии»); «Что реализовано» — структурированный список без «планов»; +3 преимущества в «Чем AMG выделяется» (отбор по ценности информации, перенос по аналогии, диалоги как память); GUIDE — настоящая ссылка на INSTALL.md в карте.
  2. **§1.33/§1.34/§1.35 ЗАКРЫТЫ** (свёрнуты в roadmap): язык-универсальность кода подтверждена грепами (`--grep роутинг` нейтрализован); русские примеры `entrypoint/CLAUDE.md` → английские; правило «словесная просьба — операция памяти только при явной ссылке на память/граф памяти/AMG» + привязанные примеры во всех 3 блоках, GUIDE, READMEs, INSTALL, SKILL-триггерах (включая вьюер).
  3. **Проход (а)/(в)/(г) по ссылкам на roadmap ВЫПОЛНЕН ЦЕЛИКОМ:** ~245 stage/audit/roadmap-атрибуций удалены/заменены смыслом по всем скриптам движка, селфтестам, вьюеру, consistency-model.md, install.py и шаблону config.yml (6 легаси); SKILL amg-retrieve вычищен + починен разорванный список Reference; `test_stage13_fields_roundtrip`→`test_trust_fields_roundtrip`; греп `Stage \d|stage \d|audit 1\.|roadmap` по skills/agents/entrypoint/config.yml/install.py ЧИСТ. Батч-скрипт замен и бэкапы — в scratchpad сессии.
  4. **Перенумерация 18→21/19→22 дочищена:** CLAUDE.md (3), CHANGELOG.md (6, история перенаправлена; записи о настоящих Этапах 18–19 не тронуты).
  5. **Полнота GUIDE закрыта по коду:** `--intent`, все флаги `partition_queue`/`notes.py`, `migrate_schema.py`, `--pattern-demo`, `--compare-embeddings`, `--by-commit`, `lifecycle session-start/session-end [--transcript]`. INSTALL.md сверен со всеми флагами install.py — полон. **09-config сверен по запросу пользователя** с шаблоном И с ключами, читаемыми кодом: покрытие полное; дописана легаси-форма `sources:`.
  6. **Глоссарий создан** — `docs/en/glossary.md`, ~190 пар по доменам, правила во вводке. **Решение пользователя: сохранить (пригодится), но ссылок на него из документации НЕТ и не будет — упоминается только в roadmap.**
- **Осталось на Этап 21 (сессии 2+), до перевода:** (1) **прозаический регистр §3.4 + повторная зачистка §3.2** по всему корпусу docs/ru (рецидивы «деривации», батч/ресенд/чекпоинт/резюмируемость/линковка; тексты Этапов 8–20 — к регистру живого объяснения); (2) **нейтрализация `.claude/*`** гибридом (в командах конкретный путь, в прозе `<каталог агента>`, ведущая оговорка в каждом файле); (3) финальный §7-чеклист каждого файла со сверкой чисел/дефолтов с кодом. Затем — **перевод**: docs/en зеркально docs/ru строго по глоссарию (ссылки en→en), THEORY/GUIDE/architecture/README архитектуры; README.md — до-синхронизация; новые термины дописывать в глоссарий на месте.
- **Тесты:** 22 селфтеста движка + selftest_install + mypy(18) — зелёные после свипа (комментарии + 1 имя теста).
- **ВАЖНО (решения пользователя, действуют):** живой замер ×20–30 и связности — совместная приёмка Этапов 19–20 на dogfood-стенде, вне dev-сессий; глоссарий — без ссылок из доков; два dogfood-пункта Этапа 17 (эмиссия паттернов, флип `apply_hebbian` на реальном usage.log) — на живом проекте после Этапов 18–20.
- **Замечания пользователя (действуют всегда):** поставляемые артефакты и НОВЫЕ комментарии скриптов не ссылаются на этапы/аудит roadmap (легаси теперь вычищено — не вносить заново); хардкод весов/порогов — только осознанный; документацию писать регистром живого объяснения (§3.4 / CLAUDE.md §7 п.7); английское зеркало — native-level без калек с русского.
- **Стенды (НЕ удалять):** `../amg-bigtest`, `../amg-testbed`. Реальный dogfood-граф — путь у пользователя (не в репо).
- **Среда/процедуры (durable):** torch/эмбеддинги работают, но ST-бэкенд нестабилен под тяжёлым пере-кодированием (изолировать в subprocess). Гейт типов: `python -m mypy` без аргументов → 18 модулей. Коммитить через Bash-heredoc; пуш — `git push --follow-tags`. Большие правки roadmap — python по маркерам с бэкапом в scratchpad. В батарее селфтестов НЕ глотать вывод (`out=$(python "$f" 2>&1)`, печатать при провале). `BASE-PROMPT.md` — gitignored. Не-Claude-Code-среды — Этап 22.
