# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** Этап 7 (базовая синхронизация документации) **ЗАКРЫТ** (v0.9.0, 2026-06-15; тег `v0.9.0` — после явного «пушь» пользователя). Следующий — **Этап 8 (жизненный цикл и команды управления)**, не начат.
- **Что сделано на Этапе 7 (5 коммитов, документация; ядро не менялось, кроме докстринга `graph_store.py` и нового селфтеста):**
  - `c9ff39e` — `README_RU.md` + английский `README.md` (витрина: что такое AMG, две плоскости, граф-над-деревом, PPR-извлечение на запрос, бюджеты ярусов + сжатие/забывание веток, захват→консолидация, устойчивость к сбоям, умолчания, локальная/глобальная установка + `CLAUDE.md` + оговорка `.claude`/`.agents`, полуручной быстрый старт, mirror/absorb + приём, режимы/команды, зависимости). roadmap §2-преамбула + Этап 18 (README уже есть; финальный проход уровня Этапа 7 — на Этапе 18).
  - `212addb` — §3-язык по всем докам, грамматически-корректно, код-идентификаторы сохранены: `деривация`→`семантическое обогащение` (05 переименован, английский глосс при первом упоминании), `лок`→`блокировка`, `крэш-безопасность`→`устойчивость к сбоям`, `харнесс`→`измерительный стенд`, `хеш-гейт`→`фильтр по хешу`, `сырьё`→`материал`, `фронтматтер`→`frontmatter` и пр. Ноль остаточных калек.
  - `73a224b` — архитектура 01–10 под код: `cache/`+`log.md` в раскладке, честная формулировка самотестов, RST/NDJSON-fallback, точные хеши (функция/класс/модуль), `--no-pack`↔ко-активации (1.28), `models` declarative (1.14), `--make-demo` создаёт демо-граф, eval-gate реализован.
  - `616b8f6` — `consistency-model.md` (читается моделью при bootstrap): убраны фантомные `index.md`/`graph.json` (§4/§6/§7/§12 + докстринг `graph_store.py`), раскладка `doc/`+`data/`, `derived`→`synthesized`, absorb через `policy`, §11 log.md best-effort (1.15), `docs:`→`doc:`. Новый тест `case_documented_layout`. **Девять селфтестов зелёные.**
  - `fe24173` — THEORY/GUIDE/INSTALL: absorb переживает удаление источника; mirror перенос/переименование; «веса не градиентным спуском»; журнал решений помечен planned; `gap-report.md` в GUIDE; привязка опережающих разделов к этапам.
- **Ключевые решения Этапа 7:**
  - **`log.md` — best-effort** (решение 1.15, не транзакционный лог): пишет только `consolidate` (вне транзакции, `txid` в строке, без де-дупликации, ошибка проглатывается); `reconcile` не пишет. Корректность графа держит `journal/`, поэтому рваная строка безвредна. Описано в 02/03/07/consistency-model + поправлен шаг 6 SKILL не понадобился (оставлен — bootstrap отдаёт счётчики пользователю).
  - **«деривация» → «семантическое обогащение»** по всей прозе (по просьбе пользователя — режет слух); код-идентификаторы (`derived-*.json`, `apply_derivation`, `derived_from_hash`) не тронуты.
  - **`consistency-model.md` править безопасно:** не исполняется/не импортируется/не парсится кодом (только читается моделью при bootstrap + ссылки в докстрингах); фантомные `index.md`/`graph.json` использовались НИГДЕ (grep по всему коду и промптам — 0). Это единственный справочный документ в `skills/`.
- **Закрытие выполнено:** roadmap свёрнут (§2 пп. 2.1–2.5/2.7/2.9–2.12/2.14 → однострочники, 2.13 — лид-нота; §5 Этап 7 → «Выполнено»); VERSION 0.9.0; CHANGELOG; этот STATUS; `BASE-PROMPT.md` — строки Этапов 8/9/10 расписаны подробно (правимый механизм, соседи по данным, known-расхождения).
- **Следующий шаг — Этап 8:** управляющий слой, не ядро. Главное — `entrypoint/CLAUDE.md` (команды `/amg on|off|status|repair`, `automation`, always-on дайджест — блок ИСПОЛНЯЕТСЯ моделью, описывать только реализованное); хуки `SessionStart`/`SessionEnd` в `.claude/settings.json` (не в `config.yml`); новый дайджест из `consolidate.py` (5–10 активных решений/вопросов); `/amg status` из `graph_store.verify`. Закрывает 2.9 п.6, 2.12 п.1 (снять «Планируется (Этап 8)» в GUIDE), 2.16 п.3. Подробная строка загрузки — «Этап 8» в `BASE-PROMPT.md`.
- **Открытые вопросы / среда:** push тега `v0.9.0` ждёт явного «пушь» пользователя (push не делается автоматически). Окончания строк — часть файлов LF, git нормализует в CRLF (содержимое не страдает). `mypy --strict` — Этап 12 (1.31). sentence-transformers segfault под нагрузкой (Python 3.14) — не блокер.
