# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап:** **Этап 17 (semantic layer одного проекта) — ЗАКРЫТ и ВЫПУЩЕН (v1.8.0).** Следующий — **Этап 18 (развёртывание и разрешение store)**, первый из новой тройки 18–20 (см. ниже). **21 селфтест зелёный** (20 движка + `selftest_install`) + mypy(17) + eval-демо pack-recall 1.00 + pattern-демо. Пуш v1.8.0 `--follow-tags` — в этой закрывающей сессии.
- **Этап 17 — что закрыто (детали в roadmap §5 «Этап 17» свёрнуто + CHANGELOG [1.8.0]):** узлы-паттерны (Группа B: `retrieve.PATTERN_TYPES`, strategic-ярус, эмиссия `amg-synth` через `apply_derivation`+`exemplifies`, `eval_retrieval.pattern_metrics`/`--pattern-demo`, `selftest_pattern`); ленивая деривация A–C (Группа C: `config.yml derivation`, `partition_queue.priority_split`/`PRIORITY_KINDS`/`--priority`/`--usage`, `retrieve.stale_in_pack`, SKILL bootstrap/retrieve); замеры (Группа D): lazy off/on `_measure_lazy.py` (naive hop→0, first-touch 0.18 BM25 / 0.42 m2v → дефолт `eager`), Хебб регрессия `_measure_big.py` (дефолт `apply_hebbian:false` — реального usage.log нет), semantic-drift не нужен, hypervector — заметка THEORY §16. Новая теория: THEORY §13 (паттерны) + §16 (символьный канон vs гипервектор). Коммиты A–C `a5767d5`/`6947063`/`cd49eb0` + закрытие.
- **⚠️ ГЛАВНОЕ ДЛЯ СЛЕДУЮЩЕЙ СЕССИИ — по итогам dogfood на реальном проекте (~1 МБ: код+доки+тесты) появились ТРИ новых этапа 18–20 «производственная надёжность сборки».** Сборка реального графа вышла фрагментарной (322 компоненты, 1838 висячих рёбер, гайды 56/234) и разорительной (~30 ч, ~70 Мтокенов). Механический слой цел; дефекты — в семантическом конвейере, полу-реализованном §4.2 (calls/backbone/наследование не резолвятся/висят) и отсутствии контроля качества/воспроизводимости/стоимости. Полный разбор: **аудит §1.39–§1.49** + **§5 «Этапы 18–20»** + вводка «производственная надёжность сборки» + целевой 4-фазный конвейер. Перевод сдвинут → **Этап 21**, проверка сред → **Этап 22**; §1.37/§1.38 сведены в Этап 18.
  - **Этап 18** — разрешение store (`resolve_amg_root` перехватывает чужой `amg/`-чекаут/шаблон — §1.39; поэтому и искались `src`/`doc` из шаблона) + установка без захламления (движок ставить ВНЕ проекта, `install.py --target`; переписать README/INSTALL/GUIDE «из корня») + вопросы потока (absorb/исключения/показ конфига) + мерж глобального→локального конфига (§1.37) + gitignore движка (§1.38).
  - **Этап 19** — корректная связная сборка: **детерминированный глобальный пасс резолвинга ссылок (самый важный)** + backbone `defines` + наследование + сильный **глобальный смысловой линковщик** поверх summary + **эмбеддинг-кандидаты (ANN)** + приёмочный **гейт связности** + **derivation-cache** (воспроизводимость). Устраняет §1.40–§1.46. **Обязан быть lazy-aware** (гейт не считает отложенные `stale` фрагментацией; линковщик — инкрементально) — блок «Совместимость с ленивой деривацией» в теле Этапа 19 + §4.10.
  - **Этап 20** — экономная сборка ×20–30 без потери качества: **текст юнита в `queue.json`** (крупнейший выигрыш), тривиальные юниты, тонкая оркестрация, **чекпоинты/авто-чанкинг/авто-resume** билдеров, **честный сигнал успех/обрыв** (не писать «Done» при падении) + **прогресс в %**. Устраняет §1.47/§1.48.
- **Два dogfood-пункта Этапа 17 отложены на живой проект ПОСЛЕ Этапов 18–20** (записано в вводке «Этапы 18–20» → «Стенд воспроизведения»): (1) **реальная эмиссия паттернов** живым `amg-synth` — качество зависит от линковщика Этапа 19; (2) **флип `apply_hebbian`→true** — ждёт реального `usage.log` с чисто пересобранного графа при удержании полноты. Оба — не в dev (нужен живой субагент/реальные сессии).
- **Стенды (НЕ удалять):** `../amg-bigtest` — теперь несёт `_measure_lazy.py` (lazy off/on) рядом с `_measure_big.py` (Хебб); README дополнен обеими находками. `../amg-testbed` (dogfood). Реальный dogfood-граф (~1 МБ) — приёмочный кейс Этапов 18–20; путь у пользователя (я его храню не в репо).
- **Среда/процедуры (durable):** **torch/эмбеддинги РАБОТАЮТ**, но **ST-бэкенд нестабилен под тяжёлым пере-кодированием** (в `_measure_lazy.py` ST-воркер сегфолтит — как в `_measure_big.py`, где ST-eval изолирован в subprocess; замер lazy отчитан по BM25+model2vec). Гейт типов: `python -m mypy` без аргументов → **17 модулей** (предупреждение `[mypy-selftest_*]` давнее, exit 0). **21 прогон селфтестов зелёный** (20 движка + `selftest_install`), `eval --make-demo` pack-recall 1.00, `--pattern-demo` корректен. **Коммитить через Bash-heredoc** (PowerShell 5.1 ломает многострочный commit); пуш — `git push --follow-tags`. **Большие правки roadmap — частями, точным копированием из свежего Read; для удаления блоков — `sed` по номерам строк** (с `cp` бэкапом). `BASE-PROMPT.md` — gitignored (несёт строки загрузки для Этапов 18–20). Не-Claude-Code-среды — Этап 22.
