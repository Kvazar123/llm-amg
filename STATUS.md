# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** 3 закрыт (v0.5.0, 2026-06-14); следующий — Этап 4 (автоматический eval-gate для консолидации), не начат
- **Сделано в последней сессии:** закрывающая сессия Этапа 3 по протоколу §4.6 — синхронизирована документация (`THEORY.md` §8 переработан + новый §8.1 «почему Хебб по умолчанию выключен» с диаграммой петли самоусиления, §13; `07-consolidation.md` целиком — веса/план/применение/компрессия/ключи; `09-config.md` — `apply_hebbian`/`default_edge_weight`/константы плана; `GUIDE.md` — заметка «веса по умолчанию не обновляются»; `02-data-model.md` правок не потребовал — код приведён к нему; README-заметка про консервативные умолчания внесена в спецификацию README преамбулы раздела 2); DoD прогнан (7 селфтестов зелёные, 13 проверок консолидации); roadmap свёрнут (тело §5 Этапа 3 → однострочная сводка; пункты 1.8–1.11/1.17/1.20–1.23 → строки «исправлено на Этапе 3»; закрытые 2.8 пп.1–6 и 2.10 пп.3/5/8 → кратко); выпуск v0.5.0 (VERSION, CHANGELOG, аннотированный тег, пуш с тегами). Этап 3 целиком — 5 коммитов (3 рабочих: `7dfe3f4`/`1ae76ff`/`67876af`, + docs-синхрон + release)
- **Ключевое решение Этапа 3 (держать в уме):** хеббово обновление весов **по умолчанию выключено** (`weights.apply_hebbian: false`) — `consolidate weights` лишь копит `coact` (питает salience), проводимость статична; включается явно в конфиге после измеренного выигрыша eval. Причина — частичная цикличность сигнала ко-активаций (THEORY §8.1). Компрессия — отдельный флаг `compaction.enabled`
- **Следующий шаг (Этап 4 — автоматический eval-gate):** задачи 1–7 по roadmap (1–5 — сам gate: baseline до / повтор после / reject·revert·warn / отчёт / ключи `eval_gate.*`; 6 — заменить размеченные cases; 7 — решение о дефолте `apply_hebbian`). **Грузить по строке Этапа 4 в `BASE-PROMPT.md`** — она дополнена в этой сессии: соседи по данным `retrieve.py` (eval импортирует его) и `graph_store.py` (archive/revert), архитектура `10-eval-tools.md`/`07`/`06`, THEORY §14.1/§10.2–10.3/§8.1, скиллы `amg-consolidate`/`amg-consolidator`, и known-расхождения
- **Заметки для Этапа 4 (из Этапа 3):** (1) **`evals/cases.json` устарел** — ссылается на удалённый проект FastMVC (`gold_ids` висячие); ни один автотест его не читает (тесты на `build_demo_store` / inline-cases), поэтому Этап 2 он НЕ сбивал; для selftest Этапа 4 брать `eval_retrieval.build_demo_store`, постоянный файл переделать в шаблон, gate сделать робастным к пустой/мёртвой разметке (задача 6). (2) eval-gate — место измерить «weights on/off» и решить о дефолте `apply_hebbian` (задача 7; задача 14 Этапа 3 отложила это до измеренного выигрыша). (3) `selftest_consolidate.test_apply_and_recall` — уже прото-gate (recall до/после вручную), расширить. eval по природе неотделим от графа: `gold_ids` указывают на узлы, recall неизмерим без графа — для теста граф генерируется (`build_demo_store`), в проде граф пользовательский
- **Открытые вопросы / блокеры:** нет новых. Мелкое расхождение из Этапа 2 (`STATUS.md` в карте CLAUDE.md §2, но не в каноне 4.9) — свернуть при ближайшей правке 4.9
- **Песочница `../amg-testbed`:** не создана (наполнение — Этап 6)
