# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап: Этап 21 — ПЕРЕВОД по про-файловому протоколу. Сессия 4 закрыла ВСЮ ветку `docs/en/architecture/`** (README, 01–10, 12; коммиты `ff71f55`, `905e5c9`, `0f2fe3d`, `1665e09`, `bd44f23`, `8e1f53e`, `a3dd06b`). **Остаток: `THEORY` → `GUIDE` → до-синхронизация корневого `README.md` со сверкой против `README_RU.md`.** Протокол тот же (тело Этапа 21, блок «Оставшийся объём»): шаг 1 — вычитка ru (чеклист §7 + регистр §3.4, зачистка §3.1/§3.2-рецидивов, сверка чисел/дефолтов с кодом по карте «документ → скрипты», нейтрализация `.claude/*` гибридом); шаг 2 — перевод уровня носителя строго по `docs/en/glossary.md` (новые пары дописывать на месте; ссылок на глоссарий из доков НЕТ), ни одной потерянной детали, Mermaid переводятся, ссылки en→en (корневые цели: README.md / INSTALL.md), раскладка зеркальна.
- **Конвенция, применённая в сессии 4 (продолжать в THEORY/GUIDE):** шаг 1 дополнительно вычищает из ru-текста остаточные пометки «Этап N» / «аудит 1.xx» у УЖЕ реализованного (CLAUDE.md §7 — технический слой не разбрасывает пометки этапов; смысл заменяется формулировкой или ссылкой на теорию), при этом ссылки на разделы решений §4 roadmap (4.1/4.6/4.9/4.10) сохраняются, а привязки «(планируется)» — обязательны. THEORY уже чист от «Этап N» (память: theory-no-stage-refs).
- **Решение сессии 4 по `11-roadmap.md`:** живой рабочий документ остаётся русским; при ЗАКРЫТИИ этапа фиксируется английский снимок `docs/en/architecture/11-roadmap.md` с шапкой-оговоркой «снимок на момент vX; живой документ — русский» (ссылка из README.md уже есть).
- **Дефекты ru-оригиналов, исправленные вычиткой сессии 4** (полный список — пометка в теле Этапа 21): фантомное `chat` в `provenance.kind` (02, THEORY §15.2); пропуски `lifecycle.py`/`migrate_schema.py`/`digest`/`view`/самотестов в 01/07/08; устаревшие `usage.log`-абзацы в 03/08 (правило весов реализовано — читается при `apply_hebbian`); дефолт `amg_root` в 04 и `--store` в 10 — резолв цепочкой; состав `/amg status` дополнен (ветка/коммит, конфликты, connectivity); «Три команды»→«Команды» (05); «(заглушка)»→«(планируется)» (arch README). Глоссарий +2 пары (preservation rule, earned trace).
- **Раздел 1 аудита: открытых пунктов НЕТ.** §1.54 при закрытии этапа ужать до однострочника.
- **Решения пользователя (действуют):** глоссарий сохранить, ссылок на него из документации нет (только roadmap); живой замер ×20–30 и связности — совместная приёмка Этапов 19–20 на dogfood-стенде, вне dev-сессий; два dogfood-пункта Этапа 17 (эмиссия паттернов, флип `apply_hebbian` на реальном usage.log) — на живом проекте после 18–20.
- **Замечания пользователя (действуют всегда):** поставляемые артефакты и ЛЮБЫЕ комментарии скриптов без ссылок на этапы/аудит (легаси вычищено — не вносить заново; свежий греп сессии 4: чисто, включая кириллицу); хардкод весов/порогов — только осознанный; документацию писать регистром живого объяснения (§3.4 / CLAUDE.md §7 п.7); английское зеркало — native-level без калек.
- **Поведение при смене конфига (сверено по коду, для GUIDE-ответов):** конфиг читается заново каждым прогоном (слежения нет) — новые пути/политики подхватывает следующий `/amg sync`/петля; эмбеддинг-кэш самолечится, но для линковки после включения повторить sync; смена `working_language` задним числом НЕ пересводит (граф двуязычится) — потому KEY; тиринг `models` вступает в силу переустановкой.
- **Стенды (НЕ удалять):** `../amg-bigtest`, `../amg-testbed`. Реальный dogfood-граф — путь у пользователя (не в репо).
- **Тесты:** 21 селфтест движка + `selftest_install` + mypy(18) — зелёные после сессии 4 (правки только документационные).
- **Среда/процедуры (durable):** torch/эмбеддинги работают, но ST-бэкенд нестабилен под тяжёлым пере-кодированием (изолировать в subprocess). Гейт типов: `python -m mypy` без аргументов → 18 модулей. Коммитить через Bash-heredoc; пуш — `git push --follow-tags`. Большие правки roadmap — python по маркерам с бэкапом в scratchpad. В батарее селфтестов НЕ глотать вывод (`out=$(python "$f" 2>&1)`, печатать при провале). `BASE-PROMPT.md` — gitignored. Не-Claude-Code-среды — Этап 22.
