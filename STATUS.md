# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап:** Этап 12 — «производительность и масштабирование» — **В РАБОТЕ**. **Группы 1–4 закрыты.** **Осталось — ЗАКРЫТИЕ ЭТАПА** (doc-sync + сворачивание §1/§5 + релиз v1.3.0). НЕ создавать отдельный подэтап перед 13: остаток — незакрытый Этап 12.
- **Готово в этой сессии:**
  - **Группа 1 (хвост):** `bench.py`+`selftest_bench.py` (задача 9); барьер «Boundaries» в 3 блоках + 08 (задача 10, реестр 2.16 п.6 закрыт); задача 4 подтверждена. Коммиты `291b082`, `a44f21a`.
  - **Группа 2 (ИНДЕКС, задачи 1–2):** `index_store.py`+`selftest_index.py`; `cache/index.sqlite` под `retrieve.load_nodes` (читатель сверяет сигнатуру в мета-таблице sqlite, иначе безусловный скан+пересборка, сигнатура ДО скана); эжер-инкрементальный `refresh_after_commit` у 6 писателей через `graph_store.Transaction.node_paths()`; форма `load_nodes` идентична. **15× на 7600 узлах.** Задача 3 (adjacency-кэш) ОТЛОЖЕНА по измерению. Коммит `e03303b` (+`test(bench)` фикс комментария).
  - **Группа 3 (задачи 11, 13, 5–6, 7):** §1.27 — `make_plan` near-dup сужен до эпизодических не-derived (`test_near_dup_scope`); задача 13 — `amg-builder` эхоит `content_sha`, `reconcile.apply_derivation` сверяет с текущим `source_hash` → `skipped_stale`, SKILL «применить остатки `derived-*.json` первыми» (`case_resume_freshness`); задачи 5–6 — новые `partition_queue.py`/`inspect_queue.py` + `selftest_queue.py`, SKILL шаг 3 указывает на них; задача 7 — блоки на месте, переключатель НЕ введён. Коммит `5021b96`.
  - **Группа 4 (задача 8):** шаблон `config.yml` блок `models` → структурная форма с градиентом усилия (`discovery: {haiku, low}`, `synthesis: {opus, high}`, `module_summary: sonnet` ПЛОСКИЙ — питает извлечение, не резать без замера); протокол замера тиров в `09-config`. Живой замер отложен. `selftest_install` зелёный. **Провайдерские модели в `model` уже работают** (непрозрачный пас-сквозь, §1.14/v1.1.0; routing — уровень развёртывания, не AMG; Claude Code #44385 на `model:`; Codex/живая проверка — Этап 19).
- **Уточнения по развилкам (изучением/измерением) — отступления от STATUS:**
  - Сигнатура индекса ВНУТРИ sqlite (атомарность); тело узла ХРАНИТСЯ (форма `load_nodes` идентична).
  - **Задача 3 (adjacency-кэш) ОТЛОЖЕНА по измерению:** `build_adjacency` 23–49 мс vs `load_nodes` 4–8 с (<1%); сериализация блоба рискует net-negative. Вернуть только если замер на 10⁴⁺ опровергнет. (Согласовано с пользователем — он не возражал.)
- **Решения по развилкам (ранее зафиксированы):**
  - **Задача 8 (Группа 4):** per-role дефолты + `reasoning_effort`; живой замер отложить.
  - **mypy селфтесты:** ИСКЛЮЧЕНЫ из гейта. Гейт: `python -m mypy` без аргументов → **15 модулей** движка.
- **ОСТАЛОСЬ в Этапе 12:**
  - **Группа 4 — задача 8:** в шаблон `config.yml` блок `models` добавить структурную форму с `reasoning_effort` (низкий discovery, высокий synthesis); задокументировать протокол замера (eval off/on по тирам; дешёвая модель там, где eval не проседает; reasoning не урезать, где качество падает). **Живой (платный) замер отложить** (как Хебб / Этап 19) — здесь дефолты + метод. Механизм 1.14 уже готов (установщик рендерит per-role в субагентов).
  - **Закрытие этапа (doc-sync + сворачивание + релиз v1.3.0).**
- **К ЗАКРЫТИЮ этапа:** синхр. `03`/`06`/`09`/`10` + THEORY/GUIDE/READMEs под индекс/задачи; **индекс задокументировать как пользователю** (см. память `stage12-index-doc-at-closure`: проекция не весь граф; только скорость; автоматический/без порога/disposable; измеренный кроссовер ~13–15× с десятков узлов); **индекс в раскладку** (`02-data-model`/`03-storage`/`consistency-model §4`/`06-retrieval`); индекс **без конфиг-ключа** (автоматический — отметить в 09); **adjacency-кэш отложен** — отразить в §4.1/06; новые хелперы `partition_queue`/`inspect_queue` в 01-overview карту модулей/08; задача 13 (`content_sha`/`skipped_stale`) в 05-reconcile/08; **дрейф `episodic_types`** в `07-consolidation` §«План» (~стр. 57) и таблице (~стр. 149): `[section, note]` → `[section, note, open_question, plan]`; строка Этапа 13 в `BASE-PROMPT.md`; свернуть §1.15/1.27/1.31 раздела 1 и тело §5 Этапа 12; VERSION/CHANGELOG/тег `v1.3.0`.
- **Среда/процедуры:** **torch/эмбеддинги РАБОТАЮТ**. Гейт типов: `python -m mypy` (без аргументов; предупреждение `[mypy-selftest_*]` — давнее, безвредно, exit 0). **17 прогонов селфтестов зелёные** (16 движка + `selftest_install.py`), `eval --make-demo` hop-recall 1.00. **Коммитить через Bash-heredoc** (PowerShell 5.1 ломает многострочный commit). В фикстурах/бенче `embeddings: off`. Не-Claude-Code-среды — Этап 19.
