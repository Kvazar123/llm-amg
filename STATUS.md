# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (протокол сессии, CLAUDE.md §4.5). Назначение: мостик между `/clear`-сессиями — модель читает его третьим файлом и сразу видит, где остановились.

- **Текущий этап:** Этап 8 (жизненный цикл и команды управления) **ЗАКРЫТ** (v0.10.0, 2026-06-15; тег `v0.10.0` — после явного «пушь» пользователя). Следующий — **Этап 9 (сохранение сессий)**, не начат.
- **Что сделано на Этапе 8 (3 группы-коммита: код → артефакты → документация):**
  - `7ab1ade` — ядро управляющего слоя: `consolidate.write_digest` + CLI `digest` (дайджест `<amg>/digest.md`, 5–10 `decision`/`adr`/`open_question` по `salience`, до 6+4; обновляется в `weights`/`apply`); новый `lifecycle.py` (`session-start`/`session-end`/`status`/`on`/`off`/`repair`, гейт `active`+`automation`); `selftest_lifecycle.py`.
  - `cc9de45` — управляющие артефакты: `entrypoint/settings.json` (хуки SessionStart/SessionEnd → `lifecycle.py`), `entrypoint/commands/amg.md` (единая `/amg`-дверь, рабочие глаголы делегируют скиллам, `disable-model-invocation`), блок активации env-aware (импорт дайджеста, таблица операций с синонимами, `automation`, заметки про хуки), `sync_testbed.py` (раскладывает settings/commands, засевает `digest.md`), `automation: on`→`true`.
  - `<docs>` — документация: 08 («Слой жизненного цикла» — основной), 09 (`automation`), 02 (`digest.md`), GUIDE, THEORY §13 (дайджест как пассивный канал памяти), README/README_RU; roadmap свёрнут (§5 Этап 8 → «Выполнено»; 2.9 п.6 / 2.12 п.1 / 2.16 п.3 закрыты); карта Этапа 10 (4.9 / задача 8 / 2.13 / 2.15 / 2.16) + BASE-PROMPT. **Десять селфтестов зелёные.**
- **Ключевые решения Этапа 8:**
  - **`/amg` — единая «парадная дверь»** (расширение roadmap-набора задач 4–5 по просьбе пользователя): управляющие глаголы (`status`/`on`/`off`/`repair`) → `lifecycle.py`, рабочие (`sync`/`retrieve`/`consolidate`) → профильные скиллы (доступны и напрямую `/amg-bootstrap` и т. д.); `disable-model-invocation` (только пользователь); синонимо-толерантные словесные триггеры в блоке.
  - **Хуки — только детерминированная половина**: субагента хук запустить не может, поэтому суждательная консолидация + выгрузка сессии (Этап 9) остаются за моделью. SessionStart → исцеление+дайджест, SessionEnd → веса+дайджест. При `apply_hebbian:false` авто-свёртка безопасна (копит `coact`).
  - **Дайджест — пассивный канал памяти**: страховка «память есть, но к ней не обратились», грузится импортом `@.claude/amg/digest.md` **до хуков и помимо них** (несёт сам всегда-загружаемый файл точки входа). `session-start`/`session-end`/`weights`/`apply` держат его свежим; `sync_testbed` засевает пустой.
  - **`automation` — булев `true`/`false`** (не `on`/`off`), единообразно с `active`.
  - **Переносимость управляющего слоя — глубже замены папки**: хуки / слэш-команды / `@`-импорт — механизмы Claude Code (в Codex может не быть); универсальный субстрат — петля + словесные триггеры + прямой вызов `lifecycle.py`/скиллов. Параметризация `agent_dir`/`entrypoint` новых артефактов оставлена **Этапу 10** с железной картой (4.9 / задача 8 / 2.13 / 2.15 / 2.16 / BASE-PROMPT). Нюанс отражён в 08 / GUIDE / README / README_RU / блоке активации.
- **Claude Code mechanics сверены с докой** (code.claude.com): схема хуков `settings.json` (SessionStart матчеры startup/resume/clear/compact; **SessionEnd не инжектит контекст**), слэш-команды `$ARGUMENTS` + frontmatter (`disable-model-invocation`, `allowed-tools`), `@`-импорт `CLAUDE.md` (относительные пути от файла, глубина 4, HTML-комментарии вырезаются, отсюда комментарий-маркер в `digest.md` безвреден). Живой прогон хуков — интеграционно в песочнице; `sync_testbed` укомплектовал `../amg-testbed`, `/amg status` на 33-узловом графе показал все поля.
- **Установщик — Этап 10, не здесь** (по вопросу пользователя): запуск по запросу к модели / CLI, ключевые вопросы (local/global, `agent_dir`/`entrypoint`, mirror/absorb, `automation`, язык, эмбеддинги, сессии, бюджеты), глобальный→локальный конфиг — задачи 1–4 Этапа 10. Новое к Этапу 10: рендеринг артефактов Этапа 8 + засев дайджеста + развёртывание механизма под среду (задача 8).
- **Следующий шаг — Этап 9:** захват диалога в `sessions/` + ингест сессий как обычного источника. **Блокер DoD — 1.18** (`DEFAULT_IGNORE_DIRS` содержит `.claude` → `iter_source_files` молча отбросит `absorb_path: .claude/amg/sessions`; нужно исключение для путей внутри своего хранилища). Новый нарезатель сессий в `extract_structure` (реестр `CHUNKERS`). `lifecycle.py session-end` уже есть — Этап 9 добавит к нему выгрузку стенограммы. Подробная строка — «Этап 9» в `BASE-PROMPT.md`.
- **Открытые вопросы / среда:** push тега `v0.10.0` ждёт явного «пушь» пользователя. Окончания строк LF/CRLF — git нормализует (содержимое не страдает). `mypy --strict` — Этап 12 (1.31). sentence-transformers segfault под Python 3.14 — не блокер.
