# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап:** Этап 12 — «производительность и масштабирование» — **В РАБОТЕ** (Группа 1 почти закрыта; Группы 2–4 не начаты). НЕ создавать отдельный подэтап перед 13: остаток — это просто незакрытый Этап 12, он весь перечислен ниже.
- **Закоммичено в этой сессии (всё в `main`):**
  - `9b4cbef` docs(roadmap): **§4.10** — полный план ленивой деривации (задача 7); задача 7 Этапа 12 переформулирована (блоки сейчас → §4.10), Этап 17 получил задачу-дом.
  - `8524939` fix(store): **задача 14** — транзакционный `log.md` (`GraphStore.append_log`: дедуп по txid + ротация; `consolidate`+`reconcile` пишут под гейтом `if txid`); доки `03-storage`/`consistency-model §11` синхронизированы; `selftest_graph_store.case_action_log`. §1.15 закрыт.
  - `c34c4bd` refactor(types): **задача 12** — 11 модулей движка проходят `mypy --strict`; `mypy.ini` (`files=` фиксирует движок) + dev-группа в `requirements.txt`.
  - **след. коммит (этот):** `mypy.ini` гейт через `files=` (тесты исключены по решению), пометки roadmap §5/§1.15, STATUS. **Запушить с этим.**
- **Решения по развилкам (ЗАФИКСИРОВАНЫ):**
  - **Индекс (Группа 2):** строить СРАЗУ; `cache/index.sqlite`; эжер-обновление писателями + дешёвая валидация в читателе (`retrieve.load_nodes` сверяет сигнатуру `nodes/` stat-обходом, при несовпадении/порче — скан+пересборка, скан-fallback безусловен); BM25 оставить Python-овским (не FTS5), чтобы не сдвинуть eval; форма `load_nodes` не меняется.
  - **Задача 7:** скрытый переключатель `derivation` НЕ вводим; блоки дают 5/6/13; полная лень → §4.10 / Этап 17.
  - **Задача 8:** per-role дефолты + `reasoning_effort`; живой замер отложить.
  - **mypy селфтесты:** ИСКЛЮЧЕНЫ из гейта (test-код). Канонический гейт: `python -m mypy` без аргументов (читает `files=` из `mypy.ini`) → 11 модулей движка.
- **ОСТАЛОСЬ в Этапе 12 (детально, для свежего контекста):**
  - **Группа 1 (хвост):**
    - задача 4 (валидация embedding-кэша по хешу) — **по факту УЖЕ реализована** в `embed.node_embeddings` (хеш-гейт `_text_hash(node_text)`), описана в `06-retrieval`. Осталось: подтвердить + при желании 1 регресс-проверка. Фактически done.
    - задача 9 (бенчмарки) — НОВЫЙ скрипт `bench.py`: число узлов / время retrieval / bootstrap / eval / index-rebuild; масштабировать `../amg-bigtest/gen_big.py` до 10–50k узлов как стенд.
    - задача 10 (мягкий барьер) — уже есть в `entrypoint/CLAUDE.md:106` + `skills/amg-bootstrap/SKILL.md:122` + `08-agents-skills.md` (петля). Осталось: выверить/усилить формулировку; закрыть реестр 2.16 п.6.
  - **Группа 2 — ИНДЕКС (ядро DoD, НЕ начато):** задачи 1–3. Новый `skills/amg-retrieve/scripts/index_store.py` (+`selftest_index.py`): строит `cache/index.sqlite` (id/type/status/summary/source_path/lineno/searchable-text/edges-json/part_of-json) + кэш adjacency (инвалидация по версии графа + хешу `relation_priors`/`default_edge_weight`); `retrieve.load_nodes` читает индекс при свежести, иначе скан+пересборка; best-effort refresh-хук у ВСЕХ писателей (`reconcile.plan`/`apply_derivation`, `consolidate.apply_actions`/`fold_weights`, `notes.add_note`, `migrate_schema`) после `tx.commit()`. DoD: малый граф прост; большой не парсит все `.md`; индекс удаляем/пересобираем.
  - **Группа 3 (НЕ начато):** задача 11 (`consolidate.make_plan`: ограничить near-dup кандидатов типами `episodic_types` — 2-строчная правка, §1.27); задача 13 (resume: билдер эхоит `content_sha` в derived-элемент [правка `agents/amg-builder.md`], `reconcile.apply_derivation` сверяет `content_sha`↔текущий `source_hash`, skip протухшего; SKILL «применить остатки `derived-*.json` первыми»); задачи 5–6 (`partition_queue.py`/`inspect_queue.py` в `amg-bootstrap/scripts`, замена ad-hoc нарезки очереди в `SKILL.md:90`); задача 7 (минимальные блоки — см. выше).
  - **Группа 4 (НЕ начато):** задача 8 — в `config.yml` блок `models` добавить `reasoning_effort` (низкий discovery, высокий synthesis) + задокументировать протокол замера; живой замер отложить.
- **К ЗАКРЫТИЮ этапа (doc-sync + сворачивание + релиз v1.3.0):** синхр. `03`/`06`/`09`/`10` + THEORY/GUIDE/READMEs под индекс/задачи; **дрейф `episodic_types`** в `07-consolidation` §«План»/таблица (`[section, note]` → `[section, note, open_question, plan]`); остаточные «best-effort log» упоминания в `02-data-model` (диаграмма+таблица) и `THEORY §9`; строка Этапа 13 в `BASE-PROMPT.md`; свернуть §1.15/1.27/1.31 и тело §5 Этапа 12; VERSION/CHANGELOG/тег `v1.3.0`.
- **Среда/процедуры:** **torch/эмбеддинги ТЕПЕРЬ РАБОТАЮТ** (пользователь переустановил пакет) — прежняя пометка «torch не грузится» УСТАРЕЛА; `embed.py` диагностика OK (model2vec+st грузятся). Гейт типов: `python -m mypy` (без аргументов). **13 селфтестов + `selftest_install.py` зелёные**, живой CLI-цикл (bootstrap→retrieve→consolidate) чист. **Коммитить через Bash-heredoc** (PowerShell 5.1 ломает многострочный commit). `retrieve` с `embeddings: auto` грузит реальную модель (медленно на первый раз) — в фикстурах ставить `embeddings: off`. Не-Claude-Code-среды — Этап 19.
