# STATUS — состояние работ по AMG

Обновляется в конце каждой сессии (CLAUDE.md §4.5). Мостик между `/clear`-сессиями.

- **Текущий этап:** **Этап 17 (semantic layer одного проекта) — В РАБОТЕ.** Этап 16 закрыт/выпущен (v1.7.0). **21 селфтест зелёный** (20 движка +`selftest_install`; добавлен `selftest_pattern`) + mypy(17) + eval-демо/compare/pattern-демо зелёные. НЕ запушено (пуш — на закрытии).
- **Этап 17 — ВАЖНО: реформирован по ходу сессии (исправлена давняя ошибка интерпретации).** «Separate memory scopes» (§4.7, задача 2) НЕ про общую память между репозиториями — это была моя ошибочная эскалация (фраза «cross-project» встречалась 1 раз и противоречила инварианту «граф между проектами не делится», заявленному в INSTALL/GUIDE/12-install). Верно: **внутрипроектные слои по источнику**, причём атрибуция уже даётся `provenance.kind` (Этап 13) → отдельная scopes-подсистема НЕ нужна (YAGNI). **Задача 2 растворена**; `global`-слой = pattern-узлы (задача 3); приватность личной памяти → опциональный наследник Этапа 16. Правки карты: §4.7 переписан, цель/задачи 2–4, §8 «Возможное развитие» (межрепо-память отклонена + агрегатор-спекуляция).
- **Решения по задачам:** 1 (embeddings/eval) **выполнено** (Группа A: верифицировано, нового кода нет — off/on-протокол=`eval_retrieval.run`); 4 hypervector → исследовательская заметка без кода; 6 semantic-drift по измерению боли; 7 ленивая деривация **A–C**; 8 Хебб на доступных данных (`amg-bigtest`/dogfood), дефолт `apply_hebbian:false` остаётся (реального `usage.log` НЕТ — флип только на живом проекте).
- **Группы и порядок:** A (закрыта) → B pattern-узлы+метрики (код) → **C ленивая деривация A–C (ВЫПОЛНЕНА, код)** → D замеры Хебб/drift + заметка hypervector. Доки (02-data-model/THEORY/GUIDE/READMEs) — на закрытии.
- **Группа C — что сделано (код, зелёный):** ключ `derivation: eager|lazy` (config.yml, дефолт `eager`); `partition_queue.priority_split` + CLI `--priority`/`--usage` (фаза A: карта `module/class/package/file` деривируется, листья `function/section/record` откладываются; фаза C: `usage.log` поднимает used-узлы); `retrieve.stale_in_pack` + CLI-вывод (фаза B); промпты `amg-bootstrap` SKILL (A+C) и `amg-retrieve` SKILL (B: довести stale_in_pack до ответа). Предохранители: скелет+синтез никогда не откладываются, первое касание синхронно, дефолт `eager` (фаза D — только по измерению на большом графе, нет в dev). Тесты: `selftest_queue.test_priority`, `stale_in_pack` в `selftest_retrieve`. **Создание движком не ветвится на флаг** — `derivation` это skill-ветвление + инструменты (безвредны в eager). Осталось: dogfood lazy-цикла — на закрытии/Этап 19.
- **СЛЕДУЮЩАЯ СЕССИЯ = Группа D + закрытие Этапа 17.** Полный durable-план (handoff) — в `11-roadmap.md`, блок **«Этап 17 — Группа D + закрытие: план следующей сессии»** (под DoD Этапа 17): з.8 Хебб (перемерить на `../amg-bigtest`, **дефолт `apply_hebbian:false` остаётся** — реального `usage.log` нет); **НОВЫЙ замер lazy off/on на `../amg-bigtest`** (3 условия eager/naive-lazy/lazy+first-touch — доказать численно, что «первое касание синхронно» держит качество, а голая лень роняет); з.6 semantic-drift (измерить боль, вероятно не нужен); з.4 hypervector (заметка в THEORY, без кода); закрытие (доки → DoD → сворачивание → релиз **v1.8.0** + пуш). **GUARD: scopes — внутрипроектные, НЕ межрепо-память (§4.7 + раздел 8) — не повторять эскалацию «общей памяти для нескольких проектов».** `BASE-PROMPT.md` несёт строку загрузки файлов для этой сессии. **§1.37/§1.38 — ВНЕ Этапа 17** (отдельная config/install-сессия 17→18: мерж глобального→локального конфига + gitignore-движок).
- **Группа B — что сделано (код, зелёный):** `retrieve.PATTERN_TYPES` {architectural_pattern/recurring_fix/anti_pattern/migration_recipe} + strategic-ярус в `TIER_OF_TYPE`; `eval_retrieval`: `build_pattern_demo`+`pattern_metrics`(transfer_recall/false_analogy_rate/stale_pattern_rate)+CLI `--pattern-demo`; новый `selftest_pattern` (4/4); промпт `amg-synth` — эмиссия pattern-узлов create-элементом + `exemplifies`-рёбра НА ЭКЗЕМПЛЯРАХ (instance→pattern), консолидатор НЕ трогали (YAGNI). **Создание паттернов идёт существующим путём `apply_derivation` (тип-агностичен), нового кода движка нет.** Осталось в B: dogfood-проверка эмиссии в `../amg-testbed` (нужен живой субагент) — на закрытии/Этап 19.
- **OUT-OF-STAGE (отдельная config/install-сессия между 17 и 18):** **§1.37** — глобальный `config.yml` + наследование локальным НЕ реализованы, хотя INSTALL.md стр.60 это обещает (расхождение док↔код); решение **A** (реализовать мерж: глобальный `~/<agent_dir>/amg/config.yml` через `resolve_amg_root` для разных сред; глобальный=личные умолчания, локальный=проектные в git-каноне). **§1.38** — рекомендованный `.gitignore` Этапа 16 не покрывает движок при локальной установке (закоммитился бы; при глобальной — чисто, движок вне репо); правка GUIDE. Оба — НЕ Этап 17.
- **Этап 16 — что реализовано (детали свёрнуты в roadmap §5 «Этап 16»; CHANGELOG [1.7.0]):**
  - **Хранилище под общую папку:** `graph_store._lock_is_stale` host-aware (чужой-host lock не крадётся локальной пробой pid — только по возрасту; одномашинный путь байт-идентичен); мягкая `StoreLockError`-деградация в `lifecycle._heal`/`session_end`/`repair` и в `graph_store` CLI (`recover`/`verify --repair`); явные писатели падают с сообщением о владельце.
  - **Устойчивость к конфликтам:** три парсера (`reconcile.parse_node`/`retrieve._parse`/`consolidate._parse`) защищены от `yaml.YAMLError`+non-dict — узел с git-конфликт-маркерами пропускается, не валит `load_nodes` (`export_graph` был защищён — прецедент); `reconcile.find_conflict_markers` (`<<<<<<<`/`|||||||`/`>>>>>>>`, `=======` исключён ради setext) + поверхности `plan`/`status`/`repair`/`session_start`.
  - **Git-aware:** `reconcile._git_branch`/`_git_changed_since` (best-effort, `--relative`); ветка/commit в `lifecycle.status`; `verify_claims.verify_by_commit` + `--by-commit` (актуальность источника по коммиту: `git diff` на distinct ingest-коммит, без пере-нарезки, дополняет авторитетную хеш-сверку). Ветка — на уровне store, `_provenance` НЕ тронут.
  - **Доки:** GUIDE «Командная работа», READMEs (RU+EN, раздел в теле + перенос из планов в реализованное), `consistency-model §9`/§12, `03-storage`, `02-data-model`, `06-retrieval`. Сравнение веток — документированный git-паттерн (`git diff` / `export_graph --json`), без нового кода. Новой теории нет (git над markdown-каноном §4.1 инструментален). Строка Этапа 17 в `BASE-PROMPT.md` готова.
  - **Переносимость:** все правки — в универсальных скриптах (`graph_store`/`lifecycle`/`reconcile`), НЕ в хуках → работают и в бесскиловых/Codex-средах без сессионных хуков (петлю ведёт модель прямыми вызовами). Тесты: `case_lock_cross_host`, `case_shared_folder_contention`, `case_merge_conflict_resilience`, `test_conflict_skip`, `case_merge_conflict_surfaced`, `case_freshness_by_commit`, расширен `case_status`.
- **Отложенные дефекты промптов/косметики (Этап 18):**
  - **§1.34** — захардкоженные русские примеры намерения в `entrypoint/CLAUDE.md` (столбец «Verbal intent» + пары синонимов); прочие блоки (`AGENTS.md`/`AGENTS.codex.md`/`commands`) и `agents/*.md` уже английские.
  - **§1.35** — словесные команды памяти не привязаны к ключу «AMG»/память (общая фраза «покажи граф»/«подведём итоги» может ложно запустить `/amg`); привязать триггеры во всех блоках+доках.
  - **§1.36** — `log.md` это плоский лог, а не markdown-документ → вести обычный `.log` без `## `-заголовков (формат `[ts] txid source | msg`); затрагивает `graph_store.append_log` (имя+формат+ротация), `lifecycle._last_consolidation`/`status`, `verify_claims`/`consolidate`/`reconcile` (вызовы не меняются), `selftest_graph_store.case_action_log`, доки (`02`/`03`/`consistency-model §11`/`01`). Косметика, не дефект целостности. Отдельным `z`-исправлением или в Этап 18.
- **Перенос графа между средами (вопрос пользователя, Этап 15):** копированием папки `<agent_dir>/amg/` — `nodes/` это канон markdown, env-агностичный (id и `source_path` проектно-относительны); править лишь `agent_dir`/`entrypoint` в `config.yml`, кэши (`cache/`/`journal/`) пересобираемы. JSON-импорт НЕ нужен (был бы лоссовым обходом канона; задокументировано в GUIDE «Командная работа»). JSON-экспорт (`--json`) остаётся для сторонних инструментов анализа.
- **Среда/процедуры (durable):** **torch/эмбеддинги РАБОТАЮТ**. Гейт типов: `python -m mypy` без аргументов → **17 модулей** (предупреждение `[mypy-selftest_*]` — давнее, безвредно, exit 0). **20 прогонов селфтестов зелёные** (19 движка + `selftest_install`), `eval --make-demo` pack-recall 1.00. **Коммитить через Bash-heredoc** (PowerShell 5.1 ломает многострочный commit); пуш на закрытии — `git push --follow-tags`. **Большие правки roadmap делать частями с точным копированием из свежего чтения** (мега-абзацы легко ломаются опечаткой при ручной сборке `old_string`; для удаления блоков надёжнее `sed` по номерам строк). Стенды `../amg-bigtest` и `../amg-testbed` **НЕ удалять** (замер Хебба на реальном usage — Этап 17 з.8). `BASE-PROMPT.md` — gitignored. Не-Claude-Code-среды — Этап 19.
